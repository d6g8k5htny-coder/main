# Marked cylinder cap and consecutive palettes — 2026-09-24

OpenAI / ChatGPT, author-side mathematical candidates. Campaign/dispatch: #61.
This three-file Git delivery supplies a hash-first executable pickup; the exact
proofs, mathematical code, 36-test suite, mutations and retained evidence are in
the pinned Drive capsule. It does not alter existing research code or any source
status. It is not a new governance or admission engine.

## Actual mathematical advances

CAP-01 weakens the 2D sufficient transverse-depth event from
`lambda > 64 r (1+M3)^2` to `lambda > 8 r M3^2`, retaining `r M4 <= 1/20`.
The pins force `M3 >= 2`. A sufficient reduced-ridge third derivative is now
`F'' > 1/4`, with longitudinal clearance `11 r^3/96`. Every cap boundary is
controlled, not just paths staying in the chart. The resulting fixed-axis SIDE24
six-pin candidate has `1-p_elder <= min(1,2.4e23 r^3)` for `0<r<=1/20`, conditional
on the explicit predecessor normalizer/moment/genericity inputs. Both exact
probability coefficients improve by over 500; the absorbed ratio is about
511.803. The bound is still vacuous at0.05 and is below0.24 at1e-8.

CAP-02 supplies a full vector-ridge/cylinder proof for every dimension d>=2.
For gap `kappa r^3`, the conditions are
`lambda_min(-Hyy(M)) > [4/(3 kappa)] r M3^2` and `r M4 <= 3 kappa/10`, using the
proof's partial-block tensor operator norms. The geometry permits any positive
mark and orthonormal frame. That does NOT prove a higher-dimensional or all-angle
Gaussian probability bound. The actual eight-pin transverse-matrix boundary
estimate remains a separate task. The ridge is not a gradient trajectory.

PAL-01 proves the exact palette formula
`K = ceil(max(max_i d_i, max_j d(A_j)/c_j))` when all complete capacity sets are
consecutive in a supplied block order. Positive-demand zero-capacity sets are
infeasible. A cyclic demand-segment construction gives a compressed solution,
including some nonmatroids. The triangle is rejected. The six-block408-demand
adjacent-pair example has optimum816, under DIFFERENT supports from #59's matroid
example. Actual P15-B support completeness and compatible local covers remain
explicit premises. Interval balanced coloring is established background, not a
project novelty claim.

## Exact delivery and source identity

Cap proof: https://drive.google.com/file/d/1BnPods7Lf-ECdD34noQihZcEfcqpy7R5/view
15160 bytes; SHA256 `0bf922b9203c29088b12388807aa0e2ecd020485eb0f6e919679841b5b2636fc`.

Palette proof: https://drive.google.com/file/d/1jvV6kORTB7umKrCFAGSoQ385sUgBAKn5/view
9335 bytes; SHA256 `6abcc0607e7b4b3cfdcecec854aa2920bb3f830a238ac12e27d18babf64707eb`.

Capsule: https://drive.google.com/file/d/1soVnQdYxLU8LHcQzteabic_Bdn6fD_dE/view
86308 bytes; SHA256 `795115c377b9d37b87f953555f86c7f3f4b514298551e110391f3cba9e03340f`.

Individually readable files: https://drive.google.com/drive/folders/1vWBxZZzLNz4BTNXX0Fvjv0Y4AWpalDIv .

## Execution

Use an authorized Drive connector to obtain the capsule. The script does not
require network access or install dependencies. From this directory:

```sh
python -B -S -m unittest -v test_replay_capsule
python -B -S replay_capsule.py /path/to/MATH_PUSH_CAP_PALETTE_20260924_v1.zip \
  --output /absolute/new/directory/outside/working/tree
```

The CLI executes only the exact pinned ZIP after checking size, SHA256, CRC,
member safety and expansion limits. It refuses an existing output. It invokes
the capsule's manifest verifier and normal/optimized validation; all output is
written to the new directory. Never substitute another ZIP by changing the pin
without a separately recorded successor and evidence.

Author execution: Python3.13.5, 36 distinct mathematical unittest methods in each
mode (18cap/18palette), 9 distinct semantic mutations caught in both modes,
5184+288 exact palette instances against a separate residual-demand DP, and900
finite scalar cases. Eight additional synthetic transport tests passed in both
modes. The wrapper replayed the actual downloaded capsule successfully. ZIP and
both proof readbacks matched;113 manifest payloads verified. Replays are not
additional distinct tests. First orchestration failure remains preserved: two
mutants initially raised a semantic ValueError, not the runner's required
assertion failure; the positive-case test diagnostic was strengthened without
changing mathematical code.

No original H3/LPW interval replay, full repository CI, Python3.11 execution in
this author session, independent mathematical acceptance, 3D Gaussian theorem,
RN expected-count enclosure,24-jet closure or unrestricted prize closure is
claimed. ENG-03 requests an eligible independent3.11 replay of these exact bytes.

The existing Research Advancement Loop was enabled hourly, not duplicated. It
performs bounded work using live source/claim checks; it cannot wake outside
providers. The21 other campaign tasks are offers until actually accepted.
