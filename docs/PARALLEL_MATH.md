# Three scoped mathematical replays

These author-side candidates advance three separate obligations. They do not
change the governing registers or supply independent scientific acceptance.
All arithmetic uses exact rationals and certified intervals. Source programs
are read as pinned data, not executed.

Run `python tools/parallel_math_check.py`. CI and the closure runner require
this command. It runs the three portable copies concurrently, checks supplied
candidate data and rejects altered numerical results. Original authoring
evidence is retained in the immutable aggregate delivery.

| Target | Established by this candidate | Still required |
|---|---|---|
| [H3 fixed-r floor](../research/parallel/h3/H3_FIXED_R_RECONSTRUCTION.md) | The original safe box proves the imported exact floor `0.0077592917375327855` at `r=1/20`, with strictly positive rational slack. A separately labelled enlarged box proves `0.007759315`. | Historical upper endpoint, all-r extension, independent acceptance and canonical promotion. |
| [LPW covariance modulus](../research/parallel/lpw/ARGUMENT.md) | Full spectral tails and source profiles imply `||Gamma_r-Gamma_0|| <= 1.804*r^2` for `0 <= r <= 1/100000`. | The ten-profile model identification and endpoint `31/250` eigenfloor remain premises; no density, amplitude constant, topology or LPW headline closure. |
| [H5 normalized c2](../research/parallel/c2/DERIVATION.md) | For every `r` in `[7071/200000,1/20]`, `c2(r)=(1-k(r))/r^2` lies in the outward decimal enclosure `[0.499687630167653,0.499843785542625]`. | This is one of 24 jets, in a candidate band chosen from literal published rungs; the full band partition, remaining jets and modulus acceptance are unresolved. |

The RN point and local-square calculations retain the original H3 value. Their
own certificates import it explicitly; the companion H3 replay now supplies
an author-side proof of that premise at the same fixed rung. The stronger
successor floor is kept separate, so it cannot silently change the RN quotient.
The original printed lower endpoint was tested against the new exact enclosure:
the original safe-box contribution is strictly larger, so no historical
rounding defect is established by this reconstruction.

The H3 replay compares every mathematical field to its delivered candidate,
including source identities, box, polynomial, interval endpoints, controls,
scope and status. Three host-specific audit fields are excluded from that
comparison: repository path, executing checker hash and imported Python file
hashes. The actual portable checker independently snapshots its imports before
and after execution; the full runner also hashes the stable working tree.
The code differs from the delivered script only in its default repository path.

The LPW and c2 portable copies use repository-relative defaults. Their stored
reports are checked with strict JSON parsing and canonical equality, which
distinguishes booleans from integers and rejects duplicate keys and floats.
LPW removes only the original report's two machine-specific root-path metadata fields; every mathematical field is retained, and the c2 candidate is byte-exactly unchanged.
No report can promote itself by changing a status flag.

Cross-target review reconstructed the H3 quartic from the saved Hessian laws
and checked the two box integrals. Reviewers had source exposure and share the
OpenAI provider lineage; organizational independence credit is zero. Numerical
diagnostics support implementation review; the written analytic arguments and
outward arithmetic establish the scoped bounds.
