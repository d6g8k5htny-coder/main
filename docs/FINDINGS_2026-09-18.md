# Findings of 2026-09-18

What the active layer found on its first day, in the order the discipline ranks
them: defects in this repository's own work first, then decidable questions
settled, then what was recovered, then what remains open. Every number below is
reproducible from the repository by the command named beside it. Nothing here
promotes, closes or reclassifies any claim; every status stands as its source
records it.

---

## 1. Defects in this repository's own migration

Found by the review harness reviewing the migration that built it. These rank
first because a repository of research claims that quietly inflates one of its
own is worth less than no repository.

### 1.1 No mirrored artifact is byte-identical to its object

`registers/json/review_queue.json` row `RV-OPS-R17` declares its object as
14,073 bytes, SHA-256 `04987ba4…`. The repository's copy at
`governance/protocols/OP-PROT-019-v1.1_R17.md` is **14,073 bytes, SHA-256
`efcfdd5c…`**. The byte count matches and the digest does not, so a byte-count
check on that file confirms the wrong bytes.

Checked systematically that morning: **0 of 18** mirrored artifacts matched any
of the 325 full digests the registers declare. By afternoon the count was
**3 of 7 restored** (§1.1a below). Two causes:

| file | delta | cause |
|---|---|---|
| `OP-PROT-019-v1.1_R17.md` | +0 B | export normalised the content; byte count coincidentally unchanged |
| `FULL_DOCS_MATH_READ.md` | +239 B | the prepended provenance header, which asserted exactness, was itself the only thing making the file inexact |
| `R17_IMPLEMENTATION_REPORT.md` | +262 B | same; each delta is the header's own length |

(The header's original two-word wording is not reproduced here beside the
file's name, because `tools/provenance_check.py` is literal by design and has
no quotation escape — the same rule the reviews directory applies to promotion
language. This document is held to it like any other.)

The clause violated is in the protocol whose port violated it — `OP-PROT-019`
§2: *"The digest establishes identity, not truth or authorization."*

Fix, in two stages. First, `governance/PROVENANCE.json` recorded declared and
actual digests for every mirrored artifact — at that point none claiming
exactness — and `tools/provenance_check.py` began refusing to let any file
describe a non-exact copy as verbatim or byte-exact, naming the byte-count
trap instead of passing it quietly. Second, the repair in §1.1a restored three
objects from their declared digests, and the record now claims exactness for
exactly those three. Twelve controls, including a false-positive control so
the corpus's own discussion of byte-exact capsules does not trip the checker,
and one that constructs the byte-count trap and asserts it is still named.

Source: `reviews/records/REV-OPS-R17-001.json`, finding 1.

### 1.1a The repair: three restored, four unverifiable, two of those altered

`python3 tools/provenance_check.py` — `artifacts=7 byte_exact=3 problems=0`.
Each source Drive id was downloaded raw; a file was replaced **only** where the
download hashed to a full 64-hex digest the corpus declares, and an
independent verifier re-downloaded and re-hashed everything afterwards.

| artifact | outcome | who declares the digest |
|---|---|---|
| `OP-PROT-019-v1.1_R17.md` | restored, `04987ba4…` | a **register** row (`RV-OPS-R17`) |
| `FULL_DOCS_MATH_READ.md` | restored, `448adee5…` | source map + inventory only |
| `R17_IMPLEMENTATION_REPORT.md` | restored, `7b2f6cc1…` | source map + inventory only |
| `OP-PROT-012.md`, `OP-GDN-002.md`, `OP-CNS-001-R0.2.md`, register export | unverifiable — no digest anywhere in the corpus | nobody |

The sharp case's same-length difference is identified exactly: one blank line
inserted after the title (first differing offset 74) and the object's final
newline dropped — `copy == object[:74] + b'\n' + object[74:-1]`. The two docs
were the object plus a prepended header; the header is gone and its contents
live in `PROVENANCE.json`.

Unverifiable is not unaltered. Against the raw native-Doc exports,
`OP-CNS-001-R0.2.md` **omits the export's first line** — *"Live rule:
00_LIVE_GOVERNANCE - Independent-Eyes Rule (OP-PROT-011)."* — and
`OP-PROT-012.md` carries " at time of export" inserted into its Status line
with curly quotes flattened. Those are content divergences in reading copies
of governance protocols, recorded because no digest can settle them. Full
byte-level detail: `docs/PORT_FIDELITY_REPAIR.md`.

### 1.2 "RN3's far-region proof is outside the affected scope" was true of too little

The sentence is accurate about the far-region proof and about nothing else in
that object. RN3 §9's conditional arithmetic imports the near target
`17.6804 r³`, which the RN5 repair places inside or beside the affected scope
(corrected diagnostic ≈ `2.34195 r³` against wrong-power ≈ `17.67237 r³`, a
factor of about 7.5), so the displayed `I_near + I_far < 20.51352 r³` rests on
a number the erratum touches. Both places this repository stated the sentence
are now scoped. The same review observed that RN3's two named zones — far
`|y| ≥ 5`, near `0.1 ≤ |y| ≤ 5` — leave the open disc `|y| < 0.1` unaccounted
for in the §9 sum.

Source: `reviews/records/REV-RN3-FARZONE-20260918.json`.

### 1.3 The reading order presented a superseded protocol as current

The Drive object behind `governance/protocols/OP-PROT-012.md` is titled
*"HISTORICAL OP-PROT-012 — APPROVAL ROUTING SUPERSEDED BY R17"*. The reading
order listed it second without the qualifier. The supersession as titled is
scoped to approval routing and R17 retains parts of the historical protocols,
so this is flagged in `governance/README.md`, not resolved. Which clauses
survive R17 is an operator question.

### 1.4 The contribution plan guessed wrong about recoverability

`docs/CONTRIBUTION_PLAN.md` §7 asserted the TB-G2 capsules were recoverable
from Drive revision history. They are not: see §4.2 below. The section now
says so.

### 1.5 CI could not fail on six of its own checkers

The step pattern `test -f X && python X || echo "skipped"` — introduced by me
this morning for the review, recovery and collision checkers and copied by the
runner agent for the carrier, lane and receipt checkers — swallows a **real
failure**: if the tool exists and exits nonzero, the `||` branch runs and the
step passes. Confirmed by execution: a deliberately failing command under that
guard exits 0. For those six steps "what CI enforces" was, until fixed, only
"what CI runs". The fix is `if [ -f X ]; then python X; else echo skipped; fi`,
which skips only when the tool is absent. Sixth defect the harness has found in
this repository's own work today; applied to all six steps once the runner
agent released `.github/workflows/ci.yml`, and proved by execution — a failing
tool under the new guard exits 1.

---

### 1.6 The port graded a retracted theorem as live-root

`python3 tools/claims_check.py`; `python3 -m pytest -q tests/test_claims.py`.
From the first commit until 2026-09-18 the README table, `docs/RESEARCH_MAP.md`
§1 and §11, and `claims/graph.json` carried `Q0-C104-THEOREM-B` as
`LIVE_ROOT_THEOREM` — the historical `PROVEN-HERE` label of `Q0_MASTER` Part I —
while the registers this repository itself ships said, since 2026-07-24, that
the label was withdrawn: `automation_config` `THEOREM_B_CURRENT_STATUS` =
`CANDIDATE_UNCONDITIONAL_PROVEN_HERE_RETRACTED_EXACT_JACOBIAN_PROVED_CONDITIONAL_B0_PROVED`,
"Do not cite Q0_C104 or Q0_MASTER historical PROVEN-HERE labels as current
proof"; `activity_log` `GP-AUD-187-v1.0` "CRITICAL SCOPE NARROWING — THEOREM NOT
KILLED / NO PROMOTION". The lane-by-lane coverage audit of 2026-09-18 found the
contradiction by reading the Theorem B READ_FIRST at the source ("Canonical
Theorem B remains RETRACTED / NOT RESTORED / NOT PROMOTED"); the register rows
settle it offline. The port had read the spine and not the register that
governs the spine's routing.

Repair, by transcription: the node now carries the register row verbatim as
`register_status`, a `retraction` record, what survives (exact Jacobian;
conditional B0 under A1–A5), the seven open gates and the register's own
dispatch; `docs/OPEN_PROBLEMS.md` §G and `engine/lanes/G.json` carry the
program. `FW-RETRACTED-NOT-UNCONDITIONAL` refuses an unconditional grade on any
claim carrying a retraction, a control restores `LIVE_ROOT_THEOREM` on the node
and asserts refusal, and another binds the node's `register_status` to the
register row so the two cannot drift. Nothing about Theorem B's mathematics
changed; a label that was wrong in this repository now matches its source.

### 1.7 The lower campaign: an adjudicator named as the refuted object, a lower bound filed as upper

`python3 tools/claims_check.py`; `python3 tools/lanes_check.py`. The coverage
audit of the KIMI + AO48 LB-RATE lane (468 items) found three transcription
defects in this repository's own work. `docs/RESEARCH_MAP.md` §7 wrote
"`GP-LB-STAT-004` is refuted as theorem-grade": `GP-LB-STAT-004` is the hostile
adjudication that *issues* the refutation; the refuted object is the assembly
`K3-THM-001`. The same section's heading read the assembly's verdict onto the
whole lane, whose own words are "LOWER CAMPAIGN OPEN". `claims/graph.json` filed
`K3-THM-001` under `UPPER2D` with no source identity; it is a lower-bound
(`liminf`) assembly of the 2D lower campaign and consumes the `LOWER2D` premise
`H-B3`, so it is now `LOWER2D`, depends on `H-B3`, and names both carriers by
Drive id and digest. And `engine/carriers/MANIFEST.json` bound `jets.py`
(`CR-JETS`) to lane A1 as if its Taylor jets were the upper chart's 24-jet set;
the file sits in the lower campaign's DER-027b INCOMPLETE draft and the source
never links it to A1 — it is now bound to the new lane H. Under-coverage was the
fourth finding: the register carries five P0 open questions for the campaign
(`OQ-014`…`OQ-016-U2`) that no section of `docs/OPEN_PROBLEMS.md` transcribed;
§H now carries them verbatim. None of this moves a status: every label is the
register's.

### 1.8 The canon lane: a directory that was promised and empty, a bullet from July, a ledger never named

The coverage audit of `02_RESEARCH_CARRY_FORWARD_CANON` (766 items) found:
`governance/README.md` said the historical protocols are kept under
`protocols/history/` — the directory was empty (it now says so; three of them
exist as digest-bearing markdown in the Drive and are queued for mirroring).
`docs/RESEARCH_MAP.md` §6 described P0.2 with the July 21–22 gate layer while
the READ_FIRST's 2026-07-30 banner reads "five terminal / eight review-pending
interfaces / ten exact objects. P0.2 remains OPEN / NONTERMINAL / NOT PROMOTED"
and calls lower text "historical and nonoperative for current routing".
`Q0_LEDGER.md` — the promotion channel the Canon and the Formalization Board
name — occurred nowhere in the README, docs or governance. The live rate was
displayed without the domain the Canon writes into it (`0 < r ≤ 0.025`, birth
height 6/5). `docs/RESEARCH_MAP.md` §2 called `490ad6b2…` the frozen SHA-256 of
`D1_ASSEMBLY_v2_2.md` without the word *body*: it is the digest of the
18,311-byte marker-delimited body, and the 20,078-byte file hashes to
`7ca114f0…`. The formal/Lean state — original bundle missing from Drive, no
Lean build ever run, every item SORRY-PARTIAL / CI-PENDING / NONCANONICAL — was
not transcribed. And §A3 of `docs/OPEN_PROBLEMS.md` described the ZBAND hi side
without naming the object the proposed consumption actually consumes
(`H3_BAND_CEIL.md`, body `cfe8a3a4…`). All corrected by transcription; nothing
moves.

### 1.9 The register export was a prefix: seven tabs were incomplete from the first commit

The registers this repository ships were generated from the Drive connector's
markdown rendering of the workbook, exported 2026-09-17. Fetching the same
workbook as an xlsx export on 2026-09-18 and parsing it with the standard
library (44 worksheets, zip CRC clean) showed that the markdown rendering
returns only a *prefix* of each large sheet: `file_catalog` 310 of 2,952 rows,
`activity_log` 241 of 518, `artifact_index` 206 of 761, `transition_log` 67 of
82, `review_ledger` 101 of 138, `evidence_lineage` 137 of 485, `relations` 164
of 367 (zero key mismatches on the prefix; no hidden rows). Both markdown
renderings are also cut mid-cell at end of document (the committed one ends in
`…/vie`). `registers/README.md` presented the truncated counts as totals, and
`docs/RESEARCH_MAP.md` and the claim graph were built on the prefix. The two
tabs the workbook gained on 2026-09-18 (`Reusable Operations`, `Operation
Trials`) were the smaller half of the gap.

The repair is a new dated export, never an edit of the old one: the xlsx
becomes the source, the importer parses it with `zipfile` + `xml.etree`, all 44
tabs are regenerated with their real row counts, and every downstream
transcription the refresh moves is re-derived from the register — the review
queue (25 routes; `RV-LM004-MAIN` now `PASS_TECHNICAL` at zero independence
credit, `RV-RN-ALIGN` now `AMEND`, `RV-RN5-MOMENT-REPAIR` new at `READY`), the
quarantine index (22 exclusions; five new `DEFECTIVE_SCOPE` rows from the RN5
release), the frozen-object register (195 rows), the append-only work-events
log (35 rows; the committed five unchanged at their positions). An adversary
re-parses the xlsx independently and compares every cell before the refresh
lands. What the refresh establishes: the repository's registers are now the
workbook, not a prefix of it. What it does not: a register row is a
transcription, a `PASS_TECHNICAL` is a same-line technical pass at zero
organizational-independence credit, and nothing is promoted.

### 1.10 The prize lane: a member count doubled, two carriers summed, a registry over-read, a dependency dropped

The coverage audit of the prize reconnaissance lane (295 items) found four
statements in this repository that the lane's own status files contradict.
(1) `docs/RESEARCH_MAP.md` §8 gave the Phase05 Overlap carrier "244 members".
The archive has 122. The Drive holds two byte-identical copies of it (ids
`1XB6HnA5…` and `1Z4Rvtap…`, one SHA-256 `e976271b…`), the source map lists the
members of each copy, and 244 is the row count over both. (2) The same section
and the §10 table gave "Phase11 (620)". Two distinct carriers share the title
`Prize_Research_Phase11_2026-09-16.zip`: the stored snapshot `860411bc…`
(257,311 B, 436 members) and the `ATTACHED_ee4a16e1 — DISTINCT_AUTHOR_SNAPSHOT`
(130,779 B, 184 members). The source's continuation audit says "Neither snapshot
was overwritten or conflated"; the repository had conflated them into one row.
(3) §8 said `CLAIM_REGISTRY_VERIFIED_INTAKE.json` "grades every PR-TAL carrier"
at `AUTHOR_SIDE_PROOF_PRESENT`. It grades 28 claims, ten of them Talagrand
(PR-TAL-001…010); PR-TAL-009 and -010 carry `SOURCE_DECLARED_AUTHOR_ARGUMENT`
and `SOURCE_DECLARED_FINITE_CERTIFICATE_NOTE`; PR-TAL-011…019 are not in it at
all. (The audit itself counted 27 and the first version of this section and
of `docs/RESEARCH_MAP.md` §8 repeated it; the binding test
`tests/test_prize_lane.py` written against the mirrored bytes failed on 28 and
the count was corrected before it was pushed a second time — one more instance
of a control finding a defect in the work that introduced it.) The eighteen
non-Talagrand claims (PR-AP-001…015, PR-SID-001, PR-COL-001, PR-SP-001) and the sub-lanes' own "no unrestricted bound" statements were absent
from the map. (4) `claims/graph.json` recorded `P15-A..D` with `depends_on: []`
and had no P14 node, although `P15_CURRENT_STATE_AUTHOR_CANDIDATE.md` obtains
P15-A's palette bound "through the preceding P14 theorem" and the repository's
own review records `REV-P15-B` and `REV-P15-D` say P15 "does not establish
P14-A". A fifth, smaller: §10 and `drive/README.md` presented "77 carriers …
4,020 distinct payloads" as archive statistics; 77 is the completion report's
count (the inventory marks 76 items `ARCHIVE_INDEXED`, 73 have members listed)
and 4,020 is `Payloads.csv`, the whole source map (2,975 among members).

The repair: the lane's status layer — the root READ_FIRST,
`CLAIM_REGISTRY_VERIFIED_INTAKE.json`, `CURRENT_STATE_VERIFIED_INTAKE.json`,
the P14 and P15 claim registries and current-state files, and the Erdős 142
scope pointer — was fetched raw, hashed, found equal to the eight inventory
digests, checked against the 2026-09-18 delta (none moved or modified), and
mirrored under `drive/mirrors/2026-09-16 — PRIZE PROBLEM RECONNAISSANCE —
INDEPENDENT TRACK/`. §8 and §10 were rewritten from those bytes with every
grade quoted verbatim, the four sub-lanes added, and the two Phase11 carriers
listed separately. The graph gained a `P14-A..E` node transcribed from
`P14_CLAIM_REGISTRY.json` (`AUTHOR_SIDE_COMPLETE`, `external_independent_reviews:
0`, `prizes_solved: 0`, `original_prize_closed: false`), `P15-A..D` now depends
on it, and the three prize nodes' `source` fields name the mirrored files by
Drive id and digest; the `NUMBER_THEORY` track description now says the label is
the firewall key, not a description of the (mostly combinatorial) work. What
none of this establishes: no grade moved; `AUTHOR_SIDE_COMPLETE` is the author's
own label; every prize node still carries `original_prize_closed: false`; the
53 Talagrand proof notes, the other 19 notes, the review-queue files and the
zip carriers are still indexed only; nothing from this lane enters the q0 graph.

### 1.11 The platform lane: "what this repository makes reusable" was nothing it carries

The audit of `01_RESEARCH_PLATFORM_AND_VERIFICATION_ARCHITECTURE` (188 items,
130 of them native Docs with no digest) found the repository's descriptions of
it inflated in four places. `README.md` said the platform — "verifiers,
regression corpora, a Lean/formal system lane" — "is what this repository makes
reusable": the repository carries none of the lane's verifiers
(`verify_obligations.py` and its twelve-mutation regression runner,
`math_integrity_gate.py`, the eight `hostile_tests.py`, `extract_claims.py`,
`prep_agent_v2.py`, `AUTO_SORTER_LOGIC_v2.py`, the `FS2_PCT003` Apps Script
executor, the `FS2_CONTROL_REGISTRY` sheet); it indexes them. "Lean/formal" read
as Lean-prover formalization, and the lane holds zero `.lean` sources and zero
prover receipts — its formal content is Markdown statements, sympy tests and
PDFs; the program's Lean material is `GP-FOR-001`, `GP-FOR-189`, `GP-REC-220`
elsewhere. `docs/RESEARCH_MAP.md` §9 described "regression corpora and negative
controls" and "a replication tree": in the snapshot these are one two-item
folder about a single blank-line conversion control ("Scientific effect: none")
and an evidence tree whose five output folders are empty. `governance/GIT_ADAPTATION.md`
mapped the Drive's protocols construct by construct and omitted the Fresh Start
2.0 control plane — the state machine `INGESTED → PREPPING → CANDIDATE →
REVIEWED → CORE`, gates PG-01…PG-10, DRAFT_ID transactions — which the lane's
`00_READ_FIRST` and `FS2-SETUP-001` call an "INSTALLED / ACTIVE CONTROL PLANE".
Two attributions were loose: `legacy/README.md` credited "legacy may ask a
question; legacy may not supply the answer" to "the Fresh Start 2.0 charter"
without a source. (Correction of 2026-09-19: that attribution was right in
substance — the charter exists, `FS2_ACTIVE_RESEARCH_AND_LEGACY_QUARANTINE_CHARTER_v2.0.md`,
Drive `1KdOEc5L3vkwmvG5swNqnsYvigStc5qB9`, 6,880 B, `c5ab7980…`, now mirrored
byte-exact, and its §1 states the rule as its headline; the 03_PERSONAL zone
notice and the vault manifest's "Companion rule (Fresh Start 2.0)" repeat it.
The 2026-09-18 repair had looked at the platform lane's `ARCHITECTURE_RULES.md`
instead and replaced a correct attribution with the claim that the charter
phrased the rule differently.) The other loose attribution: the graph's `PLATFORM` track was declared and empty without saying
whether the program has no verifier objects or the repository never transcribed
them (it has two with register rows: GP-VO-001 / DQ-060 at "LIVE MIGRATION HOLD"
/ "HOLD — DO NOT CLAIM", and DQ-059 at "PASS / COMPLETE / SANDBOX RELEASED").

The repair is wording, done against the registers this repository already holds
and the audit's verbatim quotations: README §3 now says what is carried and what
is only indexed; §9 names the control plane, the eight capsules' file set and
reading rule ("CURRENT_STATUS.json before historical proof banners",
`parent_theorem_effect: NONE AUTOMATIC`), the empty output tree and the two
prototypes at their register statuses, and states the absence of Lean sources;
`governance/GIT_ADAPTATION.md` gained a row saying the control plane has no
repository equivalent; the legacy rule is attributed by Drive id; the `PLATFORM`
track description says it is intentionally empty and why. Not done, and
recorded as the lane's ranked gaps: transcribing the eight capsules'
`CURRENT_STATUS.json` labels (24 native Docs), the Fresh Start 2.0 charter
documents as reading copies, and the two digested carriers
(`DQ060_Verification_Obligation_Prototype_v1.0.zip` 2bab4e67…,
`math_integrity_gate.py` 69626116…) byte-exact — the latter never to be wired
into CI, as the source's own "LIVE MIGRATION: HOLD" and R17 require. What this
establishes: the repository's account of the lane now matches what it holds.
What it does not: any status of any capsule, prototype or gate, all of which
remain exactly what their sources say, and none of which bears on the five
premises, D3-LEMMA-RN-UNIF, P0.1, P0.2 or Theorem B.

### 1.12 The audit's second pass: a spine held nowhere, a successor's labels on the active root, a carrier counted twice, an index searched by the wrong name

The lane-by-lane fidelity audit of 2026-09-18/19 (§6) re-read this repository's
own account of the Drive against the sources, offline, and found the following
in this repository's work. Each is corrected in place with a dated "until …"
sentence at the corrected spot.

- **The canon's promotion channel is cited and not held.** `docs/RESEARCH_MAP.md`
  §1 cites `Q0_LEDGER.md` by digest (`d1fea170…`, 253,067 B); §9 said the
  cross-model ledgers lane was "digest-bearing" and stopped. It now says that the
  spine, 33 C091–C108 cycle ledgers and the COMMAND CENTER — 44 digest-bearing
  objects, 494,416 B — have no byte-exact port here. A digest is a name, not a
  copy.
- **P1.1's labels were the wrong object's.** §6 attached "NONCANONICAL
  q0-law-specific/1.2 FAIL-CLOSED PASS / 12-OF-12 NEGATIVE TESTS / INDEPENDENT
  REVIEW OPEN" to the active root, whose own row says "authoritative and
  untouched". Those labels belong to the additive 1.2 successor (`GP-REG-188` /
  `GP-DATA-188`); the 1.2.1 hardening package is `RV-DQ-017`'s exact object and
  the dashboard's current word is "1.2.2 CANDIDATE; ACCEPTANCE OPEN". Corrected.
  `engine/carriers/MANIFEST.json` (`CR-Q0-VERIFY`) had the same confusion in its
  `lane_basis` — it described the route's object as the 957 KB active verifier
  it stores; the route's object is the 25,843-byte `5202c5fa…` member of two
  2026-09-17 carriers, "NOT this file". Corrected; `carriers_verify` green.
- **One carrier, two copies, summed.** §10 gave
  `SIDE24_G9_R2_RECOVERY_AND_RECONSTRUCTION_2026-08-01.zip` 1,146 members. The
  Drive holds two byte-identical copies (`1Uz1xeyn…`, `18PdDtAi…`) of one
  573-member carrier. Corrected.
- **The SIDE24 3D row said less than the register.** `README.md` graded the 3D
  result in a phrase; it now carries the register's own word,
  **RATIFIED-AT-STATED-SCOPE** (`operator_decisions` row `AO48-OPR-045`,
  2026-08-02), the stated scope, and that the record was authored by the AO48
  line relaying the operator's one-line sign-off, with carried dependencies and
  reopening conditions. The graph node `SIDE24-3D-AO48-OPR-045` is enriched
  field-for-field from the byte-exact mirror of the record (`e48d7c27…`) and
  bound to the register row and the inventory by `tests/test_claims.py`; the
  ratification chain (`AO48-OPR-045`, `AUD-044/043/036`, `REC-034/035`, folder
  README) is mirrored byte-exact and `AUD-033` is a tree-only row after two
  downloads disagreed with the inventory by one byte. `docs/RESEARCH_MAP.md` gains
  §7a for the track. What this establishes: the transcription. What it does not:
  anything on the 2D tracks — the firewall the record itself states.
- **Two register layers, one bullet.** §6's P0.1 bullet now separates the Easy
  Closure Queue v1.9 row from the Autonomy Control v1.10 / `LS-CTL-003-v1.3`
  row; they say different things about the same item and both are the
  register's.
- **"The only operator sentence" was not.** §4.10 is revised; the COMMAND
  CENTER and CL-REQ-225 restate the private-repository decision. The "three
  writable register copies" bullet of `governance/GIT_ADAPTATION.md` is
  rewritten from the R17 §1 rule, the DQ-018 sandbox's "DO NOT USE LIVE" and the
  two GP-REC-EC020 snapshots: the duplicates are in the live workbook's own tabs.
- **The recovery ledger's route (a) searched by the wrong name**, and the reading
  volumes' display rule was undeclared — §4.2 addendum. Two candidates are now
  recoveries; one unrecoverable capsule's packaged verifier is held; three
  successor records pin their predecessors.
- **The registers' `run_log` was misdescribed** as automation runs; the 32 rows
  are receipts of manual connector sessions, closure watches, a self-healing
  audit and a custody reconciliation in the sheet reserved for automation, and
  `GP-AUTO-034` was never installed or run. `registers/README.md` corrected.

What this section establishes: that the repository's description of the Drive
now matches the Drive on these points. What it does not: any status. The five
premises are OPEN, `D3-LEMMA-RN-UNIF` is not closed, Theorem B is retracted, the
lower campaign is open, no prize problem is solved, and nothing on the 3D track
composes with anything on the 2D tracks.

### 1.13 The audit's third pass (lanes L10–L15): a citation under the wrong id, three note-layer words under-transcribed, an invented label, a stale rung line, and eleven prose defects

The lane-by-lane audit's last six lanes (the UPPER2D intake, the thematic
tracks, governance, foundations, the non-authority lanes and the registers /
source map) were read against the sources on 2026-09-19; every finding below
was re-verified here against a byte-exact mirror or a register row before the
correction, and each corrected spot carries a dated "until …" sentence.

- **A3 cited the v2.3 DRAFT's sentences under the consumption note's name.**
  "On the band: the two-sided normalizer is now bracketed uniformly" and the
  `H3_BAND_CEIL` citation are the DRAFT's (`1RQE2B3…`); the consumption note is
  `18R0wwSF…`. The status conclusion (PROPOSED, authority none, hi side OPEN)
  was unaffected; the citation was wrong about which object says what.
- **Three premise rows said less than the register note.** B4.rem is
  "ADJUDICATED YES … CLOSED by B4LOC-R1 for the O(r³) validity grade" (note §2,
  §5), not "still open"; PERC-DECAY is listed "CLOSED … in its RESTATED form"
  with PD-CONN moved to the REFINEMENT register, not merely "RESTATED";
  OBL-D1-PROMOTE is reduced *and enlarged* ("and now also the uniform-band
  extension of the B4LOC/B2-far/far-route certificates"). All three are note
  layer, effective at the next issuance; the frozen layer stays OPEN and the
  README's two-column table keeps both.
- **An invented label.** `OBL-H5-ZBAND` carried `status_register_note:
  "PARTIAL"`, a word no source uses. It now carries the frozen word OPEN, and
  the consumption note's "OPEN → DISCHARGED (consumption grade)" is recorded as
  a separate PROPOSED layer with its authority-none qualifier.
- **The rung theorem's statement was its input.** `D1-v2.2(1)` stated the H5
  chart total `647.8048·r³`; the theorem's own display is `Ĩ_hi = 650.1827·(0.05)³`
  plus three further terms with `C_RN(0.05) ≤ 3.46` (v2.2 §1, mirrored).
- **A stale rung line.** "two shards resuming" described RUNG2's 2026-09-15
  snapshot; the v2.3 DRAFT records the rungs "FROZEN mid-flight (125 + 6 / 42 +
  2 banked)" with "Kimi dark until 2026-09-30". Engineering, not status.
- **A "verbatim" quotation that was a summary.** The `chi2_grad_bound` slack
  record said CL-RNU-001 was "quoted verbatim in LANE_RN_UNIF.md §2"; that
  memo's §2 summarises CL-RNU-001 in its own words. The numbers agreed; the
  record now quotes CL-RNU-001's own sentences, and both documents are mirrored
  byte-exact.
- **Governance.** OP-PROT-013..018 were called historical; the register labels
  them ACTIVE / CURRENT / PUBLISHED and only OP-PROT-011 and -012 carry a
  HISTORICAL retitle. The OP-PROT-012 reading copy's header asserted its
  autonomy classes "remain the governing description", which no source says;
  the register retires its decision budget under R17. §4.5 of this document and
  `governance/README.md` presented the OP-PROT-011-versus-012 question as open;
  `operator_decisions` records the operator's 2026-07-25 answer. The
  autonomy-classes, frozen-objects (188/55/133 → 195/62/116), handoff-envelope
  (`ACCESSIBILITY_HANDOFF.json` embeds no digest) and vault-tree (6 items plus
  the external manifest) rows of `governance/GIT_ADAPTATION.md` are corrected.
  "OP-PROT-003" denotes two Drive objects; the README says which one is held.
- **Foundations and the non-authority lanes.** The 2026-09-18 "correction" of
  the legacy rule's attribution was itself wrong: the Fresh Start 2.0 charter
  exists, states the rule as its §1 headline, and is now mirrored byte-exact.
  `legacy/README.md` said it "mirrors" two Drive lanes it only indexes, and
  folded two strata the sources keep apart; `sandbox/README.md` lacked the
  never-link rule. Two `docs/RESEARCH_MAP.md` root rows described lanes by the
  wrong contents ("registers" in a lane that holds none; "history and archive"
  for the lane holding the program's only positive-admission allowlist).
- **Counts.** 77 carriers is recomputable (76 `ARCHIVE_INDEXED` + the
  `BINARY_UNRENDERED` S2-DATA-002 zip), "73 with members listed" was 72 + that
  zip, and "2,975 distinct member payload digests" counted an empty cell;
  `docs/CONTRIBUTION_PLAN.md` still said 42 tabs; `07_MODEL_ACCESSIBILITY` was
  "mirrored here" when four of its ten audit files are; `inventory.jsonl` is a
  projection of `Files.csv`, now said, with the five files' digests recorded.
- **Lanes.** The Theorem B carrier record still said no open-problems section
  covered it after §G existed; lane G said no carrier was bound and the
  READ_FIRST was not mirrored; the C091 gate-reduction script sits in lane B by
  this repository's classification while the Drive files it under T1 and its
  charter calls the gate the lower side's — all now stated. "T4" names two
  unrelated things (the RN-UNIF push region and the thematic track); the
  collision is now flagged at every T4 mention.

Not done in this pass and recorded as gaps for the next port: the five
workspace charters and leaf cards of the thematic tracks; the OP-RECON set, the
R17 bundle members and OP-PROT-013..018 as reading copies; the remaining Fresh
Start 2.0 v2.0 artifacts; the accessibility publication's other six audit
files and its verification receipts; the quarantine-lane notices; and index
manifests for the legacy and quarantine lanes. Three cross-register
observations that no checker can see are listed in `registers/README.md`.

What this pass establishes: that the repository's description of these six
lanes now matches the sources on the points named. What it does not: any
status. The five premises are OPEN at the frozen layer, `D3-LEMMA-RN-UNIF` is
not closed, `OBL-H5-ZBAND` is not discharged, Theorem B is retracted, and
nothing on the 3D track composes with the 2D tracks.

## 2. Decidable questions, settled by exact arithmetic

Reproduce with `python3 -m research.lpw.headline` and
`python3 -m pytest -q tests/test_lpw_headline.py tests/test_identities.py`.

### 2.1 LPW headline: direction decides admissibility

Exact constant `260 / (3790446482793 · 2⁴⁰ · 10²¹)` =
`6.2385427029355877929430410845…e−44`.

| decimal | position | as upper bound | as lower bound | as equality |
|---|---|---|---|---|
| delivered `6.239e−44` | above exact by `+7.33e−05` rel; correctly rounded to 4 s.f. | **admissible** | not admissible | not admissible |
| proposed `6.238e−44` | below exact by `−8.70e−05` rel | **not admissible** | admissible | not admissible |

The §C repair direction is wrong for an upper bound: a decimal below the exact
value asserts `quantity ≤ 6.238e−44`, which is stronger than the chain
supports. And under an equality headline **nothing rounded is admissible**,
because the reduced denominator carries a prime other than 2 and 5, so the
expansion never terminates. This settles direction-soundness only; it does not
verify the derivation behind the fraction, and qualitative LPW is unchanged.

### 2.2 `E(|ξ|+|η|)⁴ = 12 + 16/π` is false

True value `12 + 32/π = 22.18591635788…`. The claim dropped one of the two
equal cross terms `4E|ξ|³E|η|` and `4E|ξ|E|η|³`, each worth exactly `16/π`.
Exact in the basis `{1, 1/π}`. The sibling audit found one further identity of
that shape in the repository — the Rayleigh fourth moment `Eρ⁴ = 8` — and
confirmed it exact; six items of roughly the right shape could not be settled
and are recorded with reasons. Complete over the repository, incomplete over
the Drive. Refuting the identity flags its consumers; it does not by itself
invalidate any derivation that cited it.

### 2.3 The A1 display-vs-enclosure gap, made numerical

From the three published `I_hi/r³` rung certifications quoted in
`CHART_SIDE_JETMOD_PLAN.md`:

- At the **same** `r = 0.05`, frozen v1 `731.4311` and live v3 `647.8048`
  differ by **12.91%**.
- Under the natural reading of the displayed κ = 1/8 modulus,
  `|Δ(I_hi/r³)| ≤ C·δ^κ`, the two adjacent bands force `C ≥ 23.17` and
  `C ≥ 5.18` — a **4.47×** discrepancy.

Stated with its assumption: this is the natural reading of the displayed fit
applied to `I_hi/r³`, not a quotation of a source's own modulus statement. It
refutes nothing and discharges nothing. It shows, in numbers, why three point
certifications cannot substitute for a band enclosure — which is exactly the
distinction the sources draw and `OBL-H5-JETMOD` names.

---

### 2.4 The A1 machinery, and what building it showed

`python3 -m research.bands.ladder`; `python3 -m pytest -q tests/test_bands.py`
(72 tests, 20 negative controls). `research/bands/` reproduces §2.3's numbers
from its own Fractions — spread `836263/10000`, `12.9092%`; `C ≥ 5.1818` and
`C ≥ 23.1709`; ratio `4.4716` — with the assumption attached to every returned
object and printed by every formatter.

Three things the build established beyond §2.3:

- **`ratio(κ)` has a closed form and is strictly decreasing** —
  `(|Δ_wide|/|Δ_narrow|)·(δ_narrow/δ_wide)^κ` — with certified values 4.6696 at
  κ = 0, 4.4716 at 1/8, 3.3017 at 1, 1.1672 at 4, 0.8253 at 5. It crosses 1
  only between κ = 4 and κ = 5, which is where the two forced *minima*
  coincide — not where reconciliation becomes possible. **The first draft of
  this bullet said "no exponent below 4 reconciles the two adjacent bands under
  one constant". That was false**, and the adversarial pass caught it: a
  modulus is an inequality, so two bands that force different minimal
  constants are satisfied together by the larger one.
  `ladder.common_admissible_constant(κ)` now exhibits that constant — max over
  bands of `constant.hi`, re-checked against each band with both roundings
  chosen against the conclusion — and returns `all_satisfied` at every κ in the
  sweep (`C = 23.1709` at κ = 1/8), with a control that fails if the maximum is
  replaced by the minimum. The 4.47× ratio is a statement about *tightness*:
  the two bands differ in the minimum each forces, not in whether one constant
  serves both. (Another defect the harness found in its own work today — and
  the first in a mathematical sentence rather than in a checker or a port.)
- **A decay envelope is a premise no finite computation here can check, and
  the first draft let it default to "certified".** The attack pass fed
  `band_enclosure` an envelope with `B = 7` against a kernel that decays at
  `B = 1/2` and received `certified = True` with a tail of 8.69e−26 against an
  honest 2.13e−1 — wrong by 2.45e24, exactly computed. `DecayEnvelope` now
  carries its own `certified` flag defaulting to `False`, `PlaneKernel.certified`
  defaults to `False`, and a `BandEnclosure` is certified only as the
  conjunction of the two, with the missing flag named in a NON-CERTIFYING
  caveat. The number is unchanged and still wrong by 2.45e24 — the control
  asserts that explicitly (`tail < honest / 10²⁰`) so the relabelling cannot be
  read as a repair of the bound. The falsifier's printed report had the same
  shape of hazard: two rows rendering identical decimals (`width=0.001
  modulus=0.001`) with verdicts PASS and FALSIFIED and no marker; every row now
  carries an `exact:` line with the Fractions, and the decimal columns are
  labelled as renderings.
- **The tail bound is uniform in the displacement, and that is the whole
  point.** For a kernel with a certified decay envelope, the omitted-lattice
  tail over an interval box depends on the displacement only through the box
  radius — proved by the reverse triangle inequality on shells, with no second
  truncation on the Gaussian branch and an integral comparison on the power
  branch. The frozen engine's `tail_bound` hard-codes a point separation
  (`rho = m*_LT − 17`). At the program's own period the proved Gaussian tail is
  ~1e−498 against a band width of 1.6e−3, which is exactly why the engine's
  "< 1e−60" side-assertion reads as sufficient and why the defect is easy to
  miss: the missing thing is not the tail's size but that it is never carried,
  and never uniform.
- **Two testing defects in its own first draft, found by running the controls
  against broken copies and fixed before shipping.** Wide-box containment tests
  passed even with the tail bound deleted, because band variation swamps a
  5.6e−3 tail — a point-band test was added. And a 4× shell undercount was
  uncatchable by domination on the Gaussian, because the honest bound is already
  ~13× the true omitted mass — two independent closed-form reimplementations
  were added and asserted faithful. The measured looseness (12.7× point, 13.5×
  box on the Gaussian; ~2× on the inverse-power kernel) is reported, not hidden.
  The README's mutation table is dated: the `a = L·M + R` row read 8 in the
  first draft, 9 on the auditor's recount against that file, and 14 against the
  current file, because the whole-box sweep and three near-edge parametrisations
  added since also catch it. Measured mutation counts move when tests are added;
  anyone adding tests must re-measure rather than trust the printed numbers.

What it does not do: compute a band bound for any jet of this program. The
kernels exercised are reference kernels. The central missing piece is a
certified decay envelope for the program's `kplane` at every order the 24-jet
set reaches — the engine's own `_he_abs_poly` is a one-axis majorant where the
true term is two-axis — plus the jet definitions, the band endpoints and the
six-pin displacement geometry. `OBL-H5-JETMOD` is OPEN (display only).

### 2.5 The A5 cover driver — the one the lane records as unwritten

`python3 -m pytest -q tests/test_cover.py` (37 tests, 20 negative controls).
`research/cover/` is the annulus Riemann-sum driver RN5's next exact action
describes and `LANE_RN_UNIF.md` records as unwritten: a region-generic adaptive
cover whose accept / refine / reject / pending ledger is the first-class
output. Partition exactness is decided structurally over `Fraction` by
coordinate compression, never by area comparison — the sharpest control in the
package is a cover whose cell areas sum exactly to the domain's while carrying
both a gap and an overlap, which any area-based validator accepts.
`total()` raises `PendingCellsError` while any cell is pending and there is no
bypass flag; at max depth cells are left pending, not accepted. The RN5
annulus `0.1 ≤ |y| ≤ 5` and the T4 polar region `d ∈ [5, 17]` are instantiated
separately and never summed.

What building it established, as exact arithmetic rather than concern:

- **The acceptance rule's area factor cancels.** `width(|C|·rng) ≤ tol·|C|/|D|`
  reduces to `width(rng) ≤ tol/|D|`: a zeroth-order sup cover imposes a
  *uniform* bound on the integrand's oscillation per cell regardless of where
  the cell sits. That is the near-axis refinement cost RN5 says must be faced,
  in one line.
- **The polar anisotropy is 50×, exactly.** At a target Cartesian cell
  diameter of 1/10 the annulus needs 98 radial × 629 angular = **61,642**
  uniform cells; inner arc extent ≈ 9.99e−4 against outer ≈ 4.99e−2, the ratio
  being `r_hi/r_lo = 50` exactly. No fit, no sampling.
- **θ-halving alone cannot tighten the T4 cover past its shell resolution.**
  For a purely radial integrand, halving θ leaves a cell's range enclosure
  unchanged, so any tolerance below the shell's radial floor is unreachable at
  any depth. Demonstrated by running it, not asserted — and it bears directly
  on the freeze step `LANE_RN_UNIF.md` names as "polar cover d ∈ [5, 17] with
  theta-halving".
- **Choosing polar cells moves the boundary difficulty rather than removing
  it, and the module says where to.** Radii 1/10 and 5 are rational, so the
  annulus boundary is exact and the rejected boundary area is exactly 0; in
  exchange every cell area carries π as a certified enclosure rather than an
  exact rational. The Cartesian bracket of the same region rejects straddling
  cells with retained boundary-area bounds and residual enclosures instead.
- **Its reject path had no containment control, and two mutations proved
  it.** The first draft's controls caught a dropped interior-gap branch and a
  dropped boundary residual, and said so. The adversarial pass then found two
  mutations — `MX3`, quartering the rational upper bound on a rejected cell's
  area, and `MX4`, which leaves the bookkeeping untouched — that each left all
  29 tests green while returning `certified = True`, `covers_region = True`,
  no caveats, and an enclosure of `[71.09, 74.22]` (respectively
  `[71.09, 71.11]`) for an integral whose true value is 78.508. Seventeen of
  the 29 test docstrings named no mutation at all. The repair is a *reference
  integrand whose integral is known from geometry alone*: `f ≡ 1` on the
  annulus integrates to `π·(25 − 1/100) = 78.5084…`, computed from the region's
  exact radii and the certified `π` independently of any cover run. Control 22
  asserts the bracket's accounted and rejected areas sum to exactly 100 (catches
  `MX3`); control 23 asserts the certified total contains the geometric value
  (the only control that catches `MX4`); control 24 builds an all-OUTSIDE
  classification fault by hand — every flag reads clean and the bookkeeping
  still balances, so containment is the only thing that refuses it. A control
  that could only catch what its author imagined was the hole; a value known
  independently of the code is the patch. The test docstrings and README now say
  that completeness is *not* established: nothing shows a third such mutation
  does not exist.
- **`Total.certified_enclosure()` is the accessor a publisher must use.** The
  ledger's `enclosure` field is an enclosure of the *accounted* part; whether it
  is an enclosure of the region's integral is what `certified` and
  `covers_region` say. `engine/run.py`'s A5 task read the raw field and stamped
  it `certified_interval` regardless — latent on the polar region, which rejects
  no cells, and live the moment the lane is pointed at the Cartesian bracket.
  The runner now calls the accessor and records the refusal as a result when
  it fires; a control drives the task on a hand-built ledger over the bracket
  domain whose `Total` does not cover the region and asserts that no certified
  enclosure is published, and it fails when the raw-field read is restored.

What it does not do: certify any cell of the program's actual cover. Every
integrand exercised is a labelled reference; a float probe is included only to
prove the NON-CERTIFYING label propagates to the total and the receipt. The
constant-per-cell bound costs O(tol⁻²) cells, so a tight cover of the 78.5-area
annulus with a real integrand needs a higher-order cell bound behind the same
protocol. Both Pieces of `D3-LEMMA-RN-UNIF` remain OPEN; the lane receipts still
carry `lemma_closed: false`.

## 3. What the certified interval library's audit found

`research/interval/` is the foundation the program lacks: every blocking
obligation demands certified interval bounds, and every computational carrier
in the corpus runs on `mpmath` floats. Four adversarial lenses attacked the
library after it was built. Reproduce with
`python3 -m pytest -q tests/test_interval.py`.

| defect | consequence | fix |
|---|---|---|
| exact-rational `exp` endpoints grow as `1.4427·\|t\|` bits; quadratic through `erf`/`Phi` | `Phi(Interval(-10**5), 1)` was OOM-killed — an unconditional failure on the Gaussian tail, which is what tail bounds need | cap the exponent at `2²¹` bits; past it, underflow degrades to the honest `[0, 2^-limit]` and overflow raises |
| argument sizing used `len(str(n))` | CPython 3.11 refuses `int→str` above 4,300 digits, so `sin`/`cos` of a large argument raised `ValueError` before any arithmetic | bound the digit count from the bit length; an upper bound is the safe direction |
| three containment-breaking mutants survived the whole suite | in `__abs__`, the sign of the `sin` extremum at negative odd multiples of `π/2`, and monotone endpoint selection on wide inputs | one test per mutant, each confirmed to fail against a deliberately broken copy |
| `erf` tail branch clamped `hi` to exactly 1 | valid but crude; a test then pinned the clamp rather than the property | tightened to a real bracket (`1 − erf(8)` in `[1.12230e−29, 1.12250e−29]`, true `1.12243e−29` inside); the test now asserts containment against an independent literal |

The last row is a lesson about tests, not code: a test that records an
implementation fails when the implementation improves.

---

## 4. Recovered, reviewed, proposed

### 4.1 Six nonauthor technical reviews, all at zero independence credit

`python3 tools/reviews_check.py`. R17 §4 permits a fresh nonauthor session of
any provider to perform technical review; independence is recorded separately.

| route | object author | verdict | why credit is zero |
|---|---|---|---|
| `RV-OPS-R17` | openai | AMEND | not the same-provider rule — the author family differs. `OP-PROT-012` §5's predicate fails on (b), (c), (e), (i): no frozen task spec, the reviewer had read this repository's derived port first, no frozen comparison hash |
| `RV-RN3` | openai | AMEND | same predicate failures, plus the queue's own note that Anthropic far-zone work is not a review |
| `RV-P15-A`, `-B` | — | PASS_TECHNICAL | prize track; HOLD, not for submission; `original_prize_closed` stays false |
| `RV-P15-C`, `-D` | — | AMEND | as above |

Every record carries `gate_status_after: UNCHANGED`, which the checker refuses
to accept any other value for. The adversarial challenge re-downloaded objects
and hashed them itself, and independently reproduced the reviews' negative
controls (RN3's cover recount 13,612 / 13,604 / 13,596 with exactly 8 tangency
boxes; `η = 0.116820011`; P15-C's `M = m(3s−2)` by enumeration). Verdict: no
fabrication, no overclaim.

### 4.2 Thirteen exceptions recovered; the named priority is not recoverable

`python3 tools/recovery_check.py` — 31 records: **13 RECOVERED**, every one
digest-corroborated; 3 CANDIDATE; 15 UNRECOVERABLE.

Recovered: three archive members via the member index, eight
`ENCODED_BLOCK_FAILURE` chunks re-extracted from `LS-DATA-009-v1.1`, two gzip
payloads from `CL-AUD-084`. Several came back only because the Drive has moved
since the 2026-09-17 snapshot.

The TB-G2 capsules (`LS-DATA-015-v1.0-R1/R2`) are **unrecoverable**, with the
reasons established on fresh bytes: all three shells report
`createdTime == modifiedTime`, so no post-creation revision exists; their bodies
are still a bare UTF-8 BOM; the corpus states no digest for any of the eight,
so even a recovered body could not be corroborated; and the "Corrected" shells
were created minutes after an original whose own capsule is titled *"MALFORMED
PAYLOAD … DO NOT USE"*. Missing content is not invented.

One recovery surfaced an identity conflict: `LS-DATA-009-v1.1`'s result exists
under two digests — the carrier-declared `07e02214…` and the accessibility
publication's decoded-capsule `39e3defd…`. Both are retained; neither is
silently preferred.

**Addendum, 2026-09-19 — 15 RECOVERED, 1 CANDIDATE, 15 UNRECOVERABLE.** Two of
the three candidates were recoveries waiting for one display rule. Comparing a
payload whose bytes were independently in hand (the `LS-DATA-013` verifier,
extracted from its carrier zip) with its published reading copy showed that the
reading volumes render every blank line twice — a run of *k* newlines as
2*k*−1 — which the volume banner ("Display line endings normalized") does not
say. Halving the blank-line runs in the 2026-09-18 candidate bytes of the two
Markdown members reproduces their declared digests and byte counts exactly
(8,176 B `e9df4b6f…`, 4,705 B `15714b97…`); the three plain-text members have no
blank line, which is why they were already exact. The ledger records this as
successor records `RDF-01-S1` and `RDF-02-S1`, leaving the 2026-09-18 records and
their candidate blobs untouched, and `tools/recovery_check.py` now pins every
superseded record's canonical digest and re-tallies the counts. The same pass
found that `ENB-04`'s route (a) had searched the member index by the capsule's
title and by other capsules' payload names and reported "no member occurrence";
searched by `LS-DATA-013` the index holds the verifier 1.2.1 and its report in
two carriers. Both were extracted from a fresh, digest-verified download of
`CLOSE-20260917-b9c2_PROOFS_CODE_AND_VERIFICATION.zip` (now mirrored byte-exact),
corroborated against the source map, and stored as related artifacts of
`ENB-04-S1`; the capsule body itself is still unrecoverable, and the record says
so. The lane README of the SIDE24 ratification folder, mirrored byte-exact,
states the cause of the five `READ_FAILED` members' carrier ("TRUNCATED /
CORRUPT. A base64 transfer was clipped in transit during upload … DO NOT USE
IT") and names intact replacements whose digests appear nowhere in the
inventory; recorded as a cross-source observation, not re-fetched. What this
establishes: bytes. What it does not: any status of `LS-DATA-013`, `RV-DQ-017`,
the P1.1 acceptance or the SIDE24 audit findings, all of which stay as their
registers say; independence credit remains 0.

### 4.3 Sixteen identifier collisions, proposed not repaired

`python3 tools/collision_proposal_check.py` — all 16 findings in
`registers/KNOWN_FINDINGS.json` covered, 13 successor identifiers proposed under
`OP-CNS-001` §2's preserve-and-disambiguate remedy, exported registers
byte-unchanged. Needs an operator.

**2026-09-19.** The seven duplicate keys the complete xlsx export exposed
(`KNOWN_FINDINGS.json` section `findings_first_visible_in_2026-09-18_export`:
six Artifact Index rows beyond the markdown prefix and one Evidence Lineage
row) sit in rows the markdown export never delivered, so the 2026-09-18
proposal's line-quoting checker had nothing to quote for them and that
proposal, frozen under CLAUDE.md rule 8, was not edited. A numbered successor,
`registers/collision_proposal_2026-09-19.json` with companion
`registers/COLLISION_PROPOSAL_2026-09-19.md`, covers exactly those seven
(`python3 tools/collision_proposal_check.py --proposal registers/collision_proposal_2026-09-19.json`
— 7 records, 7 successor identifiers, every row quoted cell for cell from the
JSON tabs and re-hashed, the xlsx re-hashed against `registers/source/SOURCES.json`,
no successor colliding with any register identifier or with the first
proposal's). Reading the cells classified the pairs: five Artifact Index pairs
and the Evidence Lineage pair are one Drive object registered twice with
different status text (the `EV-LS-REQ030` rows share a Drive ID and a content
digest but differ in eight of sixteen cells, so they are not the exact duplicate the
finding text suggested); `GP-REQ-194-v1.0` is one identifier on two different
review requests with two different Drive documents, the case `OP-CNS-001` §2
names by hand. One pair (`LS-MAN-045-v1.0`) has its append order and its
self-reported timestamps inverted, and the region around it is not
chronological, so the keeper-by-append-position rule there needs the operator
to confirm append order from the workbook's history. Still a proposal; still
nothing repaired; together the two documents cover all 23 findings once each,
and the test file asserts it. A second adversarial pass the same day found that
the document-level summary (`summary_counts`, the classification lists), the
recorded `row_canonical_bytes`, the follow-up `from`/`to` identifiers, the
`workbook_tab` labels, the recorded list of predecessor-issued ids, the append
target and the companion's verbatim row blocks, table and prose counts were
stated but not recomputed, so a falsified summary or a tampered companion block
passed the checker; every recorded value was re-derived and found correct, and
the checker now recomputes each of them with a CLI negative control per check.
That the values were correct was luck the checker could not distinguish from
design; the checks, not the values, are what changed.

**2026-09-19b.** The fourteen duplicate primary keys that keying `relations`,
`review_ledger` and `definitions` surfaced (§5, sixth item; `KNOWN_FINDINGS.json`
section `findings_first_keyed_2026-09-19`) are covered by a third numbered
proposal, `registers/collision_proposal_2026-09-19b.json` with companion
`registers/COLLISION_PROPOSAL_2026-09-19b.md`, `successor_of` the 2026-09-19
document by SHA-256, which in turn names the frozen 2026-09-18 document
(`python3 tools/collision_proposal_check.py --proposal registers/collision_proposal_2026-09-19b.json`
— 14 records, 14 successor identifiers, every row quoted cell for cell and
re-hashed, the chain walked two deep with every predecessor's digest checked
on disk, no successor colliding with any register identifier or with anything
either predecessor issued; together the three documents cover all 37 findings
once each, and the test file asserts it). Reading the cells classified the
pairs: the ten `REL-036…040`, `REL-048`, `REL-049`, `REL-132…134` pairs, both
Review IDs and `DEF-049` are one identifier assigned to two different objects
(different Source URLs, different Exact Object IDs, different Source URLs
respectively), each in a block that was renumbered from an already-issued
number; `REL-EC021-CLS141` is the one same-object pair, and it is not the exact
duplicate the finding text calls it: the relation triple, Source URL and Status
agree, but six of fifteen cells differ (Exact scope, Evidentiary effect,
Authority effect, the Target URL's sheet fragment `#gid=200011` vs `#gid=200012`,
Provenance and Last reviewed), so the VOID-DUPLICATE remedy the workbook applied
to the same EC-021 double registration in four other tabs (Closure Log row 22,
Operator Decisions row 20, Transition Register row 68, Review Independence
row 42) under `GP-COR-143-v1.0` is discussed and not applied. The checker gained a per-tab
drive-object rule (Source URL for the Relation Index and Definition Registry,
Exact Object ID for the Review Independence ledger, which carries no URL), a
SAME_RELATION token bound to the relation triple, locators REL / RVL / DEF, a
workbook-tab map verified against the importer's sheet names, a
predecessor-chain walk, and a cross-read of this file and `registers/README.md`
that fails when prose attributes to one of the fourteen identifiers an
exact-duplicate claim or a cell count the live rows refute (the README had made
exactly such a claim about the one same-object pair, three lines from a pointer
to the record that refutes it, and so had the rationale it came from, which is
corrected in the same commit with a note of what it said before); each has a CLI
negative control. The bare
`REV-P02-GP-INTERVAL-001` is cited in other tabs for both of its objects
(Evidence Lineage row 39 names the keeper's `ADJACENCY_TO_ONE_CUBIC_TYPED_MS`;
Evidence Lineage rows 47 and 48 and Transition Alarms row 59 name the later
row's `P02_DETERMINISTIC_INTERVAL_CERTIFICATE`), so re-keying the later row
would leave three of those citations pointing at the keeper: recorded as a
residual question, not repaired. Still a proposal; still nothing repaired.

---

### 4.3a The bound-slack registry, and its counterexample to itself

`python3 -m research.slack.registry`; `python3 -m pytest -q tests/test_slack.py`
(52 tests; 13 mutations of the module were run against a scratch copy and every
one was caught). Seven records, each with a named carrier and source, values as
certified intervals over exact rationals, ratios exact.

| bound | slack ratio | band | unsound? |
|---|---|---|---|
| `chi2_grad_bound` (frozen RN engine, lines 1690–1720) | `[1.00e19, 1.01e19]` | UNUSABLE_AS_STATED | no source says so |
| `T4_kap(5) = C_comp·T4_form(5)` (candidate) | `[3.9e5, 4.0e5]` | SEVERE | — |
| τ entrywise envelope, `‖Δ‖_F²/λ_min` (engine header) | `[5.32e3, 5.34e3]` | SEVERE | — |
| `c_Z·r²` floor for `Z_r` at the rung (H3) | `[1.92, 1.92]` | TIGHT | — |
| `I_far` upper (RN3 far-zone) | `[1.27, 1.27]` | TIGHT | — |
| `cone_slope_margin` (GP-DATA-106) | `[1 − 7.04e−18]` | VIOLATION_WITNESSED | **yes**, CL-AUD-202 |
| `envelope_v` (d3_perc.py) | `[0.302, 0.304]` | VIOLATION_WITNESSED | **yes**, RN5 |

The rule the module enforces in code rather than prose: a slack ratio measures
a bound's *utility*, never its *correctness*. `severity()` never reads
`known_unsound`; the two are computed from disjoint data, and `known_unsound`
is reachable only through a witness the module re-computes by exact interval
comparison — a merely loose bound's own numbers, offered as a witness, prove
the opposite of a violation and are refused. The registry's own data is the
demonstration: **the tightest record is unsound and the loosest is not known
to be.**

Three things the build settled beyond the table:

- The headline's `~1e19` is reproduced from the source's quoted digits held as
  rounding intervals — `[1.565e14, 1.575e14] / [1.5625e−5, 1.5635e−5]` — and
  every endpoint of the resulting enclosure has order of magnitude 19. Both
  inputs are quoted from prose, so the ratio is INDICATIVE_ONLY; the one-line
  diagnostic §4.4 names would make it certified.
- The engine's own header case (`×5.3e3` on τ) is the same mechanism as the
  headline at one order lower: near-null directions of `S_pair` that a
  log-likelihood ratio cancels and an entrywise envelope does not.
- CL-AUD-202's printed overshoot `+6.083e−20` does not match its printed
  stored digit string read as an exact decimal (`+7.3506e−20`); it matches the
  IEEE-754 double whose shortest repr that string is. Both readings are
  pinned rather than one chosen.

Stated gaps: no record lands in the ROUTINE `[2, 10)` or LOOSE `[10, 1e3)`
bands, and whether that bimodality is a property of the corpus or of the
search is not established; the RN3 §9 factor of ~7.5 was examined and
deliberately not registered because it is a corrected-versus-defective
diagnostic pair, not a claimed-versus-attained bound. The registry refutes no
bound and repairs nothing.

## 4.4 The frozen RN engine, recovered byte-exact — and what reading it found

`python3 engine/rn_engine/verify_recovery.py --selftest`. `LANE_RN_UNIF.md`
caveat 5 recorded `d3_rn_unif.py` as never downloaded, leaving the lane's
preferred carrier `rnu_ds3.py` un-runnable. `drive/source_map/Archive_Members.csv`
resolved it; five ZIP carriers agree on the payload digest; the whole
`D3_percolation` + `H2_foundations` + `H3_closure` tree is now under
`engine/rn_engine/frozen/`, every file byte-exact against the member index
(`d3_rn_unif.py` 103,166 B / `85d7725f…`), verified independently of the agent
that recovered it.

The Drive "reading copy" was not the file: it is an access reading *volume*
bundling fourteen unrelated sources, and Google Docs renders each source newline
as two. `engine/rn_engine/reconstruct.py` inverts that and returns bytes only on
a digest match. Across the same volume it recovers 11 of 14 members and fails on
3 — LaTeX `\boxed{` exports as `\x08oxed{`, `\frac` as `\x0crac`. Those three
are candidates for the accessibility exception list. Exactness is per payload.

What the engine's own text settles (`docs/ENGINE_RECOVERY.md`, with line refs):

| finding | where |
|---|---|
| The ~1e19 slack in `chi2_grad_bound`: `log q` is a *signed* sum of four terms; line 1718 sums their absolute values. Each term is `dS` conjugated by `S_pair⁻¹` (`1/λ_min ≈ 3.8e9`); `gdetM` carries the inverse twice → `1/λ_min² ≈ 1.5e19`. A log-likelihood ratio cancels its near-null directions; `abs()` discards that. Structural, not measured — the one-line diagnostic is named and deliberately not run. | §3.5 |
| `mean_grad_exact` builds `dTY` alone, omitting both residualisation terms of `dTY6` — a rule the same engine writes correctly at line 1882. Corroborated independently by `rnu_meanfix.py`. A bound fed a defective derivative is not loose; its certification is void. | §3.6 |
| `run_certification`, `kap_fn_p1` and `env_tau` are defined and **never called**. The frozen body's last executable statement is a four-point probe loop. That is the literal form of "certifier never invoked". | §3.8, §3.7 |
| The image sum is the first shell only; its tail is a side *assertion* (`< 1e-60`) never added to the value, evaluated at point separations only, itself truncated with no remainder, and float. That is `OBL-H5-JETMOD`'s "lattice-tail constants re-certified uniformly in the band", located. | §3.2 |
| `T4_form`, `T4_kap` and `C_comp` are **not in the frozen engine** at all; the EXECUTE freeze list's "valid T4 from `env_form`" refers to `rnu_t4.py`, which sits under `CANNOT_VERIFY`. | §3.7 |
| `mp.dps = 100` at line 41, never changed; no `Fraction`, no `Decimal`, no interval arithmetic anywhere in 2,241 lines. | §3.9 |

Recovery closes a caveat and makes a carrier re-runnable (given `mpmath`). It
closes neither piece of `D3-LEMMA-RN-UNIF`, discharges nothing, patches nothing
in the frozen body, and produces no number.

## 4.5 Operator questions raised by the lane ledger

- The ledger caught a transcription error in this repository's own
  `docs/OPEN_PROBLEMS.md`: §D's heading said the 24 routes were "all
  unassigned", while the register shows 23 unassigned and `RV-LM009-MAIN`
  carrying "OPS4 nonauthor / exposed / OpenAI". The register is the source; the
  heading is corrected. (Counted with the port-fidelity failure, the RN3 scope
  sentence, the superseded protocol and the TB-G2 guess, this is the fifth
  defect the harness has found in this repository's own work today.)
- When the ledger first ran, 22 of the 24 review routes had no object in
  `claims/graph.json`, so lane D's blocked-claim count (2) understated the
  queue's weight. The claims lane then added the eight LM routes as nodes and
  lane D now blocks ten claims, which moves it to the top of the dispatcher's
  ranking above `A5` (3) and `A1` (2). That is a scheduling signal — the review
  backlog is the heaviest open item the graph can see — and nothing more; 16
  routes still have no graph object, so the count is still an understatement.
- The `STATUS_STRENGTH` refusal ordering in `tools/lanes_check.py` is the
  agent's construction, documented as a promotion-blocking order and never a
  mathematical hierarchy, with unknown values failing closed. Its tiers should be
  sanity-checked by an operator.
- (2026-09-19) **Which independence rule governs — answered by the register,
  not open.** The LIVE governance index names `OP-PROT-011` as the Independent-Eyes
  rule while the canon's `HISTORICAL OP-PROT-011 — INDEPENDENCE GUIDANCE;
  TECHNICAL REVIEW NOW R17` carries a 2026-07-25 SUPERSESSION NOTICE; for a few
  hours on 2026-09-19 this bullet presented that as an open operator question.
  It is not: `operator_decisions` rows `OD-OP-PROT-011-20260724-001` (Active
  FALSE, "Superseded 2026-07-25 for approval and terminal routing by
  OP-PROT-012. Retained as independent-eyes evidence guidance") and
  `OD-OP-PROT-012-CROSSFAMILY-20260725-001` ("Direct operator answer; not
  inferred from Drive content", source CL-GOV-222) record the operator's answer,
  `artifact_index` labels OP-PROT-011 "RETAINED AS TECHNICAL PREDICATE /
  SUPERSEDED AS LIVE APPROVAL ROUTER BY OP-PROT-012", and R17 (2026-09-17) then
  superseded OP-PROT-012's approval routing in turn. The owner-authority
  sentence stands; `governance/README.md` now carries the chain.

## 4.6 The runner and its receipts — recording that cannot decide

`python3 engine/run.py --list`; `python3 tools/receipts_check.py`. The
repository can now run its lanes and record what happened, with the recording
machinery structurally unable to decide anything. A receipt (`q0.engine.receipt/v1`)
carries the lane, what was run with a recomputable argument digest, the
repository commit with a dirty flag, an outcome from a closed enum, every
numeric result tagged `certified_interval` / `exact_rational` /
`float_noncertifying`, and a mandatory `does_not_establish` that the writer
refuses if missing, blank, a placeholder or under 40 characters. Its `verdict`
field admits exactly one value, `NO-MATHEMATICAL-VERDICT`. Every number is
stored as a string — rationals as `num/den` — so the canonical JSON is
byte-stable across platforms and the body hash means something.

Append-only is enforced three independent ways: exclusive creation (`open(path,
"x")` is the only file-creating call and there is no remove, replace or
truncating open), self-hash recomputation, and a git-HEAD byte comparison in
the checker. The third is not redundant with the second, and the runner
demonstrated it on the real tree: a forger who edits a committed receipt *and*
recomputes its body hash defeats the hash check entirely and is caught only by
the comparison against HEAD.

What the first runs recorded (four receipts, 42 results, 16 certified
intervals, 26 exact rationals, 0 floats, 0 problems):

- **A1** reproduces the ladder arithmetic in certified arithmetic, exactly:
  spread `836263/10000`, `12.9091819…%` of `404878/625`. Its reference
  normalised enclosure `S(B)/r³` over `[1/40, 1/20]` is very wide —
  `[7990, 63980]` — because naive interval division cannot see that numerator
  and denominator move together with `r`. Recorded so nobody reads that width
  as meaningful; a mean-value form or band subdivision would tighten it.
- **A5** runs an end-to-end certified cover of the RN5 annulus on a reference
  integrand: 2,036 cells visited, 1,020 accepted, 0 rejected, **0 pending**,
  exact rejected-area bound 0, certified total `[6.2287, 6.2784]` containing
  the independent closed form.
- The RN5 `envelope_v` defect, quantified in the same receipt: the defective
  expression's fourth power is `1.526e−5` against `(0.207675…)⁴ = 1.860e−3`
  for the quantity it claims to dominate — the "bound" sits about **122×
  below** what it bounds.

The runner's mutation harness (13 single-point mutants of the receipt writer,
checker and runner) killed 11 on the first pass. One survivor was a real gap —
deleting the empty-`does_not_establish` branch survived five controls — and
was fixed; the other is a proved equivalent mutant (the exclusive-create
`FileExistsError` is the second line of defence behind the removed
`os.path.exists` pre-check). Two extra receipts exist because `--all` was run
twice during smoke testing; they were deliberately *not* deleted — deleting a
receipt to tidy the tree is exactly the reasoning append-only exists to refuse.

`.github/workflows/research.yml` runs the lanes weekly and on dispatch under
`permissions: contents: read`, uploads receipts as an artifact, and has no
commit, push or pull-request step; a governed-path guard fails the job if
`claims/`, `engine/lanes/`, `registers/` or `drive/` changed. A cron entry is
not an operator.

## 4.7 Twenty-five carriers bound; one merge refused; two indexes

`python3 tools/carriers_verify.py`; `python3 tools/lanes_check.py`;
`python3 engine/next_action.py`. `engine/carriers/MANIFEST.json` binds 25 of the
Drive's 206 hash-stamped runnable carriers, 20 with stored bytes at the time (22
since 2026-09-19, below), each blob at a
path that *is* its content address and re-checked against both the manifest and
its row in `drive/inventory.jsonl`. Every record carries `certifying: false`,
and the verifier refuses `certifying: true` on anything but exact rational
arithmetic. `arithmetic` was read off each carrier, not its title: 3
`exact_rational`, 6 `mpmath_float`, 4 `float`, 8 `mixed`, 4 `unknown`. Five
records stored no bytes, each with a reason from a closed vocabulary (`SIZE`,
`QUARANTINE_EXCLUSION`, `QUARANTINE_PATH`, `RECONSTRUCTION_INCOMPLETE` ×2); on
2026-09-19 the two `RECONSTRUCTION_INCOMPLETE` carriers (`d1_falsify_v4.py`,
`C104_COUPLED_PERSISTENCE.py`) were read again through the Drive connector,
decoded from the session transcript instead of retyped, and stored once their
digests and byte counts equalled the inventory's, so three records now store no
bytes and the reason is unused. Zero hash mismatches. Binding is not review, replay or endorsement, and nothing a
carrier prints about itself (`DISCHARGED`, `CERTIFIED`) is adopted.

The integration step then tried to fold the eight recovered RN-engine files of
`engine/rn_engine/BINDING.json` (§4.4) into this manifest, so that one index
would serve every lane. `tools/carriers_verify.py` refused the dry run with 43
problems — a ZIP member has no inventory row, no Drive id and no access status,
and the manifest's vocabularies are closed on purpose. The refusal was correct:
an archive member is not a Drive file, and a record shape that describes one
misdescribes the other. So there are two indexes, each validated against the
thing it actually records (the inventory for Drive files, `Archive_Members.csv`
for archive members), and `tools/lanes_check.py` resolves a lane input against
either and says which one it resolved against. The lane ledger is now bound:
A1 (3 inputs), A2 (1), A3 (1), A5 (13 — five Drive carriers and the eight
engine members), B (5), C (4), D (1); the remaining six lanes carry `inputs: []`
with a stated reason. A control removes `BINDING.json` from a copy of the ledger
and asserts the unmutated lanes fail on `RNENG-01`: the second index is
load-bearing, not decorative.

What this does not establish: nothing bound here has been run, replayed or
reviewed in this repository; 154 active-lane carrier rows (144 distinct digests)
remain unbound; the 19 carriers under `02_LEGACY_Q0_ARCHIVE` are deliberately
not bound and a negative control proves the verifier rejects any that appears.

## 4.8 The frozen-object register, cross-checked offline — and a mapping that promised a checker

`python3 tools/frozen_check.py`. The Drive freezes objects by recording a Drive
ID, an expected byte count and an expected SHA-256 in the `frozen_objects`
register (188 rows), under a *binding class* that says what the digest is of.
That makes part of the register decidable offline against a second Drive-side
record, the accessibility publication's own digests in `drive/inventory.jsonl`:

| binding class | rows | what the digest is of | offline verdict |
|---|---:|---|---|
| A — whole-file byte freeze, D — exact raw file | 55 | the whole Drive object | **all 55 agree** on SHA-256 and byte count |
| B — marker-delimited body, C — native export | 133 | an *extracted* body | not comparable to the containing object; 17 body digests occur byte-exact in the payload / archive-member index, 116 do not occur anywhere the source map indexes |

The one row a naive whole-file comparison flags — `GP-DER-118-v1.10`, a
30,933-byte raw markdown file whose frozen body is 29,293 bytes — is a class-B
row: the two digests differ by construction, not by drift. A control pins that
class-B/C rows are never compared against the whole-file digest, because a
checker that did so would report 116 false drifts and bury a real one.

What this establishes: two independent Drive-side records of the same 55 raw
objects agree. What it does not: no body this repository does not hold is
verified; the register's own `Drift Status` column is carried verbatim and not
re-graded; nothing is re-frozen; no status moves.

The finding underneath: `governance/GIT_ADAPTATION.md` stated since the port
that the frozen-object register was verified by `tools/verify_manifests.py` and
that "CI fails if any frozen byte drifts". No checker consumed the register at
all — `verify_manifests.py` reads `_MANIFEST.jsonl` and `MANIFEST.sha256` files,
of which the repository contained none — so the sentence described a mechanism
that did not exist. The same document referenced `drive/vault_tree.txt` and
`drive/handoffs/*.json`, neither of which existed. A scan of every
path-like reference in the architecture documents found those four (the
frozen-object claim, the vault tree, the handoff directory, and the per-folder
manifests). The vault tree now exists as metadata only (7 items, never opened);
the frozen checker now exists with eleven negative controls; the handoff and
manifest rows are corrected as the 2026-09-18 Drive delta lands, since that
delta carries the program's actual handoff envelopes.

## 4.9 The Drive moved after the snapshot; the repository now says so

`python3 tools/drive_index.py stats`; `python3 tools/verify_manifests.py`. A
freshness check on 2026-09-18 found the Drive changed after the 2026-09-17
publication the repository mirrors: a folder-structure session (six month
folders renamed and re-parented, thirteen origin-collection trees grouped under
one container, one index renamed, the source-map Sheet retitled), a proposed
Drive–GitHub execution contract with work-order and receipt design examples,
a reusable-operations guide with two new register tabs, and edits to the
Research Home and router. Each changed Drive folder is now mirrored under `drive/deltas/2026-09-18/` (byte-exact where the object has a payload digest; the Research Home and the router are native Docs, which have none, so they are held, if at all, only as reading copies — until 2026-09-19 this sentence said "byte-exact" of them too) with a manifest `tools/verify_manifests.py`
checks in CI; every stored file was re-hashed here and compared with Drive's
byte count and, where the producing session published one, with its own
digest. The 238 inventory rows whose path the restructure changed are listed
in `PATH_CHANGES.jsonl` — derived here from the session's rollback record and
equal to the session's own count — and `tools/drive_index.py` overlays them,
with controls that the overlay is bound to the export row by row and fails
closed on a row it cannot tie to it. The export itself is not edited.

What this establishes: which bytes the Drive held on 2026-09-18 for these
objects, and where the inventory's paths are stale. What it does not: the
contract's disposition is "PROPOSED EXECUTION CONTRACT / NOT DEPLOYED" and
nothing in it was applied; the rollback record is `RECORDED_ONLY_NOT_EXECUTED`
and stays so; the session's own `scientific_dispositions_changed: 0` is its
statement, not this repository's verification.

## 4.10 The operator's one sentence about a Git repository

The FORMALIZATION BOARD's OPERATOR PACKAGE DECISION of 2026-07-24 (Drive
`10o4YRYOr8a2fB6rtnFnzMn7HkQMfv9-FZ5Mh0L-KF_o`) reads: "REPOSITORY ROUTING:
private Git repository creation and exact-history push are approved when
platform access becomes available." It approves a *private* repository, and it
sits beside "NOT APPROVED: blanket terminalization or mathematical promotion of
any included claim." Until 2026-09-19 this section called it the only operator
sentence in the corpus about a repository; it is the decision, and two GP/CL-line
records restate it — the 2026-07-24 COMMAND CENTER ("Git repository status:
OPERATOR-APPROVED / PLATFORM-BLOCKED — create and push the private repository
when connector or platform access is available. Do not retry unsupported
creation in a loop and do not claim a repository already exists.") and
CL-REQ-225 Rule 5, which keeps "the GitHub repository item" recorded "exactly
this way". `governance/GIT_ADAPTATION.md` now cites all three. This repository is public. The 2026-09-18 execution-contract draft
records the owner asking to restrict who works in it rather than to hide it;
the two statements are both recorded here and the visibility decision is the
owner's. Nothing in this repository changed because of either.

## 5. Checkers that were narrower than they read

The adversarial challenge found three, all fixed in the checker so they cannot
recur:

- `reviews_check.py` scanned only `*.json`; an 841-line controls appendix in
  `records/` was never opened. It now scans every `.md` there and reports any
  whose stem matches no record.
- Two sibling documents stated their honest negative in exactly the promotion
  wording the reviews rule forbids under negation. Reworded to the positive
  form; verbatim-quoted register rows keep theirs.
- Three blobs were indexed but reachable from no ledger record.
  `recovery_check.py` now asserts reachability through `stored_path` or
  `related_artifact` and names the route.

A fourth, found by binding the lanes (§4.7) rather than by the challenge: the
dispatcher `engine/next_action.py` resolved inputs against `MANIFEST.json` only,
while `tools/lanes_check.py` accepted the same inputs from either index — so the
two tools disagreed about lane A5, one reading its inputs as bound and the other
printing `INPUTS no`. The dispatcher now consults both indexes and its note says
which one each lane resolved through; a control drives the workspace copy
through both tools with the same inputs.

A fifth, named from outside on 2026-09-19 (the CI-hardening candidate recorded
under `drive/deltas/2026-09-19/CHATGPT_CI_INTEGRITY_HARDENING_CANDIDATE/`,
stored as data and not applied): the lane-invariants step of
`.github/workflows/research.yml` read `test -f tools/lanes_check.py && python
tools/lanes_check.py || echo "skipped"`, so a `lanes_check` failure printed
"skipped" and the job stayed green. The step now runs the checker bare, and
`tests/test_workflows.py` scans every `run:` step of both workflows and refuses
any repository checker or pytest invocation followed by `|| echo`, `|| true` or
`|| :`; its negative controls include the pre-2026-09-19 line itself. The rest
of that candidate is the owner's to integrate or not; its patch no longer
applies to the current head.

A sixth, found on 2026-09-19 while recording the cross-register observations
of `registers/README.md` as bound records: `tools/registers_check.py` keyed
twelve tabs and had never read the id column of `relations`, `review_ledger`
or `definitions`. Keying them surfaced 14 duplicate primary keys that both
exports carried from the first commit (eleven Relation IDs, two Review IDs,
one Definition ID), now allowlisted row by row in `registers/KNOWN_FINDINGS.json`
(`findings_first_keyed_2026-09-19`) and covered, one record per key, by the
third numbered collision proposal `registers/collision_proposal_2026-09-19b.json`
(§4.3, 2026-09-19b). The five observations themselves are now records whose
cited cells, inventory rows and path changes the checker re-verifies on every
run (`observations_cross_register`, 45 bindings); a control drifts one cited
cell, deletes one cited row, moves one cited object into another lane, and
makes the absent allowlist id reappear, and the checker refuses each.

---

## 6. Standing scope

`OBL-H5-JETMOD`, `OBL-H5-ZBAND`, `OBL-H5-REMOTE-THRESHOLD`, `OBL-D1-PROMOTE`,
both pieces of `D3-LEMMA-RN-UNIF`, `PERC-DECAY` and `OBL-B1-BRANCH` stand
exactly as `docs/OPEN_PROBLEMS.md` records them. The 2D and 3D tracks are not
composed anywhere. Original prize problems solved: 0. Every independence gate
on every review route remains open. A certified arithmetic is not a certified
result, and a green run is a run.
