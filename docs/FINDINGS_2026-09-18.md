# Findings of 2026-09-18

What the active layer found on its first day, in the order the discipline ranks
them: defects in this repository's own work first, then decidable questions
settled, then what was recovered, then what remains open. Every number below is
reproducible from the repository by the command named beside it. Nothing here
promotes, closes or reclassifies any claim; every status stands as its source
records it.

---

## 1. Defects in this repository's own migration

Found by the review harness reviewing the migration that built it. These rank
first because a repository of research claims that quietly inflates one of its
own is worth less than no repository.

### 1.1 No mirrored artifact is byte-identical to its object

`registers/json/review_queue.json` row `RV-OPS-R17` declares its object as
14,073 bytes, SHA-256 `04987ba4…`. The repository's copy at
`governance/protocols/OP-PROT-019-v1.1_R17.md` is **14,073 bytes, SHA-256
`efcfdd5c…`**. The byte count matches and the digest does not, so a byte-count
check on that file confirms the wrong bytes.

Checked systematically that morning: **0 of 18** mirrored artifacts matched any
of the 325 full digests the registers declare. By afternoon the count was
**3 of 7 restored** (§1.1a below). Two causes:

| file | delta | cause |
|---|---|---|
| `OP-PROT-019-v1.1_R17.md` | +0 B | export normalised the content; byte count coincidentally unchanged |
| `FULL_DOCS_MATH_READ.md` | +239 B | the prepended provenance header, which asserted exactness, was itself the only thing making the file inexact |
| `R17_IMPLEMENTATION_REPORT.md` | +262 B | same; each delta is the header's own length |

(The header's original two-word wording is not reproduced here beside the
file's name, because `tools/provenance_check.py` is literal by design and has
no quotation escape — the same rule the reviews directory applies to promotion
language. This document is held to it like any other.)

The clause violated is in the protocol whose port violated it — `OP-PROT-019`
§2: *"The digest establishes identity, not truth or authorization."*

Fix, in two stages. First, `governance/PROVENANCE.json` recorded declared and
actual digests for every mirrored artifact — at that point none claiming
exactness — and `tools/provenance_check.py` began refusing to let any file
describe a non-exact copy as verbatim or byte-exact, naming the byte-count
trap instead of passing it quietly. Second, the repair in §1.1a restored three
objects from their declared digests, and the record now claims exactness for
exactly those three. Twelve controls, including a false-positive control so
the corpus's own discussion of byte-exact capsules does not trip the checker,
and one that constructs the byte-count trap and asserts it is still named.

Source: `reviews/records/REV-OPS-R17-001.json`, finding 1.

### 1.1a The repair: three restored, four unverifiable, two of those altered

`python3 tools/provenance_check.py` — `artifacts=7 byte_exact=3 problems=0`.
Each source Drive id was downloaded raw; a file was replaced **only** where the
download hashed to a full 64-hex digest the corpus declares, and an
independent verifier re-downloaded and re-hashed everything afterwards.

| artifact | outcome | who declares the digest |
|---|---|---|
| `OP-PROT-019-v1.1_R17.md` | restored, `04987ba4…` | a **register** row (`RV-OPS-R17`) |
| `FULL_DOCS_MATH_READ.md` | restored, `448adee5…` | source map + inventory only |
| `R17_IMPLEMENTATION_REPORT.md` | restored, `7b2f6cc1…` | source map + inventory only |
| `OP-PROT-012.md`, `OP-GDN-002.md`, `OP-CNS-001-R0.2.md`, register export | unverifiable — no digest anywhere in the corpus | nobody |

The sharp case's same-length difference is identified exactly: one blank line
inserted after the title (first differing offset 74) and the object's final
newline dropped — `copy == object[:74] + b'\n' + object[74:-1]`. The two docs
were the object plus a prepended header; the header is gone and its contents
live in `PROVENANCE.json`.

Unverifiable is not unaltered. Against the raw native-Doc exports,
`OP-CNS-001-R0.2.md` **omits the export's first line** — *"Live rule:
00_LIVE_GOVERNANCE - Independent-Eyes Rule (OP-PROT-011)."* — and
`OP-PROT-012.md` carries " at time of export" inserted into its Status line
with curly quotes flattened. Those are content divergences in reading copies
of governance protocols, recorded because no digest can settle them. Full
byte-level detail: `docs/PORT_FIDELITY_REPAIR.md`.

### 1.2 "RN3's far-region proof is outside the affected scope" was true of too little

The sentence is accurate about the far-region proof and about nothing else in
that object. RN3 §9's conditional arithmetic imports the near target
`17.6804 r³`, which the RN5 repair places inside or beside the affected scope
(corrected diagnostic ≈ `2.34195 r³` against wrong-power ≈ `17.67237 r³`, a
factor of about 7.5), so the displayed `I_near + I_far < 20.51352 r³` rests on
a number the erratum touches. Both places this repository stated the sentence
are now scoped. The same review observed that RN3's two named zones — far
`|y| ≥ 5`, near `0.1 ≤ |y| ≤ 5` — leave the open disc `|y| < 0.1` unaccounted
for in the §9 sum.

Source: `reviews/records/REV-RN3-FARZONE-20260918.json`.

### 1.3 The reading order presented a superseded protocol as current

The Drive object behind `governance/protocols/OP-PROT-012.md` is titled
*"HISTORICAL OP-PROT-012 — APPROVAL ROUTING SUPERSEDED BY R17"*. The reading
order listed it second without the qualifier. The supersession as titled is
scoped to approval routing and R17 retains parts of the historical protocols,
so this is flagged in `governance/README.md`, not resolved. Which clauses
survive R17 is an operator question.

### 1.4 The contribution plan guessed wrong about recoverability

`docs/CONTRIBUTION_PLAN.md` §7 asserted the TB-G2 capsules were recoverable
from Drive revision history. They are not: see §4.2 below. The section now
says so.

### 1.5 CI could not fail on six of its own checkers

The step pattern `test -f X && python X || echo "skipped"` — introduced by me
this morning for the review, recovery and collision checkers and copied by the
runner agent for the carrier, lane and receipt checkers — swallows a **real
failure**: if the tool exists and exits nonzero, the `||` branch runs and the
step passes. Confirmed by execution: a deliberately failing command under that
guard exits 0. For those six steps "what CI enforces" was, until fixed, only
"what CI runs". The fix is `if [ -f X ]; then python X; else echo skipped; fi`,
which skips only when the tool is absent. Sixth defect the harness has found in
this repository's own work today; applied once the runner agent releases
`.github/workflows/ci.yml`.

---

## 2. Decidable questions, settled by exact arithmetic

Reproduce with `python3 -m research.lpw.headline` and
`python3 -m pytest -q tests/test_lpw_headline.py tests/test_identities.py`.

### 2.1 LPW headline: direction decides admissibility

Exact constant `260 / (3790446482793 · 2⁴⁰ · 10²¹)` =
`6.2385427029355877929430410845…e−44`.

| decimal | position | as upper bound | as lower bound | as equality |
|---|---|---|---|---|
| delivered `6.239e−44` | above exact by `+7.33e−05` rel; correctly rounded to 4 s.f. | **admissible** | not admissible | not admissible |
| proposed `6.238e−44` | below exact by `−8.70e−05` rel | **not admissible** | admissible | not admissible |

The §C repair direction is wrong for an upper bound: a decimal below the exact
value asserts `quantity ≤ 6.238e−44`, which is stronger than the chain
supports. And under an equality headline **nothing rounded is admissible**,
because the reduced denominator carries a prime other than 2 and 5, so the
expansion never terminates. This settles direction-soundness only; it does not
verify the derivation behind the fraction, and qualitative LPW is unchanged.

### 2.2 `E(|ξ|+|η|)⁴ = 12 + 16/π` is false

True value `12 + 32/π = 22.18591635788…`. The claim dropped one of the two
equal cross terms `4E|ξ|³E|η|` and `4E|ξ|E|η|³`, each worth exactly `16/π`.
Exact in the basis `{1, 1/π}`. The sibling audit found one further identity of
that shape in the repository — the Rayleigh fourth moment `Eρ⁴ = 8` — and
confirmed it exact; six items of roughly the right shape could not be settled
and are recorded with reasons. Complete over the repository, incomplete over
the Drive. Refuting the identity flags its consumers; it does not by itself
invalidate any derivation that cited it.

### 2.3 The A1 display-vs-enclosure gap, made numerical

From the three published `I_hi/r³` rung certifications quoted in
`CHART_SIDE_JETMOD_PLAN.md`:

- At the **same** `r = 0.05`, frozen v1 `731.4311` and live v3 `647.8048`
  differ by **12.91%**.
- Under the natural reading of the displayed κ = 1/8 modulus,
  `|Δ(I_hi/r³)| ≤ C·δ^κ`, the two adjacent bands force `C ≥ 23.17` and
  `C ≥ 5.18` — a **4.47×** discrepancy.

Stated with its assumption: this is the natural reading of the displayed fit
applied to `I_hi/r³`, not a quotation of a source's own modulus statement. It
refutes nothing and discharges nothing. It shows, in numbers, why three point
certifications cannot substitute for a band enclosure — which is exactly the
distinction the sources draw and `OBL-H5-JETMOD` names.

---

### 2.4 The A1 machinery, and what building it showed

`python3 -m research.bands.ladder`; `python3 -m pytest -q tests/test_bands.py`
(57 tests, 12 negative controls). `research/bands/` reproduces §2.3's numbers
from its own Fractions — spread `836263/10000`, `12.9092%`; `C ≥ 5.1818` and
`C ≥ 23.1709`; ratio `4.4716` — with the assumption attached to every returned
object and printed by every formatter.

Three things the build established beyond §2.3:

- **`ratio(κ)` has a closed form and is strictly decreasing** —
  `(|Δ_wide|/|Δ_narrow|)·(δ_narrow/δ_wide)^κ` — with certified values 4.6696 at
  κ = 0, 4.4716 at 1/8, 3.3017 at 1, 1.1672 at 4, 0.8253 at 5. It crosses 1
  only between κ = 4 and κ = 5: no exponent below 4 reconciles the two adjacent
  bands under one constant.
- **The tail bound is uniform in the displacement, and that is the whole
  point.** For a kernel with a certified decay envelope, the omitted-lattice
  tail over an interval box depends on the displacement only through the box
  radius — proved by the reverse triangle inequality on shells, with no second
  truncation on the Gaussian branch and an integral comparison on the power
  branch. The frozen engine's `tail_bound` hard-codes a point separation
  (`rho = m*_LT − 17`). At the program's own period the proved Gaussian tail is
  ~1e−498 against a band width of 1.6e−3, which is exactly why the engine's
  "< 1e−60" side-assertion reads as sufficient and why the defect is easy to
  miss: the missing thing is not the tail's size but that it is never carried,
  and never uniform.
- **Two testing defects in its own first draft, found by running the controls
  against broken copies and fixed before shipping.** Wide-box containment tests
  passed even with the tail bound deleted, because band variation swamps a
  5.6e−3 tail — a point-band test was added. And a 4× shell undercount was
  uncatchable by domination on the Gaussian, because the honest bound is already
  ~13× the true omitted mass — two independent closed-form reimplementations
  were added and asserted faithful. The measured looseness (12.7× point, 13.5×
  box on the Gaussian; ~2× on the inverse-power kernel) is reported, not hidden.

What it does not do: compute a band bound for any jet of this program. The
kernels exercised are reference kernels. The central missing piece is a
certified decay envelope for the program's `kplane` at every order the 24-jet
set reaches — the engine's own `_he_abs_poly` is a one-axis majorant where the
true term is two-axis — plus the jet definitions, the band endpoints and the
six-pin displacement geometry. `OBL-H5-JETMOD` is OPEN (display only).

### 2.5 The A5 cover driver — the one the lane records as unwritten

`python3 -m pytest -q tests/test_cover.py` (29 tests, 12 negative controls).
`research/cover/` is the annulus Riemann-sum driver RN5's next exact action
describes and `LANE_RN_UNIF.md` records as unwritten: a region-generic adaptive
cover whose accept / refine / reject / pending ledger is the first-class
output. Partition exactness is decided structurally over `Fraction` by
coordinate compression, never by area comparison — the sharpest control in the
package is a cover whose cell areas sum exactly to the domain's while carrying
both a gap and an overlap, which any area-based validator accepts.
`total()` raises `PendingCellsError` while any cell is pending and there is no
bypass flag; at max depth cells are left pending, not accepted. The RN5
annulus `0.1 ≤ |y| ≤ 5` and the T4 polar region `d ∈ [5, 17]` are instantiated
separately and never summed.

What building it established, as exact arithmetic rather than concern:

- **The acceptance rule's area factor cancels.** `width(|C|·rng) ≤ tol·|C|/|D|`
  reduces to `width(rng) ≤ tol/|D|`: a zeroth-order sup cover imposes a
  *uniform* bound on the integrand's oscillation per cell regardless of where
  the cell sits. That is the near-axis refinement cost RN5 says must be faced,
  in one line.
- **The polar anisotropy is 50×, exactly.** At a target Cartesian cell
  diameter of 1/10 the annulus needs 98 radial × 629 angular = **61,642**
  uniform cells; inner arc extent ≈ 9.99e−4 against outer ≈ 4.99e−2, the ratio
  being `r_hi/r_lo = 50` exactly. No fit, no sampling.
- **θ-halving alone cannot tighten the T4 cover past its shell resolution.**
  For a purely radial integrand, halving θ leaves a cell's range enclosure
  unchanged, so any tolerance below the shell's radial floor is unreachable at
  any depth. Demonstrated by running it, not asserted — and it bears directly
  on the freeze step `LANE_RN_UNIF.md` names as "polar cover d ∈ [5, 17] with
  theta-halving".
- **Choosing polar cells moves the boundary difficulty rather than removing
  it, and the module says where to.** Radii 1/10 and 5 are rational, so the
  annulus boundary is exact and the rejected boundary area is exactly 0; in
  exchange every cell area carries π as a certified enclosure rather than an
  exact rational. The Cartesian bracket of the same region rejects straddling
  cells with retained boundary-area bounds and residual enclosures instead.
- **Two holes in its own negative controls, found by mutation and reported.**
  Disabling the interior-gap branch of the partition check left the suite
  green, because both original gap controls had gaps running to the domain's
  top edge and were caught by the trailing check instead — interior-gap and
  empty-strip controls were added. Dropping the unresolved-boundary residual
  also survived, because the control that should have caught it never reached
  `total()` — it now asserts a residual on every rejected row. One
  redundant-guard survivor (two independent guards on the non-certifying
  label) is recorded as such rather than claimed as coverage.

What it does not do: certify any cell of the program's actual cover. Every
integrand exercised is a labelled reference; a float probe is included only to
prove the NON-CERTIFYING label propagates to the total and the receipt. The
constant-per-cell bound costs O(tol⁻²) cells, so a tight cover of the 78.5-area
annulus with a real integrand needs a higher-order cell bound behind the same
protocol. Both Pieces of `D3-LEMMA-RN-UNIF` remain OPEN; the lane receipts still
carry `lemma_closed: false`.

## 3. What the certified interval library's audit found

`research/interval/` is the foundation the program lacks: every blocking
obligation demands certified interval bounds, and every computational carrier
in the corpus runs on `mpmath` floats. Four adversarial lenses attacked the
library after it was built. Reproduce with
`python3 -m pytest -q tests/test_interval.py`.

| defect | consequence | fix |
|---|---|---|
| exact-rational `exp` endpoints grow as `1.4427·\|t\|` bits; quadratic through `erf`/`Phi` | `Phi(Interval(-10**5), 1)` was OOM-killed — an unconditional failure on the Gaussian tail, which is what tail bounds need | cap the exponent at `2²¹` bits; past it, underflow degrades to the honest `[0, 2^-limit]` and overflow raises |
| argument sizing used `len(str(n))` | CPython 3.11 refuses `int→str` above 4,300 digits, so `sin`/`cos` of a large argument raised `ValueError` before any arithmetic | bound the digit count from the bit length; an upper bound is the safe direction |
| three containment-breaking mutants survived the whole suite | in `__abs__`, the sign of the `sin` extremum at negative odd multiples of `π/2`, and monotone endpoint selection on wide inputs | one test per mutant, each confirmed to fail against a deliberately broken copy |
| `erf` tail branch clamped `hi` to exactly 1 | valid but crude; a test then pinned the clamp rather than the property | tightened to a real bracket (`1 − erf(8)` in `[1.12230e−29, 1.12250e−29]`, true `1.12243e−29` inside); the test now asserts containment against an independent literal |

The last row is a lesson about tests, not code: a test that records an
implementation fails when the implementation improves.

---

## 4. Recovered, reviewed, proposed

### 4.1 Six nonauthor technical reviews, all at zero independence credit

`python3 tools/reviews_check.py`. R17 §4 permits a fresh nonauthor session of
any provider to perform technical review; independence is recorded separately.

| route | object author | verdict | why credit is zero |
|---|---|---|---|
| `RV-OPS-R17` | openai | AMEND | not the same-provider rule — the author family differs. `OP-PROT-012` §5's predicate fails on (b), (c), (e), (i): no frozen task spec, the reviewer had read this repository's derived port first, no frozen comparison hash |
| `RV-RN3` | openai | AMEND | same predicate failures, plus the queue's own note that Anthropic far-zone work is not a review |
| `RV-P15-A`, `-B` | — | PASS_TECHNICAL | prize track; HOLD, not for submission; `original_prize_closed` stays false |
| `RV-P15-C`, `-D` | — | AMEND | as above |

Every record carries `gate_status_after: UNCHANGED`, which the checker refuses
to accept any other value for. The adversarial challenge re-downloaded objects
and hashed them itself, and independently reproduced the reviews' negative
controls (RN3's cover recount 13,612 / 13,604 / 13,596 with exactly 8 tangency
boxes; `η = 0.116820011`; P15-C's `M = m(3s−2)` by enumeration). Verdict: no
fabrication, no overclaim.

### 4.2 Thirteen exceptions recovered; the named priority is not recoverable

`python3 tools/recovery_check.py` — 31 records: **13 RECOVERED**, every one
digest-corroborated; 3 CANDIDATE; 15 UNRECOVERABLE.

Recovered: three archive members via the member index, eight
`ENCODED_BLOCK_FAILURE` chunks re-extracted from `LS-DATA-009-v1.1`, two gzip
payloads from `CL-AUD-084`. Several came back only because the Drive has moved
since the 2026-09-17 snapshot.

The TB-G2 capsules (`LS-DATA-015-v1.0-R1/R2`) are **unrecoverable**, with the
reasons established on fresh bytes: all three shells report
`createdTime == modifiedTime`, so no post-creation revision exists; their bodies
are still a bare UTF-8 BOM; the corpus states no digest for any of the eight,
so even a recovered body could not be corroborated; and the "Corrected" shells
were created minutes after an original whose own capsule is titled *"MALFORMED
PAYLOAD … DO NOT USE"*. Missing content is not invented.

One recovery surfaced an identity conflict: `LS-DATA-009-v1.1`'s result exists
under two digests — the carrier-declared `07e02214…` and the accessibility
publication's decoded-capsule `39e3defd…`. Both are retained; neither is
silently preferred.

### 4.3 Sixteen identifier collisions, proposed not repaired

`python3 tools/collision_proposal_check.py` — all 16 findings in
`registers/KNOWN_FINDINGS.json` covered, 13 successor identifiers proposed under
`OP-CNS-001` §2's preserve-and-disambiguate remedy, exported registers
byte-unchanged. Needs an operator.

---

### 4.3a The bound-slack registry, and its counterexample to itself

`python3 -m research.slack.registry`; `python3 -m pytest -q tests/test_slack.py`
(52 tests; 13 mutations of the module were run against a scratch copy and every
one was caught). Seven records, each with a named carrier and source, values as
certified intervals over exact rationals, ratios exact.

| bound | slack ratio | band | unsound? |
|---|---|---|---|
| `chi2_grad_bound` (frozen RN engine, lines 1690–1720) | `[1.00e19, 1.01e19]` | UNUSABLE_AS_STATED | no source says so |
| `T4_kap(5) = C_comp·T4_form(5)` (candidate) | `[3.9e5, 4.0e5]` | SEVERE | — |
| τ entrywise envelope, `‖Δ‖_F²/λ_min` (engine header) | `[5.32e3, 5.34e3]` | SEVERE | — |
| `c_Z·r²` floor for `Z_r` at the rung (H3) | `[1.92, 1.92]` | TIGHT | — |
| `I_far` upper (RN3 far-zone) | `[1.27, 1.27]` | TIGHT | — |
| `cone_slope_margin` (GP-DATA-106) | `[1 − 7.04e−18]` | VIOLATION_WITNESSED | **yes**, CL-AUD-202 |
| `envelope_v` (d3_perc.py) | `[0.302, 0.304]` | VIOLATION_WITNESSED | **yes**, RN5 |

The rule the module enforces in code rather than prose: a slack ratio measures
a bound's *utility*, never its *correctness*. `severity()` never reads
`known_unsound`; the two are computed from disjoint data, and `known_unsound`
is reachable only through a witness the module re-computes by exact interval
comparison — a merely loose bound's own numbers, offered as a witness, prove
the opposite of a violation and are refused. The registry's own data is the
demonstration: **the tightest record is unsound and the loosest is not known
to be.**

Three things the build settled beyond the table:

- The headline's `~1e19` is reproduced from the source's quoted digits held as
  rounding intervals — `[1.565e14, 1.575e14] / [1.5625e−5, 1.5635e−5]` — and
  every endpoint of the resulting enclosure has order of magnitude 19. Both
  inputs are quoted from prose, so the ratio is INDICATIVE_ONLY; the one-line
  diagnostic §4.4 names would make it certified.
- The engine's own header case (`×5.3e3` on τ) is the same mechanism as the
  headline at one order lower: near-null directions of `S_pair` that a
  log-likelihood ratio cancels and an entrywise envelope does not.
- CL-AUD-202's printed overshoot `+6.083e−20` does not match its printed
  stored digit string read as an exact decimal (`+7.3506e−20`); it matches the
  IEEE-754 double whose shortest repr that string is. Both readings are
  pinned rather than one chosen.

Stated gaps: no record lands in the ROUTINE `[2, 10)` or LOOSE `[10, 1e3)`
bands, and whether that bimodality is a property of the corpus or of the
search is not established; the RN3 §9 factor of ~7.5 was examined and
deliberately not registered because it is a corrected-versus-defective
diagnostic pair, not a claimed-versus-attained bound. The registry refutes no
bound and repairs nothing.

## 4.4 The frozen RN engine, recovered byte-exact — and what reading it found

`python3 engine/rn_engine/verify_recovery.py --selftest`. `LANE_RN_UNIF.md`
caveat 5 recorded `d3_rn_unif.py` as never downloaded, leaving the lane's
preferred carrier `rnu_ds3.py` un-runnable. `drive/source_map/Archive_Members.csv`
resolved it; five ZIP carriers agree on the payload digest; the whole
`D3_percolation` + `H2_foundations` + `H3_closure` tree is now under
`engine/rn_engine/frozen/`, every file byte-exact against the member index
(`d3_rn_unif.py` 103,166 B / `85d7725f…`), verified independently of the agent
that recovered it.

The Drive "reading copy" was not the file: it is an access reading *volume*
bundling fourteen unrelated sources, and Google Docs renders each source newline
as two. `engine/rn_engine/reconstruct.py` inverts that and returns bytes only on
a digest match. Across the same volume it recovers 11 of 14 members and fails on
3 — LaTeX `\boxed{` exports as `\x08oxed{`, `\frac` as `\x0crac`. Those three
are candidates for the accessibility exception list. Exactness is per payload.

What the engine's own text settles (`docs/ENGINE_RECOVERY.md`, with line refs):

| finding | where |
|---|---|
| The ~1e19 slack in `chi2_grad_bound`: `log q` is a *signed* sum of four terms; line 1718 sums their absolute values. Each term is `dS` conjugated by `S_pair⁻¹` (`1/λ_min ≈ 3.8e9`); `gdetM` carries the inverse twice → `1/λ_min² ≈ 1.5e19`. A log-likelihood ratio cancels its near-null directions; `abs()` discards that. Structural, not measured — the one-line diagnostic is named and deliberately not run. | §3.5 |
| `mean_grad_exact` builds `dTY` alone, omitting both residualisation terms of `dTY6` — a rule the same engine writes correctly at line 1882. Corroborated independently by `rnu_meanfix.py`. A bound fed a defective derivative is not loose; its certification is void. | §3.6 |
| `run_certification`, `kap_fn_p1` and `env_tau` are defined and **never called**. The frozen body's last executable statement is a four-point probe loop. That is the literal form of "certifier never invoked". | §3.8, §3.7 |
| The image sum is the first shell only; its tail is a side *assertion* (`< 1e-60`) never added to the value, evaluated at point separations only, itself truncated with no remainder, and float. That is `OBL-H5-JETMOD`'s "lattice-tail constants re-certified uniformly in the band", located. | §3.2 |
| `T4_form`, `T4_kap` and `C_comp` are **not in the frozen engine** at all; the EXECUTE freeze list's "valid T4 from `env_form`" refers to `rnu_t4.py`, which sits under `CANNOT_VERIFY`. | §3.7 |
| `mp.dps = 100` at line 41, never changed; no `Fraction`, no `Decimal`, no interval arithmetic anywhere in 2,241 lines. | §3.9 |

Recovery closes a caveat and makes a carrier re-runnable (given `mpmath`). It
closes neither piece of `D3-LEMMA-RN-UNIF`, discharges nothing, patches nothing
in the frozen body, and produces no number.

## 4.5 Operator questions raised by the lane ledger

- The ledger caught a transcription error in this repository's own
  `docs/OPEN_PROBLEMS.md`: §D's heading said the 24 routes were "all
  unassigned", while the register shows 23 unassigned and `RV-LM009-MAIN`
  carrying "OPS4 nonauthor / exposed / OpenAI". The register is the source; the
  heading is corrected. (Counted with the port-fidelity failure, the RN3 scope
  sentence, the superseded protocol and the TB-G2 guess, this is the fifth
  defect the harness has found in this repository's own work today.)
- When the ledger first ran, 22 of the 24 review routes had no object in
  `claims/graph.json`, so lane D's blocked-claim count (2) understated the
  queue's weight. The claims lane then added the eight LM routes as nodes and
  lane D now blocks ten claims, which moves it to the top of the dispatcher's
  ranking above `A5` (3) and `A1` (2). That is a scheduling signal — the review
  backlog is the heaviest open item the graph can see — and nothing more; 16
  routes still have no graph object, so the count is still an understatement.
- The `STATUS_STRENGTH` refusal ordering in `tools/lanes_check.py` is the
  agent's construction, documented as a promotion-blocking order and never a
  mathematical hierarchy, with unknown values failing closed. Its tiers should be
  sanity-checked by an operator.

## 5. Checkers that were narrower than they read

The adversarial challenge found three, all fixed in the checker so they cannot
recur:

- `reviews_check.py` scanned only `*.json`; an 841-line controls appendix in
  `records/` was never opened. It now scans every `.md` there and reports any
  whose stem matches no record.
- Two sibling documents stated their honest negative in exactly the promotion
  wording the reviews rule forbids under negation. Reworded to the positive
  form; verbatim-quoted register rows keep theirs.
- Three blobs were indexed but reachable from no ledger record.
  `recovery_check.py` now asserts reachability through `stored_path` or
  `related_artifact` and names the route.

---

## 6. Standing scope

`OBL-H5-JETMOD`, `OBL-H5-ZBAND`, `OBL-H5-REMOTE-THRESHOLD`, `OBL-D1-PROMOTE`,
both pieces of `D3-LEMMA-RN-UNIF`, `PERC-DECAY` and `OBL-B1-BRANCH` stand
exactly as `docs/OPEN_PROBLEMS.md` records them. The 2D and 3D tracks are not
composed anywhere. Original prize problems solved: 0. Every independence gate
on every review route remains open. A certified arithmetic is not a certified
result, and a green run is a run.
