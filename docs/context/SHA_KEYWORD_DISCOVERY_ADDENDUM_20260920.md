# Retrieval addendum: a same-named reconstruction in archives

The active inventory and dated current-membership records contain no title or
path match for `CL_ANTHROPIC_BUNDLE`, `rnu_t4.py`, `rnu_spine.py`, `rnu_env.py`,
or `rnu_white.py`. Exact archive-member suffix search, however, finds four
occurrences of `rnu_white.py` with the same 13,344-byte payload SHA:

`859e1963b1b7c84f2c1f14b4b163fb5575ad41c5394d27c51c0d1ceff70d2dc3`

| Carrier ID | Member path |
|---|---|
| `1z_zNdgxtbs7XWxLu56HN8_QbVXxdmMMq` | `output/rn_math/rnu_white.py` |
| `1z_zNdgxtbs7XWxLu56HN8_QbVXxdmMMq` | `round3/intake/CLOSE_prior.zip!/output/rn_math/rnu_white.py` |
| `1WZfhLuZBEzvdUUkQw7v5JubmAiI1W4gt` | `output/rn_math/rnu_white.py` |
| `1TJr1awRjB0D9niq1Jyi6XGnJKuvW9Wmx` | `MATH-20260917-b9c2/output/rn_math/rnu_white.py` |

The last occurrence was reconstructed from the already-verified, non-held
repository coverage packs using the repository's Reader. Its whole carrier
SHA was checked against
`3b16c3712ed9bfe69f82e0fd288a1af72a7b7a5c4de8a7ece23935eb270bdbcb`;
the selected member's length and full SHA were checked against the values
above. Its bytes were read as source text and were not executed. This is a
verification of stored snapshot bytes, not a new live-Drive byte download.

The source header explicitly identifies this as an **author-side
reconstruction dated 2026-09-17**, with no RN-UNIF closure assertion. It is
therefore not evidence that the historical missing Claude module has been
recovered merely because its basename matches. Keep this distinction in
the search results and navigator.

The recovered text includes exact interval Schur conditioning jets, a
sufficient interval LDL positive-definiteness check, fixed-frame whitening,
and rational norm envelopes for whitened Gaussian chi-square derivatives
through order four. It requires independently established field-input
enclosures on one common domain. Its numerical point-evaluation functions
are explicitly non-certifying. Its interval type is self-contained historical
source code; new repository work should still use `research.interval`.

Local verified reading copy:
`/Users/dylanroy/Documents/Codex/2026-09-19/realtime-voice-chat/work/readback/rnu_white_859e1963.verified.py`.

No exact archive-member suffix match was found for `rnu_t4.py`,
`rnu_spine.py`, or `rnu_env.py` in the existing Archive_Members table. This
states the scope of the lookup, not complete absence from all possible
storage or undecoded containers. No scientific status is changed.
