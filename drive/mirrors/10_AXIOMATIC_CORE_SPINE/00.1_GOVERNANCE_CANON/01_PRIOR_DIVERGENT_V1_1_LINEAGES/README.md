# `00.1_GOVERNANCE_CANON/01_PRIOR_DIVERGENT_V1_1_LINEAGES`

Drive folder id `1_YLj4gSNbZO0h7irUX3ofW7bZYv__XGh`, **3 inventory items**: the
folder itself (indexed tree-only in the lane-root manifest) and two files, both
held byte-exact. The folder's own name records what they are: two divergent
lineages that both call themselves v1.1.

| file | identity |
|---|---|
| `GATE_FRAMEWORK_MASTER — v1.1-C095-EXECUTABLE-LINEAGE — 27996B — SHA2442dc38 — ID1GShZef2.md` (27,996 B) | **byte-exact**: SHA-256 `2442dc38…`, id `1GShZef2a3ZQP7YwQiQUSuhYdvgNnES_-` |
| `GATE_FRAMEWORK_MASTER — v1.1-UPLOADED-STANDALONE-LINEAGE — 62173B — SHAa16c44d5 — ID1rVk5Shp.md` (62,173 B) | **byte-exact**: SHA-256 `a16c44d5…`, id `1rVk5ShpplkZApTLMqP73WKDhDmX-d_sB` |

## The status banners, verbatim

**The C095 executable lineage** records "Version:** Master v1.1",
"Date:** 2026-07-17", "Status:** current successor specification",
"Supersedes:** Master v1.0; v1.0 is preserved and not overwritten", and
"Set:** one mandatory schema preflight, 16 numbered gates, five schema field
groups". It also carries a standalone notice, set as a blockquote wrapped across
six lines, saying the file is self-contained and that a human or model can
implement the proof-object schema, gate semantics, promotion rules and operating
discipline without any other file; it is not quoted verbatim here because each
of its lines carries the source's own `>` prefix.

**The uploaded standalone lineage** records, on one line,
"Version:** Master v1.1 (standalone) · **Date:** 2026-07-17 · **Supersedes:**
Master v1.0 (2026-07-17), retained and marked per §7 · **Status:** current
unified set — **13 established gates + 1 schema field**, plus 2 candidate gates
staged pending retrodiction (§11, explicitly *not* counted in the established
set)." The middot separators in that quotation are the source's own; the whole
banner is a single line in the file. Of its own predecessor it says the
framework's guarantees "are now stated honestly as a **type system for claims**
rather than as blanket "mechanical verification"", and it states the division of
labour as "**gates classify; disciplines detect** (§0.6)".

## The disagreement, stated and not resolved

Three files in this lane call themselves the current gate specification: these
two and the reconciled v1.2 in the sibling
`00_CURRENT_RECONCILED_SPECIFICATION`. They give two different established-gate
counts — 16 numbered gates in the v1.2 and C095 lineages, 13 established gates
plus 1 schema field and 2 staged candidates in the uploaded standalone lineage.
The v1.2 file lists both of these as things it "Supersedes without overwriting:",
which is consistent with all three being kept. This repository records the
disagreement and adjudicates no part of it; which specification governs is a
source-register question for an operator.

## What this does not establish

Both byte-exact rows establish that this directory holds the same bytes the
2026-09-17 inventory declares for those ids, and nothing further. Mirroring is
not review, replay, endorsement or promotion. The gate counts, verdict words and
supersession claims above are the sources' own descriptions of themselves; none
was decided, checked or graded here, and the fact that two files in one folder
contradict each other is transcribed rather than repaired. No gate described in
either file is run by anything in this repository.
