# CL-TRC-VER-001-v1.0 — Independent CL capsule verification: TRC-EC020-v0.1 and TRC-EC021-v0.1

ARTIFACT ID: CL-TRC-VER-001-v1.0 (proposed; reconcile to next free CL/TRC index before any register append)
AUTHOR LINE: CL — Anthropic Claude, claude-opus-4-8 session
CREATED: 2026-07-23
CLASS: TRC / AUD — non-author capsule package verification + independent hash reconstruction (per TRC-REQ-001-v1.0)
AUTHORITY: none (support role under OD-GP-PROP-175-001). CANONICAL IMPACT: NONE. Seals nothing; the operator closures already stand.
PROVIDER/SESSION: Anthropic / CL line, Cowork session, on Dylan's instruction to help complete the capsules.

[[OBJ:CL-TRC-VER-001-v1.0]] [[PROGRAM:TERMINAL-REPLICATION-CAPSULES]] [[QUEUE:EC-020]] [[QUEUE:EC-021]] [[STATUS:HASHES-VERIFIED-NATIVE-EXPORT-OPEN]]

## 0. HEADLINE VERDICTS

- APPROVE TRC-EC020-v0.1 FOR VALIDATION — embedded-capsule hashes verified 4/4 byte-exact; exact statement/label/exclusions verbatim against GP-COR-140-v1.0 and GP-CLS-PROP-024-v1.0; math scope already CL-reviewed (CL-AUD-073). MANDATORY PRE-SEAL: native-Doc export hashes (E1,E2,E4,E5,E6,E7) and the frozen Closure-Log/register-row hashes (E8) remain OPEN.
- APPROVE TRC-EC021-v0.1 FOR VALIDATION — shared dependency capsule (E8=GP-DATA-114-v1.1) hashes verified 4/4; exact object/label verbatim against GP-CLS-141-v1.0; finite-Q4/exact-field firewall present; GP-DER-044 routed to the exact-torus regularity ID; math scope already CL-reviewed (CL-AUD-074). MANDATORY PRE-SEAL: native-Doc export hashes (E1–E7, E9–E12) and register-row hashes (E13) remain OPEN.

Neither capsule may be marked SEALED until the native-export and register-row hashes exist (TRC-STANDARD-001 P4/P8). This receipt discharges the one hash task that requires raw-byte reconstruction and the verbatim scope checks.

## 1. INDEPENDENT HASH RECONSTRUCTION — GP-DATA-114-v1.1 (EC-020 §3E; EC-021 E8) — 4/4 PASS

Method: extracted both embedded gzip/base64 blocks from the native Doc (ID 1f__v4_Nn61G0yRzJgDSJymMIT7iMB3hUI6kc6qGtU9U), `base64 -d | gunzip`, then `wc -c` and `sha256sum`. No author-side bytes trusted; reconstructed independently.

| object | declared bytes | my bytes | declared SHA-256 | reproduced |
|---|---|---|---|---|
| source .py | 9,787 | 9,787 | 9d7cb5d6e13579533c5b485775e67c77da83c149c0bf8bbbe7b7bf85aefdb4ef | ✅ EXACT |
| source .py (gzip) | 3,416 | 3,416 | 18c8768cf37d4557a8c04a9ae6e60523a185098012527403c0e627b259ff1e5c | ✅ EXACT |
| receipt .json | 5,047 | 5,047 | 8472209ee589f44f1f3c472c7040d183494743a89e21ad9487a1879d192003bc | ✅ EXACT |
| receipt .json (gzip) | 1,692 | 1,692 | 88bb5bce0b875ac2959d7cbcaff235db7ea60249e3124faeccc0d26fe8351222 | ✅ EXACT |

All four declared hashes reproduce byte-exact. (Note: unlike GP-DATA-106-v1.0, whose Docs-rendered capsule did NOT reconstruct — CL-AUD-062 §2b — this capsule's base64 survives the Docs render intact.) The receipt additionally self-references: its embedded `source_sha256` field equals the recomputed source hash, and `source_bytes`=9787. Internal consistency confirmed.

## 2. RECEIPT CONTENT CROSS-CHECKS (derivable without re-execution)

The source imports `mpmath` (interval arithmetic); mpmath is unavailable in this offline sandbox, so I did NOT re-execute the interval certificate. However, because the source+receipt are hash-locked, any environment with mpmath reproduces the receipt from the verified source. I independently checked the receipt's derivable quantities:

- μ = 1/(40√2) = 0.01767766952966369 lies in the declared `mu_interval` ✅
- worst-case reduction s = q/r ≤ q_hi/r_max = −0.98/0.05 = −19.6 matches `s_upper_worst` ✅ (this monotonicity reduction is what makes the box uniform-r on 0<r≤1/20)
- `r_interval` = [0, 0.05] = (0, 1/20] ✅; box `a`=[0.0024,0.0026], `q`=[−1.02,−0.98], c40..c04=[−0.01,0.01] ✅
- binding gate = `handoff_margin` = 0.007485281374238566 = the minimum of all 19 margins, and it EQUALS GP-DER-106's binding handoff margin exactly (CL-AUD-062) — cross-consistency between the fixed-r and uniform-r boxes ✅
- 5/5 negative controls PASS; `overall` = PASS_UNIFORM_R_FIXED_Q_FULL_DIMENSIONAL_DEGREE4_INTERVAL_BOX; no failed gates ✅

## 3. VERBATIM SCOPE CHECKS

EC-020 (TRC-REQ-001 §3):
- A (statement/label vs GP-COR-140 & GP-CLS-PROP-024): PASS — capsule reproduces verbatim "CLOSED — EXACT UNIFORM-r FIXED-q FULL-DIMENSIONAL DEGREE-FOUR CAPTURE CORRIDOR ON 0<r<=1/20" for P01-UNIFORMQ-D4-BOX-001.
- B (exclusions preserved): PASS — no mass/finite-Q4/adjacency/P0.1/P0.2/numerical/theorem/machine/release/deletion, matching GP-COR-140 §1.
- E (GP-DATA-114-v1.1 → four hashes): PASS (§1).
- F (v1.1 lineage vs internal v1.0 labels): PASS — the embedded payload is filename/receipt-labeled GP-DATA-114-v1.0, its four hashes equal the v1.1-declared values, and the corrupt ORIGINAL v1.0 document (distinct Drive ID 18auUSnBQ5273Scv-hvjx7zc6-TxQC2078EtkID27eRw) is correctly excluded; v1.1 is a byte-identical re-publication of the good payload. Consistent.
- D (links/IDs): PASS for the IDs I resolved (GP-DATA-114-v1.1, GP-COR-140, GP-CLS-PROP-024 all resolve).
- C, G, H (reading-order minimality; EC-020 workflow directive verbatim; Closure-Log/transition rows): NOT INDEPENDENTLY RE-VERIFIED here — I did not open 01_LINEAR_READING_ORDER, the EC-020 workflow directive doc, or the live register rows. Flagged OPEN, not failed.

EC-021 (TRC-REQ-001 §4):
- A/B (statement + terminal label vs GP-CLS-141): PASS — verbatim "inf_{0<r<=r_1} P_{r,Q4}^{MS}(B_q) >= c_Q4 > 0" and "CLOSED — EXACT UNIFORM POSITIVE TYPED FINITE-Q4 PALM MASS OF THE FIXED-q DEGREE-FOUR CORRIDOR FOR SUFFICIENTLY SMALL r".
- C (finite-Q4/exact-field firewall): PASS — GP-AUD-112 present (E6); exclusions bar exact-field substitution and normalizer equality.
- D (GP-DER-044 routed to exact-torus regularity ID 1dn8BVsmScu690vX4Z_ewNgl0QRvMDBXcyzWiTtyJeho): PASS — E3 routes to that exact ID with collision reconciliation via GP-CLS-001.
- E (EC-020 dependency + limits): PASS — imported strictly as deterministic set-inclusion B_q⊂H_r^(4).
- H (no numerical constants / broader theorem): PASS — statement explicitly adopts no numerical r_1/c_Q4; the lower bound c_Q4=w_0 c_phi Vol(B_q)/C_Z is symbolic and r-independent.
- G/F (all sources present; HA-008 authenticates the finite-Q4 phrase "APPROVE EC-021 EXACT UNIFORM FINITE-Q4 SCOPE"): the evidence index E1–E13 is complete and HA-008 is cited by GP-CLS-141 §7; I did not open HA-008 directly — flagged as cited-in-terminal-record, not independently re-authenticated.

## 4. THE ONE BLOCKER I CANNOT CLEAR — native Google-Doc export hashes (information Dylan/GP should note)

TRC-STANDARD-001 P4 requires a declared reproducible SHA-256 export for native Docs/Sheets. I CANNOT produce these from the CL connector: it returns a Doc's text content, not a canonical byte export, and Google-Doc exports (docx/pdf/txt) are not byte-deterministic across requests. So E1,E2,E4–E7 (EC-020) and E1–E7,E9–E12,E13 (EC-021) cannot be hash-sealed by me. RECOMMENDATION (architectural, for the lead line): either (a) GP declares a fixed canonical export representation + normalization and publishes each Doc's export hash, or (b) prefer raw-file uploads (like the GP-DATA-*.py capsules) for anything that must be hash-sealed — those reconstruct cleanly, as demonstrated in §1. Register rows (E8/E13) need the deterministic ordered serialization described in TRC-REQ-001 §5 before hashing; that is a GP/live-register action.

## 5. REPRODUCTION

Local artifacts: gp114_src.py (9787 B, sha256 9d7cb5d6…), gp114_rcpt.json (5047 B, sha256 8472209e…), decoded from the two embedded base64 blocks. Cross-check script output recorded above. No file was moved, renamed, or deleted; this is additive support under OD-GP-PROP-175-001. Human approvals (EC-020 directive, HA-008) remain the operator's; this receipt does not restate or substitute them. Silence is not verification.

END CL-TRC-VER-001-v1.0
