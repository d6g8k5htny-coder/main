# H5 — RUNG 2 certified (2026-09-15): r = 0.025, E_w(r)/r³ = 664.3979

OBL-D1-PROMOTE rung 2 of the ladder {0.05, 0.0354, 0.025, 0.0177, 0.0125}.
Same cover, r-scaled (s = 0.5): zone constants c·r, η ∝ r, fixed K/TD slack
parameters; same fail-closed merger (rung-scoped + rung-scaled), same named
lemmas re-certified in r-scaled form. New package; frozen documents unaltered.

## Headline

    I_lo = 1.426579082e-09
    I_hi = 1.038121775e-02  =  664.3979 r^3        (r^3 = 1.5625e-5)
    C1 containment display: C1_TOTAL(0.025) = 1.605315e-4·(0.5)^3
                          = 2.006643750e-5  ∈ [I_lo, I_hi]  PASS
    coverage self-test (81×73 rung-scaled grid): PASS
    mutation suite at rung 2: 6/6 trip; process-level A/B: byte-identical
    totals file: h5_totals_r0.025_v2.json
      sha256 f7697bcfa0fe32b5c87c8ef8adef5d4384ab1bda7cd4a7c4782fef8c99103fcb
      (versioned 'x'-mode write; the merger never overwrites any totals file)

## Part ledger, r-law display vs rung 1 (v3 clean, 647.8048·r³)

| part | rung 1 (v3) | rung 2 | ratio | r³ = 0.125 |
|---|---|---|---|---|
| 2·R1 (70 arch-band cells) | 1.2217e-2 | 2.4976e-4 | 0.0204 | faster (conservative) |
| 2·rim envelope | 1.9492e-4 | 2.3698e-5 | 0.1216 | ≈ r³ |
| S-cone envelope | 1.3161e-3 | 1.5943e-4 | 0.1211 | ≈ r³ |
| M-fwd envelope | 2.7759e-3 | 3.6363e-4 | 0.1310 | ≈ r³ |
| M-bwd envelope | 3.4604e-8 | 1.6661e-9 | 0.0482 | faster |
| S-disk envelope | 4.1383e-3 | 5.1708e-4 | 0.1250 | r³ (1e-4 relative) |
| stitch (dominant) | 5.7890e-2 | 8.7622e-3 | 0.1514 | r³ × 1.211 |
| remote (B_remote·r³) | 2.4438e-3 | 3.0547e-4 | 0.1250 | exact by construction |
| 2·patches_hi | 1.1e-6 | 5.86e-8 | 0.053 | faster |
| TOTAL I_hi | 8.0976e-2 | 1.0381e-2 | 0.12820 | r³ × 1.0256 |

I_hi/r³: rung 1 = 647.8048, rung 2 = 664.3979. The r-scaled certificate is
nearly r³-invariant (+2.56% per halving) — against the fixed-geometry
certificate's 8.9×/halving blow-up documented in D1 v2.0. C_unif candidate
731.4311 (frozen v1 anchor) dominates both rungs with margin 9.2%.

## The stitch slack drift, stated plainly (falsifier watch)

The stitch part's certified envelope decays at ratio 0.1514 per halving
(effective power ≈ 2.73, vs the structural 3). This is certificate slack —
the upper bound's finite-tiling/TM-remainder content — not a deviation of
the truth from C1's ledger: every upper bound remains valid, C1's exact
ledger (−3+6−2+2 = 3) is untouched, and the same slack at rung 1 compressed
731.43 → 647.80 under refinement. Production path (per the obligation):
Grade-B fine-path refinement of the dominant stitch sectors at each rung
(the one-work-unit refiner promote_stitchfix.py already demonstrated:
M-backward sector 6 coarse 2175.69 → refined 2.4174e-4 at 8×5 tiling; the
coarse record stays banked, min-per-sector supersedes). D1's falsifier
(family exceeding C_unif) does NOT trip at rungs 1–2: 664.40 ≤ 731.43.
Extrapolation display (labeled, never consumed): at +2.56%/halving the
envelope crosses the v1 anchor near r ≈ 0.0063 — rungs 3–5 will measure the
drift directly; if it persists, rung-level refinement compresses it.

## Named-lemma re-certification at rung 2 (r-scaled form)

- H5-AXIS v3 regions (S-cone 18°/162°, M-fwd 12°, M-bwd 168°, S-disk
  d_S < 0.062·s): all envelope constants re-certified from banked edge
  probes at r-scaled positions; the constants' r¹ law display (update
  package §7): sdisk max 0.08564 (ratio 0.4998 vs 0.5), sring max 0.06054
  (0.4921); contributions inherit r¹ × area(r²) = r³ (ledger rows above).
- Coverage census at the rung-scaled grid is IDENTICAL to rung 1:
  {rim 365, r1 2409, stitch 2476, remote 1761, env 2495, multi 3108} —
  the r-scaled cover is combinatorially invariant (certificate-level
  display of the promotion thesis).
- Patches: 8/8 sites, lo sum 7.1329e-10 (r³ ratio 0.1222 vs rung 1, ledger
  row exact). (Note: a census by unique θ reads 5 — the site key is (θ,δ);
  all 8 (θ,δ) sites banked; the merger's ck counts sites, not θ's.)
- Z window: live-recomputed (H3 c_Z·r² lo; own-bracket hi), byte-consistent
  with the banked rung-2 driver windows.
- Remote: B_remote = 19.55 (frozen v1 consumption) carried symbolically;
  D3's κ-inclusive R3 value drops in on landing (merger parameter bremote).

## Gates tripped and cured en route (receipts)

1. stitch 13/22 fail-closed (outer S-ann band incomplete + M-sec-6 coarse
   poison 2175.69): cured by resuming the stitch driver (resume-skip;
   banked sectors preserved) and the one-work-unit sec-6 refiner
   (stitchfix_025_sec6.log, 606s). Merger then passed all cks.
2. In-process A/B differs at 1e-44 (module cache warmup) — process-level
   A/B byte-identical (the falsify standard); noted for the record.

## Rung ladder

    r = 0.05:   731.4311 (frozen v1) / 647.8048 (live v3 clean)
    r = 0.025:  664.3979   ← THIS PACKAGE
    r = 0.0354: stitch outer band + sec-6 refine executing (cells 70/70,
                probes/patches complete; sec6 refined 65.16 → 8.2809e-4)
    r = 0.0177: cells 42/70 (2 shards resuming), probes/patches complete
    r = 0.0125: cells 21/70 (2 shards resuming), probes/patches complete

body-sha256: 91a34d83688b5110b886ab4fc168a55030343cc0da4a98e38347d3b0e6e48102
