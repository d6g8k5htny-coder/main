# GP-LB-STAT-004 -- K3 PARTIAL DELIVERY HOSTILE ADJUDICATION v1.0

Date: 2026-08-05/06 (America/Chicago)

Program: q0 / SIDE24 persistence-pairing asymptotics

Campaign: two-dimensional LB-RATE lower bound for `1 - q(r,6/5)`

Provenance: ChatGPT/GP independent audit of Dylan's exact K3 ZIP carrier and Drive state, with hostile mathematical review isolated from Kimi and AO48 PASS labels. This is an additive status delta. It is not a Kimi-authored carrier and does not replace or mutate any prior raw evidence.

## CONTROLLING DISPOSITION

**REFUTED AS A THEOREM-GRADE OR VALIDLY STATED CONDITIONAL ASSEMBLY / LOWER CAMPAIGN OPEN.**

The package contains useful symbolic, forensic, numerical, minimality, and composition work. It does not establish an all-small-r lower theorem, a valid fixed lower constant, or the displayed `K3-THM-001` conditional conclusion. `K3-THM-001` is therefore **REFUTED AS WRITTEN / NONCONTROLLING**, not promoted. The strongest preserved mathematical content is an abstract conditional implication schema stated in Section 7 below.

## 1. Exact intake and custody

- Outer ZIP: `OKComputer_Project_Gap_Closure(6).zip`.
- Bytes: 22,744,178.
- SHA-256: `b316369b5d4b368f9d0982865b149cce70f3cbfee77879097c931895e1a1fd98`.
- Archive test: 651 regular entries, 62,352,060 uncompressed bytes, all CRC checks passed.
- No absolute paths, traversal, symlinks, special files, encrypted members, duplicate archive paths, case collisions, or control-character paths were found.
- `K3_SIDE24_LB/`: 291 files / 36,446,545 bytes. After excluding 21 cache files and 25 empty files, 245 meaningful files / 36,063,725 bytes remained.
- The individual Drive landing contains 243 of those raw files. The other two meaningful files were already present byte-exactly at Drive root and were not duplicated:
  - `K3-THM-001`: Drive ID `1VCDrxE5sp983c1uMTFCpmH4hJdDUw6Ut`, 9,760 B, SHA-256 `715124dba1f089430594a3322b7d32c3508fbcb10c14f6591a432ff582f8854d`.
  - `OPERATOR_HANDOFF`: Drive ID `1WVYjYHJltR9zVs_XiofmCxRLgthYMFeV`, 4,248 B, SHA-256 `849d5370c7ce5066bb7b15ca1be8d6b6fe8b2ada3daae4f313156fe552e07b5c`.
- Four nested ZIPs are redundant wrappers and were not used as controlling representations. `.pyc`, `__pycache__`, work/cache JSON, and empty artifacts were excluded.
- The Phase-0 ledger and input manifest align on 598 records (578 uploaded-file records plus 20 research-carrier records). The delivery is not a self-contained copy of the 578-file upload corpus: only three of those 578 objects are present by hash, while all 20 research carriers are present.
- Root `MANIFEST.sha256`: 266 records, 264 current matches. `W3_numerics/lbox_A.txt` and `lbox_B.txt` changed after the manifest snapshot and are unsealed.
- W13 is an earlier live-tree snapshot, not a final seal: 223 of 241 listed records currently match, 16 differ, two old transcript names are missing, and 30 later files are omitted.

## 2. Missing-square-root defect remains confirmed

For the typed Kac--Rice window-saddle intensity, Cauchy--Schwarz gives

`rho_WP <= p_grad * sqrt(E[det(H)^2 | grad=0]) * sqrt(P(type and window | grad=0))`.

Replacing the final square root by the probability itself is not a generic upper bound. W2 independently confirms this algebraic defect. Therefore the prior `I_cs` value near `1.30399e-5`, the WP rung upper-bound table, the fitted `5.5e-3 r^1.6` envelope, and every downstream fixed lower constant or `r0` consuming that branch remain quarantined.

W2's corrected pointwise upper inequalities are mathematically useful. They do not supply the required spatial WP bound. W2 Lemma 17's lower-box claim is false as written because it applies a Sidak-type product bound to an off-center Gaussian rectangle without the needed hypotheses.

## 3. Workstream dispositions

- **W2 -- PARTIAL.** Corrected symbolic pointwise upper chain is valid at its stated conditional Gaussian slice scope; no spatial or weighted-Palm closure. Lemma 17 requires amendment.
- **W3 -- OPEN / PRE-FREEZE.** Only 10 plus 15 of 4,608 boxes were processed. No frozen rigorous integral enclosure or formal refutation was delivered. The harness is not clean-room portable because it hard-codes `/mnt/agents/output/...`; its `harness_O.out.sha256` is empty despite a nonempty output; pin/zone mutation receipts are absent.
- **W4 -- NUMERICAL EVIDENCE ONLY.** The independently replayed direct estimate is `I_WP(0.025) = 4.756917657978e-6`, consistent with `4.7602e-6` to about 0.07 percent. This is not an interval enclosure. W4's auxiliary corrected-CS calculation mixes a conditional covariance with a marginal mean, underestimates the relevant moment by factors of roughly 6--9 in audited probes, and has an invalid tail treatment. The direct estimate is not invalidated by that auxiliary error.
- **W5 -- FORENSIC / PARTIAL.** The archive materially improves provenance of the old figures, but provenance does not validate the old quantitative upper bound.
- **W6 -- REFUTED AS A RIGOROUS MODULUS / NUMERICAL ONLY.** `w6_rhofast.py` explicitly uses midpoint `Pwin*G(mid)` and finite Gauss--Hermite approximation. `w6_fullzone.py` uses ordinary floating-point trapezoidal integration, begins its radius grid at 0.08, and supplies no continuum spatial error enclosure. `w6_wp2.py` checks eleven rungs and assumes `P-mono`. The report itself leaves `P-mono` and interval quadrature open. Its fixed law uses one `Y = r*(-1.26,0.24)` unweighted nine-pin Gaussian slice, whereas W10 requires uniform control over every arch `y` under the exact W3-weighted triple-Palm law. No Radon--Nikodym weight bound or transfer is proved. Thus the advertised `3.5e-3 r^(3/2)` or `4.2 r^(3/2)` modulus is not theorem grade.
- **W7 -- PARTIAL MINIMALITY RESULT.** W7 correctly shows that an exponential Gamma-LOC rate is unnecessary for Forms B/C/D if a strictly positive terminal floor and all consumed loss moduli are otherwise proved. It does not remove the need to prove the exit loss is `o(1)`.
- **W8 -- FORMAL NONCLOSURE / INCOMPLETE RECEIPTS.** The Lambda-side report correctly declares nonclosure. The advertised `0.9001` floor is conditional on an unproved sampled finite-difference premise `H-B3`. Referenced optimized transcript, `-O` transcript, script, hashes, identity receipt, and S6 excerpt are absent from the delivery.
- **W9a -- PARTIAL.** Uniform continuation is inferred from finite-rung evidence without a proved monotonicity/interval mechanism.
- **W9c -- HOUSE-GRADE FINITE-INTERVAL EVIDENCE.** It covers only `[1e-6,0.05]`, uses sampled mesh caps rather than analytic suprema, and leaves `r -> 0` open.
- **W10 -- VALID ABSTRACT COMPOSITION INSIGHT, LOAD-PATH GAP OPEN.** The event-inclusion algebra and `eta_r` firewall are sound conditionally. It correctly identifies that the lower composition may need only a WP loss `o(1)` after multiplication by a Lambda-side `r^3` mass, not WP `= O(r^3)`. But it requires every loss uniformly under the exact weighted triple-Palm law across all arch locations. The delivery proves neither that law transfer nor the essential Bonferroni pair term `o(r^3)`.
- **W11 -- METHODS ONLY.** Useful method inventory; no theorem-grade new bound is supplied by literature routing alone.
- **W12 -- HOSTILE FINDINGS MATERIAL.** The Isserlis display omits mean terms and overcounts by exactly `2*(det(nu nu^T))^2`; the direction is conservative for the affected upper estimate, but the printed identity is wrong. This does not repair the W6 or weighted-Palm gaps.
- **W13 -- STALE SNAPSHOT / NONCONTROLLING.** Selected normal and `python -O` replays are byte-identical and useful. The reported PASS predates later mutations and cannot seal the final delivered tree.

## 4. `K3-THM-001` defects

`K3-THM-001` is not a valid conditional theorem as written:

1. The displayed coefficient `0.9666` is a measured tier, while the package's stated theorem-tier far-field factor is `0.089569*P0`.
2. `0.9001 * 0.9666 = 0.87003666`, not the printed `0.8705...`.
3. The hypothesis list omits W10's essential Bonferroni pair-term hypothesis `o(r^3)`.
4. H3 incorrectly promotes W6's numerical calculation to a proved modulus.
5. The needed uniform transfer from an unweighted fixed nine-pin conditional law to the exact W3-weighted triple-Palm law over all arch locations is absent.
6. The Lambda-side premise is not proved; `0.9001` remains `H-B3`-conditional and its certificate delivery is incomplete.
7. Measured exit/capture zero counts are not a proof that the consumed exit loss is `o(1)`.

The root carrier is preserved byte-for-byte. It is classified **REFUTED AS WRITTEN / NONCONTROLLING**. No theorem successor is minted here.

## 5. Reconciliation with `AO48-AUD-065`

Drive contains `AO48-AUD-065` at ID `1gBREGX2DIw7FmJ0LZ-YQsTLiBas1QaeL`. Its byte-custody reconciliation is useful, including the 264/266 root-manifest result and its W2 body-seal check. Its mathematical promotion is not accepted. In particular, the statements that the WP channel is closed at proved exact-integrand modulus grade, that clean-room reproduction seals the delivered tree, and that `K3-THM-001` is a valid conditional theorem are refuted by Sections 3 and 4 above. `AO48-AUD-065` is an audit artifact, not a separate authorized control fold.

## 6. Numerical evidence retained without theorem promotion

- Rigidity-zone estimates `4.7602e-6` and `4.756917657978e-6` agree to about 0.07 percent at `r=0.025`.
- The larger full-zone value near `7.50e-6` is reported for a different domain and is not a contradiction to the rigidity-zone values.
- Corrected-CS values and the missing-square-root ratio are strong diagnostics, not a successful final composition.
- No mesh, floating-point agreement, finite rung list, or high-precision point value is promoted to an interval-enclosed continuum theorem.

## 7. Strongest valid conditional implication

The following implication schema survives the audit:

If

1. `Lambda_r/r^3 -> c_Lambda > 0` at the exact consumed scope;
2. the exact far-field failure under the required Palm law is uniformly bounded by some `p_far < 1`;
3. every arch, WP, exit, capture, localization, normalization, and adjacency loss tends to zero uniformly under the exact W3-weighted triple-Palm law, with the `eta_r` transfer carried; and
4. the Bonferroni pair term is `o(r^3)`;

then the W10 event inclusion yields

`liminf_{r->0} (1-q(r,6/5))/r^3 >= (1-p_far)*c_Lambda > 0`.

At the package's stated theorem far-field tier, the coefficient is only `0.089569*P0*c_Lambda`. The measured coefficient `0.9666*c_Lambda` may be displayed only under an explicit measured assumption `p_far <= 0.0334`. The archive proves neither the needed positive finite-r Lambda constant nor the complete uniform weighted-Palm loss package.

## 8. Three-track firewall

- **Track A -- SIDE24 three-dimensional upper theorem:** unchanged; AO48-OPR-045 remains ratified at its stated compact positive-mark scope, with RP-C and RP-S closed.
- **Track B -- q0/P0.1 overlay:** unchanged; live LS-CTL-003-v1.3 remains `E0=TRUE`, `E2=TRUE`, `FOUNDATION=TRUE`, `APPLICATION=TRUE`, `INDEPENDENCE=FALSE`, `INTEGRITY=FALSE`, `ELIGIBLE_P01_V110=FALSE`, and `P0.1=HOLD / NOT PROMOTED`. B0 remains fold-ready/state unchanged; C2 remains advisory.
- **Track C -- two-dimensional LB-RATE lower campaign:** OPEN. No fixed lower theorem constant or all-small-r closure is accepted.

## 9. Transport previews

The supplied checkpoint PDFs are preserved only as superseded transport evidence. Their current exact identities are:

- `00_READ_FIRST(1).pdf`: 45,866 B, SHA-256 `66bb924fb8b6633f351c644181ad525f6f103c435646672d7a3550b494fa7e75`, five U+FFFD replacements.
- `ERRATA_2026-08-05.pdf`: 38,404 B, SHA-256 `224f98816a2353597444254cbe19355a774b1d0be0c103aa0350894031a4b9c8`, twelve U+FFFD replacements.

They are not canonical mathematical sources. Exact raw Markdown Phase-0 files are now present in the ZIP and landed individually; their raw bytes, not damaged PDF extraction, control.

## 10. Open blockers and falsifiers

- WP: prove an arch-uniform `o(1)` loss under the exact W3-weighted triple-Palm law, with a rigorous Radon--Nikodym transfer or direct weighted calculation, plus spatial/quadrature/tail/pin-chart/continuum-in-r enclosures.
- P-NMZ-gamma: unnecessary only for the W7 Forms B/C/D route after all other losses and a positive terminal floor are proved; otherwise remains open at consumed scope.
- Lambda side: prove a positive r-uniform constant without `H-B3`, or retain it conspicuously as a premise with complete receipts.
- DER-027a: replace finite-rung/monotonicity inference by an analytic interval proof.
- DER-027c: close `r -> 0` and replace sampled mesh caps by analytic suprema.
- Final assembly: prove the `eta_r` transfer, exit `o(1)`, exact weighted-Palm uniformity, and Bonferroni pair term `o(r^3)`.

A counterexample with a Radon--Nikodym weight concentrated on a small event falsifies any attempted unweighted-to-Palm transfer without a weight bound. A single arch location or a finite rung list falsifies any claim of the required supremum/continuum scope. A nonzero limiting exit loss falsifies the W7 simplified Form C assembly.

## Mandatory control firewall

This work changes no LS-CTL Boolean, eligibility predicate, theorem status, RP status, ratification status, or q0 package status. Any such change requires separate operator adjudication.
