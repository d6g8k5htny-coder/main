AO48-WO-064 -- WP CHANNEL RE-DERIVATION + CAMPAIGN COMPLETION -- work order
for KIMI -- q0 Rate Program / LB-RATE -- 2026-08-05

Echo requirement: every sealed report produced under this WO echoes THIS
file's sha256 (delivered with the ferry; also carried in the Drive title).
Issuing context: AO48-AUD-064 (Drive ID 1wV_F3nW-pyN0N3X-IRGAYG4v8bAXLP3s,
25,860 B, sha256 406406fb...) -- read its Sections 7-9 before starting.

INPUTS
- AO48-AUD-064 (above): the DEF-WP-CS-01 adjudication and blast radius.
- verify_wp_witness_v1.py (your original; sha256 da424a76... per your
  manifest) and verify_thm023_v1_1_v1.py -- the defective formula lives in
  analysis_from: wp_exact = pgrad * sqrt(max(D2,0)) * min(cant, Pw_exact).
- GP-LB-STAT-002 substance (operator relay): GP's independent flag of the
  same defect; GP's transcript byte report (normal 7,953 B / -O 7,948 B).
- KIMI-DER-025 (body 7e76c548...), KIMI-DER-027a/b/c, KIMI-AUD-023 v1.1
  (body a7decb6f...), KIMI-THM-023 v1.1 draft (as-shipped body d5a053bb...).
- Standing: WO-063 remains the prior campaign of record; its sha256:
  e699bf44b7b84fca88faa2b509c1686ea9f8e500aee0dbe618c859b5ec5ad2e2

========================================================================
TASK A -- WP CHANNEL RE-DERIVATION (headline; gates Tasks C and D)
========================================================================
A0 ADJUDICATION ECHO. From your own source copy, confirm the implemented
form wp_exact = pgrad * sqrt(D2) * min(cant, Pw_exact) and record
concurrence with DEF-WP-CS-01 (the linear probability factor is not
certified by Cauchy-Schwarz; the valid CS form carries
sqrt(min(cant, Pw))). ALTERNATIVE: if you can PROVE the linear-P form valid
by an argument not present in the shipped code, present it as a named
derivation -- a valid proof falsifies DEF-WP-CS-01 and collapses Task A to
documentation. Absent that proof, proceed:
A1 PRIMARY ROUTE (condition-then-CS; AUD-064 Section 9(iii)). Given g = 0
and the 9 pins:
  E[|det H| 1{det<0} 1{f in W}]
    = int_W phi_{mu_t, v_t}(u) E[|det H| 1{det<0} | f=u] du
    <= Pw_exact * sup_{u in [b-ell, b]} ( sqrt(E[det^2 H | f=u])
         * min(1, sqrt(P(det<0 | f=u))) ).
H | (g=0, f=u) is Gaussian: mean nu + (cvec/v_t)(u - mu_t), covariance
Sigma - cvec cvec^T / v_t. E[det^2 | f=u] is an explicit quartic in u by
the Wick machinery already in analysis_from. The conditional Cantelli
factor is optional tightening; the sup factor is mandatory.
A2 SUP CONTROL. Certify the sup over the width-ell window rigorously:
endpoint evaluation plus an explicit derivative pad on the quartic (state
the pad's derivation), or monotonicity on the window if provable. Precision
labels per house discipline.
A3 RUNG TABLE. Re-run the full rung table (the WO-063 20-rung set PLUS
r = 0.005) under the repaired bound. Mesh stability: {0.1, 0.05, 0.025} at
r >= 0.02 as before, and AT LEAST TWO meshes at EVERY r <= 0.01 (the
small-rung rows were single-mesh; the r = 0.0025 droop is untested --
AUD-064 D4). Report truth (u-slice engine, unchanged) alongside for the
looseness display.
A4 ENVELOPE AND ORDER. Derive the repaired envelope with its PROVED
exponent and constant. If the repaired channel is O(r^3)-class (expected
under A1), display the constant and the assembly's remainder as 1 - O(r^3)
with explicit r0. If not, state the proved order and the restated
remainder -- do not carry the O(r^3) claim unsupported. Address AUD-064
7.7 (DEF-WP-ORD-02) explicitly.
A5 LB-1[B] INDEPENDENCE. Confirm by source inspection and cite line
references that LB-1's above-b certificate (the E/tol chain
1.7e-23 .. 2.5e-506) nowhere uses the min(cant, Pw)-form product. This
closes the survival claim of AUD-064 7.6 at certificate grade.
A6 CERTIFICATE. Fail-closed (ck()/SystemExit(1), no bare asserts), BOTH
modes (python3 and python3 -O), byte-identical transcripts or a disclosed
single-line diff, falsifier section, WO-064 hash echo.

========================================================================
TASK B -- COMPLETE KIMI-DER-027b (independent of Task A)
========================================================================
Populate sec. 7 (verdict) and sec. 8 (gap paragraph); the skeleton's
expected verdict is PROVED at floor tier conditional on the single named
premise H-B3 -- confirm or revise, never smuggle. Full certificate run,
both modes, byte-identical transcripts, seal the body.

========================================================================
TASK C -- KIMI-AUD-023 v1.2 (gated on Task A)
========================================================================
Amend register item 4, item C029(1), and errata E3/E4: replace the
decertified "certified ub 1.30399e-5 / 1.047*r^3" figures with the Task-A
repaired bound (or mark OPEN if v1.2 must ship before A completes -- state
which). Carry the R2 misprint flag and all v1.1 dispositions otherwise
unchanged; v1.1 remains in the record; new body seal; supersession block
citing a7decb6f4b8b3a896d7e0aadddcd698853e57a9a253ef787173bb3c702136a32.

========================================================================
TASK D -- KIMI-THM-023 v1.1 REBUILD AND SEAL (gated on Task A)
========================================================================
Fix D1 (exponent-band notation and band; AUD-064 Section 8), D2
(c(0.05) = 0.914351274375 -> display 0.91435), D3 (add '0.005' to RUNGS or
drop it from T5-3 -- the shipped list guarantees a KeyError), and the
global-ell mutation hygiene. Fold in 027a and the completed 027b. Rebuild
the T5-4 assembly on the Task-A repaired WP bound; include a
correction-order section per A4. Run the certificate in BOTH modes to PASS
with byte-identical transcripts; then SEAL v1.1 (replace
PLACEHOLDER-BODY-HASH with the real body seal). The v1.0 body
ba3974c02b20dca87d0eaf9fe21d2e574a30d2c12fc4f6b3eb61c8841f73a5fc is
superseded at that moment.

========================================================================
TASK E -- EXPORT CUT (last)
========================================================================
Cut KIMI_EXPORT_<date-of-cut> containing: C022 Observed Update.json exact
bytes (9,435 B; must hash
9bc0647b885931ba4dc862bedfb879f9bd12c4cd6d39f51c6e98e54c7f75d5cb);
LS-DER-030 native; ALL WO-063 and WO-064
certificates, transcripts, and manifests; a per-file sha256 manifest for
the bundle. Two-hex-line convention as ruled canonical (KIMI-DATA-028).

========================================================================
STANDING DISCIPLINE (unchanged from WO-063)
========================================================================
Fail-closed certificates; both-mode runs; precision labels (EXACT vs
decimal-dps-N vs float64-with-certified-pad); named premises, never
smuggled; no status or Boolean claims; theorem-grade vs measured-grade
labels; measured values never become theorem constants; mandatory
falsifier sections; body-seal convention (bytes strictly before the
"SHA-256 of this report body" line).

SEQUENCING: A and B in parallel; C and D after A; E last.
FALSIFIER (for this WO): a valid A0 counter-derivation proving the
linear-P form falsifies DEF-WP-CS-01 and re-certifies the WO-063
quantitative layer; an independent recomputation contradicting any Task-A
certified value beyond its stated pads kills the repair.
