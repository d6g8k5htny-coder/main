# AO48-WO-063 — LB-RATE HARDENING CAMPAIGN (issued 2026-08-04, for the KIMI line)

Operator: upload this file to the KIMI sandbox unmodified and report its sha256; every
KIMI report must echo that hash in its Inputs block. Agent mode is AUTHORIZED and
ENCOURAGED: parallelize tasks across sub-agents, one deliverable owner each, with a
final reconciliation agent that merges, cross-checks constants between deliverables,
and cuts the export bundle. Each sub-agent carries its own independence disclosure.

## STATE OF RECORD (bind to these; do not re-litigate)
- KIMI-THM-023 (body ba3974c0…) is at HOLD per GP mirror and GP-LB-STAT-001
  (3,967 B, sha256 49060aeb858caeb702878b341835cc2e5e5728999b9cfab070945319762d63be).
- KIMI-AUD-023 is at AMEND REQUIRED (GP mirror). KIMI-AUD-024 is at HOLD.
- The AUD-061 reconciliation's "0.9144 proved" admission is SUPERSEDED noncontrolling
  language; v1.1 claims must carry corrected grades.
- The 08-03 export is Drive-resident and verified: 140,956 B, sha256 e7948595…, 25/25
  frames. KIMI-AUD-022 bytes are landed exact (whole-file 8efcd937…).
- Ten original C030/C031 files are unchanged; C031 remains e7998ef0….
- LB-3 availability: 8/9 exact objects recoverable; ONLY exact C022_Observed_Update.json
  (sha256 9bc0647b885931ba4dc862bedfb879f9bd12c4cd6d39f51c6e98e54c7f75d5cb) is absent.
- P0.1 and all Booleans are HOLD/unchanged; AO48-OPR-045 unaffected. NOTHING in this WO
  changes any Boolean. Theorem-grade vs measured-grade labels are mandatory throughout;
  measured values are diagnostic and never become theorem constants.

## TASK 0 — INTAKE INVENTORY (first, cheap)
Hash-inventory every uploaded file (sha256 + bytes), echo this WO's hash, and file the
ABSENT list for anything a task below needs but the intake lacks. One report:
KIMI-DATA-025.

## TASK 1 — WP RESOLUTION (BLOCKING; run before Task 2)
The LB-1 ledger prints |∇f|(WP) ≈ 3e-15 at the witness point while the certified rung
binds 1.3e-5 — a live contradiction and THM-023's named conditional (i). Recompute f
and all first derivatives at the exact witness point on the exact normalized periodized
side-24 field at ≥ 80 dps, with a certified truncation tail (explicit bound < 1e-60),
in BOTH representations — spectral lattice sum ((π/12)-lattice, masses e^{−|k|²/2}) and
wrapped-kernel image sum — requiring cross-representation agreement < 1e-50. Rule which
printed value was wrong and trace the code-path origin of the bad print. Propagate: for
every rung tolerance that consumed the WP value, recompute and display old→new. State
whether LB-1's sup-over-zone conclusion survives — claim it only if the recomputation
proves it. Deliver KIMI-DER-025 + verify_wp_witness_v1.py (fail-closed, SystemExit,
survives -O) + normal and -O transcripts (byte-identical).
FALSIFIER: an independent recomputation disagreeing with your certified value beyond
the stated tail bound kills the resolution.

## TASK 2 — KIMI-AUD-023-v1.1 (BLOCKING; after Task 1)
Execute GP's AMEND REQUIRED. (a) Strike the zone-wide value-kill claim — it is FALSE
as stated: ridge maxima at (1.3056, 0.6858) with m = 1.931 and (−1.1040, 0.8797) with
m = 1.667 stand. (b) Fold in Task 1's corrected WP row. (c) Keep the six rung
certificates (E/tol chain 1.7e-23 → 2.5e-506) untouched — they were verified; do not
re-cut passing certificates. (d) Add an errata table (old claim → corrected claim →
reason). Version v1.1 with an explicit supersession line naming v1.0's body hash.

## TASK 3 — γ-LOC CONDITION (ii-c) DISCHARGE
Quote condition (ii-c) verbatim as LB-3 states it, then deliver ONE of:
(a) a full proof at the R3′/C028 grade, or (b) a proof modulo exactly ONE named
analytic premise with a quantified sufficient condition checked against the station
table of record (the c031_verify.json class, sha256 83313c96…) — the KIMI-DER-009/009b
discipline: name the premise, never smuggle it, and display the discharge chain
step-by-step if you can close it. Machine-certify every computable link. Deliver
KIMI-DER-026 + script + both-mode transcripts. This clears THM-023 conditional (ii).
FALSIFIER: a counterexample to any displayed step, or a premise whose sufficient
condition fails against the station table.

## TASK 4 — RIGOROUS FAR / Λ-GRID / RIDGE REPLACEMENTS (three sub-agents, parallel)
Replace the three measured-grade inputs with proved bounds. Common standard: outward-
rounded interval arithmetic or certified-tail high-precision sums on the exact
periodized field; explicit constants; fail-closed scripts; both-mode transcripts.
4a FAR-FIELD: upgrade the measured far bound to a theorem — starting point is the
   LB-3 S2–S4 recomputation class (lattice (π/12)ℤ², masses e^{−|k|²/2}, |k| ≤ 30,
   tail ≤ 4.4e-185): prove a monotone decay envelope beyond an explicit radius, with
   the envelope's constants derived, not fitted. Deliver KIMI-DER-027a.
4b Λ-GRID (H7): convert grid coverage into a theorem: certified Lipschitz/modulus-of-
   continuity bound for the scanned functional on the zone + a net-spacing inequality
   proving no off-grid excursion exceeds the margin. Explicit constants; state the
   kill condition (a grid spacing that violates the inequality). KIMI-DER-027b.
4c RIDGE: certified two-sided control at BOTH ridge maxima above — interval evaluation
   with second-order localization and stated margins. KIMI-DER-027c.
These clear THM-023 conditional (iii) and the H7 hypothesis.

## TASK 5 — KIMI-THM-023-v1.1 (ASSEMBLY; last)
Assemble Tasks 1–4 into the hardened lower theorem: 1 − q(r, 6/5) ≥ c·r³ for ALL
r ∈ (0, r₀], with EXPLICIT r₀ and EXPLICIT c. Report the honest constant — if the
hardening moves c off 0.9144, say so and show the ledger; do not preserve the number
by weakening a bound silently. Every remaining conditional is either discharged (cite
the Task deliverable + hash) or named in a single labeled list. Include a v1.0 → v1.1
comparison table (claims, grades, constants). All-small-r closure is the acceptance
bar: no r-interval gaps between the rung domains.

## TASK 6 — TRANSPORT SETTLEMENTS (anytime; cheap)
AO48 custody scan (285 files, every operator container, 2026-08-04) confirms NEITHER
item below exists operator-side — recovery is KIMI-session-or-nowhere:
(a) The orphaned pre-update THM body sha256 40596829… that KIMI-AUD-024 cites: recover
    byte-exact from session records and export it, OR file KIMI-DATA-028 declaring it
    UNRECOVERABLE (one paragraph) so AUD-024 is formally re-pointed to ba3974c0….
(b) Exact C022_Observed_Update.json (9bc0647b…): same recover-or-declare.
(c) CANONICALITY RULING: the 08-04 addendum README's DEFECT NOTE fixed the stale
    second hex line in the 08-04 reports (corrected 020/021 builds now landed:
    3b07062b… / d9ab4d6c…). State explicitly whether a corrected re-cut of the 08-03
    bundle exists or the as-shipped b8a8815… bundle (two-hex-line convention) stands
    canonical. Expected: stands as shipped — but say it on the record.
(d) If still held in-session: the M2-class native text/plain exports (S2-DER-010,
    LS-DER-031) and the DATA-015/017 REQUEST-UPLOAD natives. Export any you hold.

## EXPORT SPEC (the reconciliation agent cuts this)
KIMI_EXPORT_2026-08-05: UTF-8 no BOM, LF only, ordinary spaces, one trailing LF, and
ONE hex line after each body-hash marker (the corrected convention). Ship: all report
.md files, all .py scripts, all transcripts (.out.txt and .O.out.txt), MANIFEST.sha256
(whole-file hashes), KIMI_EXPORT_CONCAT.txt with frame lines
"=== BEGIN FILE: name | bytes B | SHA256 hash ===" and a self-test count, README with
any defect notes, and a cover line declaring the whole-zip sha256. If context limits
force a partial export, ship completed tasks with a CONTINUATION marker naming the
next unstarted task — never ship an unverified deliverable to fit.

## EXECUTION ORDER
0 → 1 → 2 → {3, 4a, 4b, 4c in parallel} → 5; 6 anytime. Precision labels EXACT vs
decimal-dps-N on every number. A falsifier section is mandatory in every deliverable.
No status or Boolean claims — HOLD language stands until AO48/GP reconciliation.
