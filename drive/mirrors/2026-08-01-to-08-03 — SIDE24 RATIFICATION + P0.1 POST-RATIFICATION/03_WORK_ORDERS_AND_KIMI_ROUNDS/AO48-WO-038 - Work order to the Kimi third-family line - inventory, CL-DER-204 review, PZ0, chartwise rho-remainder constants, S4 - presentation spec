ARTIFACT ID:       AO48-WO-038-v1.0
AUTHOR:            Claude Opus 4.8 (Anthropic) — AO48
CREATED:           2026-08-02
CLASS:             WO — work order issued to the Kimi line (third provider
                   family, Drive-unreachable, operator-relayed), per
                   AO48-DER-037 §6(b). The verbatim prompt below was handed
                   to the operator this session. Kimi outputs will return by
                   operator paste and be verified/filed by AO48.
CANONICAL IMPACT:  NONE. Task 1 verdict, if APPROVE, supplies the
                   distinct-family review the §5 gap requires — operator
                   ratification still governs.

The prompt (verbatim, as issued):

[see conversation record 2026-08-02; identical text relayed in chat]

Task summary: T0 corpus inventory handshake; T1 CL-DER-204 distinct-family
review (gated on inventory); T2 PZ0 periodization reconstruction (gated);
T3 chartwise relative ρ-remainder constants for Γ_d(u) — self-contained,
formulas embedded, acceptance powers 1/d² (axis), ~1/d (transverse,
generic); T4 the S4 convex-combination identity — self-contained, closes
the corpus's oldest single-source item at third-family tier.
Presentation spec: paste-safe plain text, KIMI-<CLASS>-<NNN> headers,
verdict line, computation-path-labeled receipts at ≥50 digits, falsifier,
self-hash. Calibration fixtures supplied (four 50-digit det Γ values).
