# SIDE24 — RP-F Note Audit & Package-Identity Closure
**Date:** 2026-07-31 · **Auditor:** Claude (independent) · **Inputs:** RP-F fixed-distance witness note (inline); V3.2 audit-reconciliation response (inline); `10_PACKAGE_SCOPE_MATRIX.csv` (inline; no file artifact delivered); re-uploaded `OKComputer_Project_Gap_Closure.zip`, SHA-256 `bd01de231b5ad119eb4f337d958f2e9f5f4c1fc40301780d9ea0e72adacc3c0f`, 109 files.

---

## 1. Package identity — hash-first protocol adopted

**[P3 pinned.]** The re-uploaded bundle is **byte-identical** (full-tree diff) to the bundle audited in my reconciliation. The scope matrix's `P3_REPORTED_V4 / UNAVAILABLE` hash is therefore now pinned: `bd01de23…c0f` for **both** deliveries. My reconciliation's package observations (8 evolved gap_fill DIFFs; 8/8 binaries) attach to this hash.

**[Channel authenticated.]** My deliverable hashes match the matrix exactly: P6 = `9e2e0637…`, P7 = `7be11c3e…`. The V3.2 response's quoted input hash equals my reconciliation snapshot's true hash.

**[G1–G3 adjudications accepted.]** G1 rescopes to P3 only — V3.1/P5 reportedly carries both previously-missing scripts with ledger-verified bytes (P5 not delivered here; recorded as reported, not verified). G2 was a manifest-scope distinction (7 referents in the 24-row original vs 8 in the 44-row re-cut); both counts correct in scope. G3 per-archive states align with my measurements for every artifact I actually held.

**[G5] V3.2-cited artifacts absent from P3.** `drafts/fixed_distance_witness_audit.md` and `scripts/verify_fixed_distance_power_count.py` are not in the delivered archive (nor are the two P5 scripts). Under the matrix protocol this is expected — different archive — but it means the RP-F machine-check is **not deliverable-present**; my verifier below supplies an independent one. The scope matrix itself was received inline only; a verbatim copy is preserved in this snapshot (no original-file hash exists to pin).

---

## 2. RP-F note — independent verification

**[V] `verify_rpf_algebra_v1.py` — 18 checks, fail-closed, `ALL_ASSERTIONS_PASS`, normal and `-O` runs byte-identical.** Machine-checked from scratch:

- **(F.3)** Both Taylor endpoint identities hold exactly as unconstrained polynomial identities (degree-8 generic; polynomial-dense in smooth g), and the pinned forms follow with precisely the note's signs: ∂_{te_i}f(M) = −(1/r)∫(r/2−s)∂_{tte_i}f ds, ∂_{te_i}f(S) = +(1/r)∫(s+r/2)∂_{tte_i}f ds.
- **(F.4)→(F.5)** det H_p carries **exactly one** guaranteed factor r (quotient is a polynomial); D_p has total degree 3 in the normalized entries with leading normalized term a_p·det Q_p.
- **Ledgers.** Three 3×3 determinants → total degree nine; one r per endpoint → r² in (F.6); window width b−h = κr³/6 exact (F.8); power count r²·r³/r² = r³ (F.1).
- **§5 coercivity adjudication — exact.** With L_r = (b−κr³/12, −κ/6, 0, …), |L_r|² = (b,κ)M(a)(b,κ)ᵀ, a = r³/12, det M(a) = 1/36 identically. The historical coefficient **1/2 is false** (witness b = aκ gives ratio 1/(36(1+a²)) ≤ 1/36). Sharper: **1/36 fails for every r > 0** — det(M − I/36) = −a²/36 < 0 puts 1/36 strictly inside the spectrum — so **1/37 is the sharpest safe reciprocal**, and it is safe on 0 < r ≤ 1: det(M − I/37) = (1/37)(1/37 − a²) with a² ≤ 1/144 < 1/37. Spot check: λ_min(a = 1/12) = 149/288 − √19897/288 ≈ 0.027574 > 1/37 ≈ 0.027027.

**[REVIEWED, not machine-checkable]** three prose steps: the §2 uniform eigenfloor (strictly positive lattice Fourier coefficients ⇒ covariance faithfulness on derivative distributions; two-point-support independence; compactness of K × torus × frame atlas × separated region ⇒ uniform spectral window, inherited by the Schur complement); the (F.10) lower bound (after r⁻² normalization the endpoint determinants converge in moments to κ²(det Q)² on the nonempty open cone Q < 0, whose limiting conditional Gaussian law has full support); and type-indicator monotonicity. Each cited ingredient is available (positive Fourier coefficients of the periodized side-24 covariance; the independently verified contact covariance; compactness). The written argument is coherent; I concur at review level.

**Scope discipline confirmed.** The note's own limits are correct and load-bearing for future work: (F.1) is compact-mark only; the global all-typed majorant is a separate interface; and the note's two defect findings against the old global-envelope sketch (conditional vs joint density; the false 1/2) are now machine-confirmed (the second one exactly, above).

---

## 3. Concurrence, qualifications, register

**RP-F (compact positive mark sets): CONCUR with closure as stated.** The algebra is machine-verified (my `verify_rpf_algebra_v1.py`, delivered here; their `verify_fixed_distance_power_count.py`, reported in V3.2 but not delivered); the soft-analysis steps are review-concurred. The all-mark Gaussian envelope explicitly remains open.

**Accepted qualifications on my prior verifier** (from the V3.2 response): [Q1] its Hessians are 2-D contact-plane Hessians — the 3-D transfer belongs to the open finite-r interfaces; [Q2] the O(κ⁶η⁶s⁻⁶) product bound consumes the external type inequality u² < α ≤ 1, which my script does not prove; [Q3] my reconciliation snapshot was not self-contained for the six `-O` reruns or the 8/8 binary audit. This snapshot improves Q3: it carries the `-O` transcript hash list, the bundle-identity evidence, and the pinned P3 hash.

**Updated interface register**

| Interface | Status |
|---|---|
| F2 value-pinned sign mechanism | Independently confirmed twice; **load-bearing** |
| RP-F fixed-distance witness bound (compact marks) | **PROVED — independently audited** (algebra machine-checked; prose steps review-concurred) |
| All-mark Gaussian envelope / global majorant | OPEN (separate domination interface) |
| RP-A/RP-L determinant-weighted capture transfer | **OPEN — next highest value** |
| RP-C finite-r collar compactification | OPEN |
| RP-S finite-r singular-near compactification | OPEN |
| Elder-failure recombination | CONDITIONAL on the above |
| Candidate theorem | **HOLD** |

---

## 4. Run record

Environment: Python 3.12.3, sympy 1.14.0. New artifacts: `verify_rpf_algebra_v1.py` (+ normal and `-O` transcripts, byte-identical); `SCOPE_MATRIX_received_verbatim.csv` (inline-source preservation copy); `optimized_rerun_hashes.txt` (SHA-256 of the six `-O` transcripts from the reconciliation session, each byte-identical to its committed `*_out.txt`); bundle-identity evidence (`bundle_diff` empty; P3 hash pin). Prior-session artifacts unchanged and hash-pinned as P6/P7.
