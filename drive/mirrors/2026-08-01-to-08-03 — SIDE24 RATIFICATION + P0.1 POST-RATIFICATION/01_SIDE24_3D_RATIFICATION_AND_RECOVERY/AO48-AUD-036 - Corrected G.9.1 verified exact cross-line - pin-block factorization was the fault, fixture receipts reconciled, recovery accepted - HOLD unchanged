ARTIFACT ID:       AO48-AUD-036-v1.0

AUTHOR:            Claude Opus 4.8 (Anthropic) — ANTHROPIC-OPUS48 / AO48

CREATED:           2026-08-01

CLASS:             AUD — cross-line verification of the CORRECTED G.9.1 collar
                   determinant (GPT recovery answer, relayed by operator this
                   session), reconciliation of the fixture receipts, and
                   acceptance of the recovery-provenance findings

AUTHORITY:         none

CANONICAL IMPACT:  NONE. RP-C/RP-S, V3.4, and the package HOLD are untouched.
                   This closes the G.9.1 repair loop opened by AO48-AUD-033.

---

# VERDICT

**CORRECTED G.9.1: VERIFIED — EXACT.** The GPT line's corrected closed form

det Γ_d(u) = (aU + bP)(A U² + B UP + C P²) / (b²Δ),
a = (1+d²)q²−1, b = q²−1, Δ = (q²−1)² − d⁴q², A = aN, B = 2bN, C = 2aΔ,
N = −d⁶q² + d⁴q⁴ + d⁴q² + 4d²q⁴ − 4d²q² + 2q⁴ − 4q² + 2,

now agrees with this line's from-scratch 60-digit eight-pin conditioning
pipeline at **every probe, including every probe that refuted the original
form** — worst relative deviation 6.4×10⁻⁴⁸ (at the deep axis point where
det ≈ 4×10⁻¹⁰), all others 10⁻⁵⁶–10⁻⁶⁰. The original AUD-033 verdict
updates accordingly: face-exact stands, f_xx-sector refutation stands
**against the old form only**, and the repair packet is discharged.

# §A. Exact receipts (ℤ[d,q], fraction-free, this session)

E1. det Σₓ = Δ = (q²−1)² − d⁴q² — **TRUE**, via the same machinery that
    refuted the old factorization. The parity-block route (B₊, B₋ with
    determinants 1−q²±d²q) is confirmed by direct 4×4 expansion.
E2. F₁F₂ = Δ + 4d²q² — **TRUE**: the historical det Σₓ = (q+1)²F₁F₂ was
    wrong by exactly the additive term 4d²q², now identified.
E3. 3·det Σₓ − c_xxᵀ adj(Σₓ) c_xx = N — **TRUE**: hence
    Var(f_xx | pins) = N/Δ exactly, and the Hermite coupling
    c_xx = (−1, (d²−1)q, 0, (3d−d³)q) — the vector my repair packet told
    the authoring line to recheck — was **correct all along**. The fault
    was one level down, in the pin-block determinant.
E4–E6. The displayed expansions of A, B, C equal aN, 2bN, 2aΔ — **TRUE**.

# §B. Numeric agreement and stratification

Seven probes, corrected form vs pipeline: d=0.1 axis 6.4e−48; d=0.5
(0.3,0.7,−0.2) 1.3e−56; d=1 (1,1,1) 2.6e−58; d=1 face 9.6e−60; d=2
(0.9,0.1,0.4) 2.1e−60; d=4 near-axis 1.5e−59; d=4 face 4.0e−60. GPT's
100-digit probe values match the pipeline to its full ~50–60-digit
accuracy; the face value agrees to every printed digit. Stratification
series confirmed numerically: axis det/(d⁸/24 − d¹⁰/45 + d¹²/216) =
1 − 9.4×10⁻¹¹ at d=0.05 (**Θ(d⁸)**, coefficient 1/24 exact); generic
det/(d²P³ + d⁴(5UP²/6 − P³/6) + d⁶(U²P/4 − 7UP²/30)) = 1 − 7.9×10⁻¹⁵ at
d=0.01 (**Θ(d²)**, and the d²P³ leading term retro-explains AUD-033's
measured 0.444 coefficient exactly: P³/(4U₁U⊥²) at (0.6,0.8,0)).

# §C. The error, now stated precisely for the record

The false identity was the **pin-block factorization**, not the f_xx
coupling. Downstream, the wrong "conflicting series" branch was the one
that **collapsed the two pin stations before taking the Schur limit**,
identifying lim_{d→0} Cov(· | eight two-point pins) with
Cov(· | coalesced confluent pins) — losing the divided-difference
constraints that pin the axial Hessian row, hence the impossible generic
O(1)/det-4 prediction. Important scope note: the coalesced law itself
(diag(2,1,2), det 4, verified by hand in AUD-033 §1c) **remains true as
its own statement**; the error was the identification of the two limits.
This is the same confluent-pinning mechanism as AO48-DER-031 §1 in 2D,
and it is now exact in 3D on both sides of the correspondence.

# §D. Receipt correction — owned against this line

GPT's audit of my fixtures is **accepted**. AUD-033's original acceptance
strings for the d=0.1 axis and d=2 probes (…5546260781e−10;
0.97602439986282218 46) were double-precision-grade despite the ≥40-digit
label — high by 1.8×10⁻²⁵ and 1.8×10⁻¹⁶ respectively. **Logged as AO48
error #11: precision mislabeling** (printing float-contaminated values
under a high-precision claim). AO48-REC-034's fresh re-execution already
superseded both with correct values, now confirmed to 100 digits by the
independent line. The d=1 and face fixtures were correct as printed.
Reusable control: fixture tables must state the computation path
(Decimal/mpmath precision) per row, and no float-path value may carry a
digit-count label exceeding 15.

# §E. Provenance acceptances

1. **C030/C031 relay folder (1RvyeNGThLxtGsnTLREhgw_bT5NEpw8XM) is
   SECONDARY.** Controlling primary: `q0_machine.json`,
   `1PXa-ZqCrICicUUDIy37PafHgdjCOb3cj` (5,910,703 bytes, 2026-07-20,
   SHA c3a93bd2…), which hydrates the four small JSONs byte-exactly and
   preserves the observed-update objects semantically; four Markdown
   standalone primaries also predate the relay. The relay retains value
   as the earliest standalone raw carriers. AO48-REC-035 §A stands with
   this lineage note attached.
2. **The missing-carrier list is closed.** R2 recovered byte-exact
   (14eaf7…, 68/68 internal hashes); V5 supplement + v8 verifier
   recovered; LS-DER-053/065 located (native primaries + hashed
   exports); C022–C029 controlling carriers enumerated with hashes;
   V3.0–V3.4 checkpoints preserved. Per the recovery record: zero known
   high-value cited exact carriers remain local-only.
3. **Audit lane (iv) is UNBLOCKED.** The V3.4 replay folder
   (`1gsgGh7Pp1Y5NBzql5Z23Vg8PdqSlQtVI`) carries all seven requested
   files with hashes matching the check-results report (the hybrid
   verifier's 0ba99085… confirmed), the complete V3.4 ZIP (ba2a296b…),
   and a pinned environment (CPython 3.12.13; sympy 1.14.0, mpmath
   1.3.0, numpy 2.4.4, scipy 1.17.1). Execution replay is now a
   routable task for any execution-capable line; this line's sandbox has
   no network for pip and does not claim it.

# §F. Standing

G.9.1-corrected is now **cross-line verified exact** (GPT derivation ⊕
AO48 independent pipeline ⊕ six exact polynomial identities) and
supersedes the refuted original as the collar-template object. It does
not, by itself or with this verification, advance RP-C/RP-S, the V3.4
facewise closure, or any promotion gate — **HOLD unchanged**, exactly as
both lines state. The REC-035 audit lanes stand, with (iv) unblocked and
this artifact discharging the G.9.1 corner of lane (i).

---

*Cross-references: AO48-AUD-033 (refutation + fixtures) · AO48-REC-034
§1 Core C (superseding fixtures) · AO48-REC-035 (audit lanes) ·
AO48-DER-031 §1 (2D mechanism) · GPT recovery answer (operator relay,
2026-08-01): recovery folder 1ZO2wKp6IbYMzCzcvWSob_TrD2dHBhj-H,
reconstruction note 19Vwfm7SBk-4V_RIwMBxXepjcw6NUoyMy (SHA 46b6af0c…),
26/26 verifier 18nGSc8G3DhKCtuk86VVILGQyYNTLlw7- (SHA eb561bad…) ·
`_RULES_OF_ENGAGEMENT` Rules 1, 3, 7*
