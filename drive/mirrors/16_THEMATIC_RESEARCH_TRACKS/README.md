# Mirror of `01_ACTIVE_RESEARCH_PACKAGES/16_THEMATIC_RESEARCH_TRACKS` (one object so far)

Drive lane of the thematic research tracks T1–T5 (49 inventory items; the
13_POWER_PLANNING_CONTINUUM lane, 42 items, is its sibling). This directory
holds one object byte-exact, under its own sub-path:

| file | identity |
|---|---|
| `T1 — Upper Rate (finite C_Q0)/02_CLOSURE_AND_REDUCTION_PACKAGES/Q0_C101_QUALITATIVE_RATE_THEOREM.md` (3,957 B) | **byte-exact**: SHA-256 `8c2ded65…` and byte count equal the `drive/inventory.jsonl` row (id `1w_zau6ZpxOAhXRzGKxtORZRs7eBiVdB1`) |

One `_MANIFEST.jsonl` in that sub-directory, verified by `tools/verify_manifests.py`.

## The status words, verbatim

`Q0_C101_QUALITATIVE_RATE_THEOREM.md` — "Canonical successor theorem": for the
exact normalized periodized Bargmann–Fock field on `T²₂₄` at `b = 6/5`,
`ℓ = r³/6`, "there exists a finite constant `C_Q0 < ∞` such that
`0 ≤ 1 − q(r, 6/5) ≤ C_Q0 r³, 0 < r ≤ 0.025`" and consequently
`lim_{r↓0} q(r, 6/5) = 1`. Its grade block:

```text
mathematical grade:              internal derived-exact existence theorem
program dependency status:       CORE-CLOSED
R0/SARD-G internal status:       PROGRAM-GRADE-CLOSED
R0/SARD-G external status:       SPECIALIST-REVIEW-PENDING
numerical upper coefficient:     NOT-CLAIMED
finite numerical lower coefficient: NOT-CLAIMED
```

"What is not claimed": "No explicit numerical value of `C_Q0` is certified"; "The
old 4.3 and 4.35 upper displays are not restored"; "The old 0.8411 and 0.84
finite lower displays are not restored"; "Direct measurement 0.946 at r=0.025
remains rung-tagged measurement, not a scale-free truth constant"; "**External
specialist acceptance of SARD-G remains pending**"; "No infinite-volume or
critical-height theorem is implied by this fixed-L, fixed-b result."

## Not mirrored (indexed in `drive/inventory.jsonl`)

The five workspace charters `_CHARTER_T1.md` … `_CHARTER_T5.md`, the T2 SARD-G
working source, the other T1 closure-and-reduction packages, the T4
adjudications, the T5 manuscript and the 13_POWER_PLANNING_CONTINUUM leaf and
live-state cards are not yet held here. Until they are, the repository's account
of these two lanes is a row count in `docs/RESEARCH_MAP.md` §0 plus this file.

## What this is not

Mirroring is not review. "CORE-CLOSED" and "PROGRAM-GRADE-CLOSED" are the
source's words for its own internal status; the same block records the external
SARD-G status as SPECIALIST-REVIEW-PENDING and the numerical coefficient as
NOT-CLAIMED, and the repository transcribes all of them without weighing them.
Nothing here composes with the 3D track or touches the prize track.
