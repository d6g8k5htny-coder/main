# `02.1_MACHINE_ROOT_SCHEMA/05_SECOND_DOMAIN_CONTRACTS`

Drive folder id `1V5YufFARsw2V0soA78kt2Nc53GHWSB9w`. The 2026-09-17 inventory gives this
folder **1 item**, and it is held byte-exact.

## What is held, and at what exactness

| stored file | exactness | bytes |
|---|---|---:|
| `C095_SECOND_DOMAIN_CONTRACT.json` | byte-exact | 11,742 |

## The status banners, verbatim

The file records its own domain as

> finite-dimensional numerical linear algebra

its theorem as

> If A is invertible and ||A^{-1}E||_2<=rho<=0.8, then A+E is invertible and ||(A+E)^{-1}||_2<=5||A^{-1}||_2.

its grade as `PROVEN`, and its purpose as

> Demonstrates that Gate Framework v1.1 and verifier v5 operate outside the Q0 Gaussian-field domain without weakening gate semantics.

Its own gate report records `valid` and `promotable` true for the single root
`MATRIX_PERTURBATION_THEOREM`.

## What this does not establish

This object is a deliberate control, and the control's purpose is stated by the source:
to exercise the framework on a problem outside the Q0 domain. That a gate framework passes
a standard matrix-perturbation statement says something about the framework's plumbing and
**nothing about Q0**. The grade `PROVEN` and the flags `valid` and `promotable` are this
file's own records of its own run; nothing was executed here, no gate was re-run, and this
repository neither checks nor adopts the verdict. The statement's mathematical content is
not at issue and is not certified here either: it is stored as bytes, not as a theorem this
repository asserts.
