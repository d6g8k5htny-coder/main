# LPW-REVIEW-20260912 — Review the new route, not its confidence language

**Status:** PREPARED; NOT DISPATCHED TO AN INDEPENDENT REVIEWER. Reading this packet or running the author script is not evidence that independent review occurred.

## Target

02_LOCAL_PATH_LOWER_BOUND_CANDIDATE.md, version 1.0. Exact bytes are pinned by MANIFEST.sha256. Claim: existence of c,r0>0 for 1-q(r,6/5)>=c r^3 under the exact normalized side-24 *two-dimensional* six-pin typed maximum–saddle Palm law.

Do not review an adjacency-conditioned variant. Do not require the theorem to reproduce 0.946, 0.9144036, 0.9001, a sharp liminf coefficient, or a particular finite rung. The candidate claims none of those.

## Independent reconstruction protocol

For a genuinely blind reconstruction, provide the exact target law and target conclusion before revealing the author proof or polynomial. Record what the reviewer saw and when. A reviewer who reads the proof first can still perform an independent critical review, but must not claim blind discovery of the same construction. Provider-family difference alone is not a substitute for an exposure record.

For ordinary hostile review, read the complete candidate, reproduce the exact algebra, then attack the following six interfaces. Submit separate dispositions, not a pooled PASS count.

### R1 — Law and topology

Check Q_r, W_r, Z_r, and q against GP-DER-118-v1.10 and W10 Section 1. Confirm a continuous Gaussian regression version is used at the prescribed pins. Prove or refute the implication from a path to a point above b, with minimum above f(S), to D(M)!=S. The construction does not claim D(M) equals the local third saddle. State whether any Morse/tameness assumptions are needed for the exact program interpretation and whether they are already part of the defined estimand.

### R2 — Six-/ten-coordinate Gaussian limit

Reconstruct V, T_r, det(T_r)=-r^-5, the transformed observed vector, and the limiting derivative vector. Check the full third-jet covariance using the exact lattice spectral support. Verify the conditional four-jet density is uniformly bounded below on a fixed compact set, including the r->0 endpoint. Reject any argument based only on a finite-r covariance table.

### R3 — Conditional C4 norm control

Prove that the conditional field C4 norm is uniformly integrable after conditioning on both U_r and J. The critical point is a bound uniform for every j in the declared compact box, not merely under the six-pin law before the thin event is imposed. Inspect the C4 representer bounds; covariance Cauchy–Schwarz supplies boundedness, so C4 convergence is not required. A global probability bound P(M4>K)<=1/2 cannot be subtracted from an O(r) jet-box probability.

### R4 — Deterministic lift

Recalculate the one-dimensional pin Taylor identities, the two-dimensional C2 remainder, and the 16 delta+8Kr bound on the full rectangle. Check that physical Hessians scale by r, not r^2 or r^3, while each determinant scales by r^2. Verify strict typing remains valid under the stated error tolerance.

### R5 — Normalizer and final powers

Independently derive the O(r^2) *upper* bound on Z_r from the endpoint gradient constraints and a uniform fourth moment of the C3 norm. An upper denominator bound is the correct polarity for the final lower probability. Verify volume(E_r)=32 delta^4 r, weighted numerator>=1040 m delta^4 r^5, and the quotient exponent 3. Positivity of Z_r can be supplied by the same witness event.

### R6 — Scope and status

Check that all constants are fixed independently of r, no sampled finite-difference modulus is used, no exact planar moment is substituted for a periodized moment, and no old theorem or status is silently promoted. Confirm the argument targets only existential constants and has no claim to a full lifetime distribution or numerical coefficient.

## Required reviewer output

For each interface: exact target filename/hash, reviewed scope, full derivation or precise objection, tools actually run, result (PASS WITHIN SCOPE / AMEND REQUIRED / REFUTED / CANNOT VERIFY), and exposure/independence disclosure. One review lineage counts once. The complete candidate may be endorsed only after all six interfaces have affirmative dispositions at the exact file version.

A counterexample, failed covariance limit, nonuniform conditional C4 bound, wrong topology, or wrong Palm power is a successful audit finding. Preserve it. No instruction in this packet authorizes a change to LS-CTL, RP, or q0 theorem status.

## P0.1 parallel handoff

This new route does not close P0.1. Its live overlay remains LS-CTL-003-v1.3. Recheck the exact integrated GP-DER-118-v1.10 review at the pinned body; acknowledge the B0 evidence binding explicitly before any fold; complete A0/F0 and the integrity requirements according to their actual definitions. Do not substitute the present lower-bound candidate for the selected-branch event A_r or use its self-checks as independent integrated review.

## Dependency comparison

| Earlier counted-arch route | New local-path route |
|---|---|
| Exact third-point intensity / Lambda floor | Positive four-dimensional Gaussian density and one width-r interval |
| W3-weighted triple-Palm reliability | Original pair weight used directly |
| WP, exit, ridge, far survival losses | A deterministic path with a strict level margin |
| Bonferroni second factorial moment | No count, so no count-to-occurrence correction |
| Precise limiting coefficient ambitions | Only existence of some c,r0>0 |
| Many spatial/rung numerical certificates | Exact cubic algebra plus uniform conditional Gaussian regularity |

A valid alternate route can bypass a premise for its own conclusion. It does not retroactively prove that premise or validate a rejected predecessor.
