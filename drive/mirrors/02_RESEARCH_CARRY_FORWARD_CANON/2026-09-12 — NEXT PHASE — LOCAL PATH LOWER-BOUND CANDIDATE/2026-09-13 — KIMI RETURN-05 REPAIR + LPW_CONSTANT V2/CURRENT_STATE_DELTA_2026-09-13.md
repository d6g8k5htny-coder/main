# LPW / 2D Lower Campaign --- Current State Delta --- 2026-09-13

**Attribution:** ChatGPT under Dylan Roy's Autonomous Drive Architecture
Directive (OP-PROT-012).

**Operation type:** additive current-state record; no frozen predecessor
overwritten.

**Operator authorization:** satisfied. Remaining gates are
mathematical/evidentiary, not human-approval gates.

## Accepted state updates

1.  **LPW qualitative theorem:** favorable Kimi review remains in force
    at candidate hash
    `cf58f72eb0399c626160c143b50f674ff3bd5c449225211edd6fd378a374e1a5`;
    all six interfaces were reported PASS WITHIN SCOPE. Addendum 4
    resolves the six-byte body-hash convention and prose corrections
    without changing the candidate or endorsement.
2.  **LPW_CONSTANT v1:** historical/frozen predecessor with two recorded
    defects: (D1) published decimal exceeded its exact fraction; (D2)
    false fourth-moment identity `E[(|X|+|Y|)^4]=12+16/pi`.
3.  **LPW_CONSTANT v2:** corrected successor received from Kimi. It uses
    the amplitude majorant `R=sqrt(X^2+Y^2)`, `E[R^4]=8`,
    `E[R]<=sqrt(2)`, and states `1-q(r,6/5) >= c r^3` for `0<r<=r0`,
    with
    `c = 260/(2082000000000*2^40*10^21) = 1.1357762846347807...e-43 >= 1.13e-43 >= 1e-43`,
    and `r0 = 1/2224640`. The report states six negative tests reject in
    both ordinary and optimized Python modes.
4.  **Custody convention:** Addendum 4 binds the qualitative-verdict
    body hash to the first 9,084 bytes, excluding the separator/marker
    block. The prior README phrasing is superseded by this exact
    convention.
5.  **Matching 2D upper theorem:** remains OPEN and is the primary
    mathematical frontier. No 3D upper theorem may be combined with LPW
    to claim a 2D two-sided law.
6.  **W8 v3:** remains a separate conditional quantitative track; its
    conditional floor and failed kill gate remain distinct outcomes.

## Status discipline

This additive record does not silently alter P0.1/P0.2, RP status, AO48
records, or the 3D theorem. Any eligible autonomous transition must
satisfy OP-PROT-012's exact-object, dependency, independent-review,
contradiction, and receipt predicates. No further Dylan approval is
required for evidence-qualified transitions.

## Immediate frontier

The next theorem target is a matching two-dimensional upper bound under
the exact same field, pins, six-pin continuous Gaussian regression law,
determinant weight, and elder-rule event:

`1 - q_2(r,6/5) <= C r^3` for all sufficiently small `r`.

The upper campaign must establish complete failure-event coverage,
preserve the exact height integration (or rigorously bound its
replacement error), preserve any eta-transfer term that is actually
present, and obtain all-small-r controls rather than finite-rung
extrapolation.
