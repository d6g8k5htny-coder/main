# Kimi Next Work Order --- Matching 2D Upper Theorem

## Governing target

Prove, under exactly the same 2D side-24 six-pin typed pair-Palm law as
LPW,

`1 - q_2(r,6/5) <= C r^3`

for every sufficiently small `r`.

## Required sequence

1.  **Freeze the exact estimand and law.** State pins, regression
    version, determinant weight, normalizer, elder-rule event, and the
    exact failure event `D_f(M_r) != S_r`. Bind exact source identities.
2.  **Failure-event coverage theorem.** Produce an exhaustive
    decomposition of pairing failure. A sufficient subset event, such as
    LPW's local path event, cannot prove an upper bound. Every failure
    mode must enter the cover or be proved impossible.
3.  **Audit the C020/C021 historical route without promoting it.** Treat
    those carriers as superseded provenance. Re-derive any needed
    Kac--Rice intensity under the current exact law. Do not call
    representative-height substitution exact unless equality is proved.
4.  **Retain the height integral.** For any third-point or witness
    intensity, keep the exact integral over the height window, or give a
    uniform signed/error-controlled replacement bound whose direction is
    sufficient for an upper theorem.
5.  **Preserve transfer terms.** If an adjacency/exit transfer is used,
    keep `eta_r` (or its current exact analogue) unless a separate
    theorem proves it vanishes or is bounded at the needed order.
6.  **All-small-r control.** Replace finite-rung fits by
    analytic/interval bounds on the entire interval `(0,r0]`. Separate
    endpoint limits, compactness arguments, and explicit moduli.
7.  **Multiplicity/Bonferroni accounting.** If a count is converted to
    an occurrence probability, state exactly which direction is needed
    and control second factorial moments where required. Do not reuse
    lower-bound Bonferroni logic in the wrong direction.
8.  **Normalizer polarity.** Check every use of `Z_r`: upper and lower
    estimates must be in the direction required by the upper probability
    bound.
9.  **Adversarial decomposition audit.** Try to construct a
    pairing-failure field outside the proposed cover. Any successful
    witness reopens the coverage theorem immediately.
10. **Independent verification.** Freeze the candidate before
    comparison. Review the exact proof with at least one qualifying
    independent path under OP-PROT-012, including source/scope review
    and negative tests.

## Explicit prohibitions

-   No 3D-to-2D composition.
-   No silent adjacency conditioning.
-   No representative-height equality from small sensitivity.
-   No numerical zero count as proof of `o(1)`.
-   No sampled-rung fit as an all-small-r theorem.
-   No status promotion merely because multiple agents agree.

## Parallel housekeeping

Return a small additive LPW_CONSTANT-v2 custody receipt containing exact
file hashes, script/report identities, both-mode transcript hashes, and
the six mutation receipts. Preserve v1 unchanged as historical
predecessor.
