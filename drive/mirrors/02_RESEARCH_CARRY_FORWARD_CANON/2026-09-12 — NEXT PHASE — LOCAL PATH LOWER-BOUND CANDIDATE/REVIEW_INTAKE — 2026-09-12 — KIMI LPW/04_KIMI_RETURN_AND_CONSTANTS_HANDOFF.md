# Return to Kimi — scoped corrections and raw-review completion

**Target:** LPW-CAND-20260912 v1.0, exact 22614-byte file, SHA cf58f72eb0399c626160c143b50f674ff3bd5c449225211edd6fd378a374e1a5.

Your APPROVE and six interface PASS dispositions were received and preserved. The target hash matches the original ZIP and fresh Drive retrieval. The author candidate is unchanged. The following requests separate the successful proof review from unsupported consequence and numerical claims.

## A. Return the actual review evidence

Export individually, without overwriting old carriers:

- KIMI_LPW_REVIEW_VERDICT.md with the exact body extraction rule and complete hash;
- LEAD_EVIDENCE_PACK.md;
- RA_R1_R6.md, RB_R2.md, RC_R3.md, RD_R4_R5.md with full hashes;
- their independently authored scripts, exact inputs, stdout/stderr, exit records, normal/optimized runs where claimed, and environment;
- eigenvalue/density certificate source and data, spectral truncation geometry and error bounds;
- one manifest covering the final delivered tree and the exposure/commit records needed for independence classification.

The supplied PDFs lose visible mathematical glyphs. Preserve the original PDFs as transport artifacts; do not infer their hashes equal those of the Markdown. A repair PDF may be generated as a separate view once raw math is stable.

## B. Correct the two-sided consequence

AO48-OPR-045 is a d=3 theorem and explicitly excludes cross-track inference. The LPW lower result is d=2. Withdraw the statement that those two objects by themselves give a two-sided Theta(r^3) law. Either name a separately reviewed d=2 upper theorem at the identical law/event/pins, with exact source/hash and applicable radius, or retain the lower result alone.

The safe lower-only consequence is positive liminf: liminf (1-q_2)/r^3 >= c>0. It does not determine a sharp or limiting coefficient.

## C. Independence terminology

Record one Kimi-family review package with five reported working lines. Preserve the disclosed full-proof exposure: ordinary hostile review, not blind reconstruction. Explain any claimed method/source independence using actual inputs and code ancestry. Do not convert five agents into five organizational or statistically independent confirmations.

## D. Numerical corrections

The pasted relay's proposed lower floor 0.1265534497 exceeds the quoted eigenvalue 0.126553449667289. Return a directionally correct certificate. The attached author companion gives an exact rational planar bracket plus an analytic periodic perturbation and supports the safer endpoint floor 0.1265534496 for the exact coordinate normalization. Audit that companion rather than treating it as already independently certified.

Use exact periodic moments. The correct endpoint Schur diagonal is

    (mu4-mu2^2,
     mu2*(mu4-mu2^2)/4,
     mu2*(mu4-mu2^2)/4,
     (mu6-mu4^2/mu2)/36),

and the conditional mean is (-b*mu2,0,0,0). The planar rational values are a specialization, not exact finite-side identities. A spectral cutoff must state whether it bounds lattice indices or physical wavenumbers.

A tiny residual for one eigenpair is not, by itself, a lower bound for every eigenvalue. Bound matrix-entry/truncation error and the entire spectral minimum using a rigorous method.

## E. A1/A2 and the missing source

The author has supplied an additive triangle-inequality proof for the nonstationary residual and made M4_top versus the full C4 norm explicit. Review the appendix with a separate conversion disposition; do not silently transfer your old approval to a new file.

The exact GP-DER-118-v1.10 raw source is supplied. Whole-file c1d6e559274d7e87a3fa92f44cb2534a5e277a3b5941fd40db380599ddba0564; extracted body 9b7901e112a4857e3ea59942858684f72fc09a2825b1efa859360dd8fa55f014. Confirm law/pin/weight equality with LPW. Do not equate GP-DER-118's selected-branch adjacency event A_r with LPW's elder defect event.

## F. K3 identity

GP-LB-STAT-004 explicitly targets K3-THM-001 at SHA 715124dba1f089430594a3322b7d32c3508fbcb10c14f6591a432ff582f8854d. A corrected H1-H12 assembly needs its own exact carrier and a supersession/correction crosswalk. Do not relabel that exact rejection as merely a historical THM-023 v1.1 issue.

## G. Safe explicit-constant work order

The following is a separate quantitative task; it does not reopen the existential proof merely because a useful numerical constant is difficult to obtain.

Choose a proposed rational r_G and bound the actual ten-coordinate Gamma_r uniformly on [0,r_G], not just Gamma_0 or finitely many rungs. Bound regression means and Schur complements under the exact periodic law.

Declare the exact density domain. Either use the original [0,r_G] x K_J, or explicitly amend to the actual moving thin boxes on [0,r0]. For any four-dimensional conditional covariance with eigenvalues in [a,A], and a proven bound ||j-mu_r||<=R on that domain,

    density >= (2*pi)^(-2) A^(-2) exp(-R^2/(2a)).

Enclose each quantity in the safe direction. For this formula, use an upper A and R, a positive lower a, and a certified lower exponential evaluation. More direct interval density optimization is also acceptable. No center/corner sample at one r establishes the all-r bound.

Prove B3_upper >= sup E_Qr[max(1,||f||C3)^4] and B4_upper >= sup E[||f||C4 | U_r=u_r,J=j] on the declared domains. Use the nonstationary regression representation and an explicit Fourier majorant, not a stationarity theorem for the residual. A smaller density box and a larger conditional-moment box may coexist if both domains are stated and the containment is proved.

With delta=1/1024, set

    K_upper=max(1,2*B4_upper),
    r0=min(r_G,1,1/(256*K_upper)),
    c_lower=260*m_lower*delta^4/B3_upper.

Avoid circularity by proving bounds on a preliminary r_G domain before selecting the smaller r0. Certify c_lower>0 and r0>0 as exact rationals or outward-rounded intervals. Store very small constants as rational/scientific/log enclosures rather than allowing floating underflow to zero.

Return separate objects for endpoint covariance, all-r covariance, density floor, B3, B4, r0, and c. All products/inequalities must carry the correct direction. Include mutations for wrong covariance dimension, omitted normalization, q/(2r) Jacobian, upper-versus-lower rounding, and center-density substitution.

## H. W3/W8 remain separate

The relay reports W8 test ratio 3.56 outside [0.4,2.5]. Export that failed receipt and retain it before any 1/80-reference repair. Give the repair a new version, its own full interval scope, and new tests; changing the reference rung does not by itself prove H-B3 or 0.9001. No W3/W8 outcome is a premise of the local-path construction.

## Completion boundary

Return exact artifacts and explicit per-item dispositions. No instruction here authorizes an LS-CTL, RP, theorem-status, or release change. No external Kimi job has been dispatched by this ChatGPT session; this document is the handoff for the user's existing relay workflow.
