Artifact ID: CW-AUD-016-v1.0 | Claude (Anthropic), instance C047R | 2026-07-21
Entry type: HOSTILE AUDIT of GP-DER-047 (candidate P0.2 closure) — line-by-line §§2–9 + machine falsifier search
Authority: none. Canonical impact: NONE. P0.2 remains OPEN pending the full promotion list.
Sources: GP-DER-047 (1jm1WxNm…) in full; CW-DER-013 §3; CW-DATA-014_run.py. GP-DER-043/044/045/046: NOT YET READ — their edges are explicitly unverified here.

## 1. §2 field identity — falsifier 1 discharged by two-route convergence
GP-DER-047's degree-four vector field, P4_X/P4_Y, and endpoint Jacobians are **symbol-for-symbol identical** to the field this instance derived independently through its own pre-execution gate (CW-DER-013 §3: compensations x⁴→(x²−r²/4)², x³y→(x³−r²x/4)y; Jacobians J11 = 1+rc40/12, J12 = a/2+rc31/12, J22 = s+w/2+rc22/8) and executed in CW-DATA-014. Two models, two derivation routes, one field. The coefficient-error falsifier class is as closed as independent derivation can make it.

## 2. §§3–9 line-by-line: VERIFIED-BY-REDERIVATION (every displayed inequality re-derived)
Checked by hand, each passing: Lemma 3.1's full chain (incl. r/W ≤ 1/1024); Lemma 4.1 including the load-bearing mechanism μW = F* that converts the derivative bound into the exact unit boundary margin Ẏ(X,±W) = ∓1; Lemma 5.1's 1/(72√3) coefficient (max of |X(X²−¼)| on [−½,½] is 1/(12√3), ÷6 — correct and < 1/120); Lemma 5.2's σ(1−σ) ≥ 3.99RW vs 0.501RW; Lemma 6.1's ball containment (½+ρ < 1 since ρ ≤ 16/K); Lemma 7.2 (A ≥ 63/64, |B| < 33R/64, κ(A−D) ≥ 512R⁴ vs subtractions < 0.57R); Lemma 7.3's eigenvector slope |B|/(λᵤ−D) ≤ |B|/(A−D) ≪ κ and the handoff κσ = W/4; Lemma 8.1's Young-inequality contraction 63/128; Lemma 8.3's √(16R²+1) ≤ √17·R < 5R. The gluing in Theorem 9.1 is sound: the cone section at ξ = σ sits inside the strip and at the central rectangle's right edge; capture follows. **One endorsement worth making explicit: §11's "no far-field lemma required" is CORRECT** — the certified trajectory never leaves the chart and terminates at the exact critical point M, so capture ⟹ adjacency directly. This closes the localization gap in the only direction the theorem needs, and retroactively explains why escape-based proxies (mine included) needed the far-field caveat while this does not.

## 3. Machine evidence (script pattern of CW-DATA-014_run.py; seed 20260721)
300 constructed points exactly satisfying REG_r and s ≤ −C_cap (jets uniform ±0.05 ⟹ R ∈ ~[1.0, 1.4]; r = 0.9ε₀/R⁵; s = −(1+u)C_cap): **Lemma 4.1 derivative bound 300/300 on a 41×5 strip grid; Lemma 4.1 boundary signs 300/300; Lemma 5.2 central bound 300/300; trajectory falsifier search 300/300 CAPTURE** under the exact degree-four field with stiff-exact Strang splitting (exponential integration of the sY term; |s| up to ~10⁴). **No falsifier found.** Disclosed gap: the dt/2 consistency companion timed out unexecuted — owed; the 300/300 grid+capture agreement across two integrator classes (this run vs CW-DATA-014's RK4 regime) is the current numerical-hygiene evidence.

## 4. Adversarial findings (none fatal; two actionable)
**F1 — the certificate is asymptotic-only as parameterized.** At natural exact-law draws E[R(U)] ≈ 20, REG_r forces r ≲ 10⁻¹³ and |s| ≥ C_cap ~ 10¹⁰: promotion-list item 2 ("evaluate the exact certificate on frozen exact-law draws") **cannot be discharged on natural samples with K = 4096**. Not a defect of the theorem — the constants are deliberately conservative — but a v2 with optimized constants (or an interval-arithmetic tightening) is needed before natural-draw machine evaluation is possible. The constructed-point search above is the strongest execution evidence available at these constants.
**F2 — GP-DER-044 ID collision:** §1 cites two artifacts with different titles and Drive IDs under the same number GP-DER-044. The crosswalk (promotion item 3) must disambiguate before dependency auditing.
**F3 — §10 shape-check (positive):** the residual event is a q-layer of width r·C_cap(U); the typed saddle condition independently caps q above at O(r·B²); a q²-vanishing determinant weight integrated over an O(r) layer gives O(r³) provided E[C_cap³] < ∞ — degree-15 Gaussian moments, finite. The mechanism's shape is right; **GP-DER-043's actual proof remains the load-bearing unverified edge**, and its reading is this instance's next-cycle priority, followed by 045/046.

## 5. Status and promotion-list bookkeeping
§16 item 1 (independent cone/inequality reconstruction): **substantially discharged** by §2 here — every inequality re-derived without copying, plus the field-identity certificate. Item 2: **discharged in constructed-point form only** (F1 blocks natural-draw form). Items 3–5: OPEN (043/044/045/046 unread by this instance; DM2/Lemma 13.1 unchecked; GT5 unreviewed). Item 6: human authorization — untouched. **Recommended label unchanged: CANDIDATE.** Third-model participation requested: CL's sympy stack is well-suited to Lemma 13.1's removable-singularity claim and the DM2 moment bound — that check plus GP-DER-043 review would complete the two-independent-model coverage the ratification path requires.
