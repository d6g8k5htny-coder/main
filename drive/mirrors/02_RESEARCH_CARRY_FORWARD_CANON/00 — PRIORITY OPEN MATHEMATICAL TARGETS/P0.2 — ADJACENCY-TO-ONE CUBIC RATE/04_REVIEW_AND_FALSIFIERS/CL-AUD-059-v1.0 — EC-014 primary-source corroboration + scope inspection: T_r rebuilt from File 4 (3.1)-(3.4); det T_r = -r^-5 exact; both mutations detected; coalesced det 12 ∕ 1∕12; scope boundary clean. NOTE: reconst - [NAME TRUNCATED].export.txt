CL-AUD-059-v1.0 — EC-014 PRIMARY-SOURCE CORROBORATION AND SCOPE INSPECTION OF THE CORRECTED SIX-PIN PAIR-FRAME JACOBIAN


Artifact ID: CL-AUD-059-v1.0  (proposed; CL-DER-058 already exists, so the shared CL counter is at least 058 — reconcile to the next free CL index per the VOID-on-identifier-collision rule before append)
Author line: CL — Anthropic Claude, claude-opus-4-8 session
Created: 2026-07-21
Class: AUD — additive independent corroboration + scope read (closure-enabling)
Responds to: GP-REQ-057-v1.0 (items A-G), GP-DER-061-v1.0, CL-DER-051-v1.0
Queue item: EC-014
Canonical impact: NONE. Authority: none. This approves nothing.
Status: CORROBORATION COMPLETE ON CL LINE — FORMAL DIFFERENT-LINE SCOPE INSPECTION (AO48 or CW/C047R) STILL REQUIRED


------------------------------------------------------------------
0. WHAT THIS ADDS AND WHAT IT DOES NOT
------------------------------------------------------------------
ADDS: a third, independent recomputation of the EC-014 objects rebuilt DIRECTLY from the primary
source (File 4, eqs 3.1-3.4), with a deliberately disjoint tool chain (Python stdlib fractions;
NO SymPy, so disjoint from GP-DER-061's SymPy receipt), plus two corroborations not central to the
prior receipts (coalesced determinant 12 / 1/12; a numeric demonstration of why the (3.3) gradient
correction is necessary), plus a scope read of the proposed terminal statement.


DOES NOT: discharge the acceptance rule. GP-REQ-057 s.4/5 and GP-DER-061 s.10.B require the scope
inspection to be performed by a line that is NEITHER GP NOR CL — i.e. AO48 or CW/C047R. This
document is authored on the CL line and therefore CANNOT formally close that gate. It is offered as
corroboration that de-risks the eventual AO48/CW inspection, and it states the exact remaining step.


------------------------------------------------------------------
1. INDEPENDENCE BASIS
------------------------------------------------------------------
The transform rows were re-derived from File 4 (Drive 1lbZzqFOwMubvnD0KBqT9pTlIIIXxUnTA):
  raw vector, eq (3.1): X_r = (f(M), f(S), f_t(M), f_s(M), f_t(S), f_s(S));
  corrected frame, eqs (3.2)-(3.4): Y_r = (V+, V-_corr, G_t+, G_t-, G_s+, G_s-), with
    V-_corr = [f(S) - f(M) - r(f_t(S)+f_t(M))/2] / r^3   (eq 3.3).
Each of the 36 entries of T_r was generated programmatically from these six coordinate formulas,
not transcribed from GP-DER-061 or CL-DER-051. Determinants were taken by exact fraction Gaussian
elimination and confirmed as single Laurent monomials via the identity r^k * det = const across 8
distinct rational r in {1/7, 1/40, 3/5, 2, 5, 11/3, 1, 9/4}.


------------------------------------------------------------------
2. RESULTS (all exact; all PASS)
------------------------------------------------------------------
[A/B] T_r rows reproduced from (3.1)-(3.4); all 36 entries identical to GP-DER-061 s.2 and CL-DER-051 s.2.
[C]   det T_r = -r^-5 EXACTLY (r^5 * det = -1 at all 8 test r). T_r invertible for every r>0.
      Sample: det T_(r=2) = -1/32 = -2^-5.
[C]   Sign convention: the value -r^-5 is under the (3.1) interleaving (f_s(M) in slot 4 before
      f_t(S) in slot 5). One column transposition to group longitudinal-then-transverse flips the
      sign to +r^-5; the density-relevant invariant is |det T_r| = r^-5. (Matches CL-DER-051 s.3.)
[D]   Planted-mutation sensitivity, both prior mutations reproduced:
        GP M1  (G_t^- denominator r -> r^2)      => det = -r^-6  (detected).
        CL mut (V-_corr denominator r^3 -> r^2)  => det = -r^-4  (detected).
[E]   Density convention: |det T_r| = r^-5  =>  p_X(x_r) = r^-5 p_Y(y_r).
[F]   Contact-power ledger: (r^3/6) * r^-5 * r^2 = 1/6 exactly; power bookkeeping 3 - 5 + 2 = 0.


BONUS CORROBORATIONS (tie EC-014 to the EC-007 kernel machinery):
[+]   Unscaled coalesced six-jet covariance det = 12 EXACTLY for (f, f_ttt, f_t, f_tt, f_s, f_ts)
      under K=exp(-(t^2+s^2)/2), rebuilt from Cov(d^a,d^b)=(-1)^|b| d^(a+b)K(0). (Matches C102's "12".)
[+]   Corrected-frame limit basis (f, -(1/12)f_ttt, f_t, -f_tt, f_s, -f_ts) has covariance
      det = 12 * (1/12)^2 = 1/12 EXACTLY. (Matches GP-DER-061 s.8 / CL-DER-051 s.7.)
[+]   Frame-correction necessity (File 4 Remark 3.1, the "silent trap"): numerically, as r->0 the
      uncorrected V- = (f(S)-f(M))/r^3 diverges (~1e6 at r=1e-3, the -f'/r^2 content), while the
      corrected V-_corr -> -(1/12) f'''(0) (= +0.08333 for the test field). The gradient correction
      is essential off the constraint surface, exactly as File 4 states.


------------------------------------------------------------------
3. SCOPE INSPECTION (CL read; NOT the formal different-line discharge)
------------------------------------------------------------------
The proposed terminal statement — "CLOSED — EXACT CORRECTED SIX-PIN PAIR-FRAME JACOBIAN AND
CONTACT-POWER IDENTITY" — is restricted (per GP-REQ-057 s.5) to exactly four self-contained facts:
  (i)   the declared transform T_r and ordering;
  (ii)  det T_r = -r^-5, or |det T_r| = r^-5 with sign convention disclosed;
  (iii) p_X = r^-5 p_Y;
  (iv)  (r^3/6) r^-5 r^2 = 1/6.
Assessment: these four are elementary linear-algebra / algebra facts about a fixed finite-r matrix.
None requires or implies any open analytic claim. The scope is drawn correctly. Three specific
"do-not-bundle" watch items, all already correctly EXCLUDED by the proposed statement:
  - Lemma P1 UNIFORM invertibility / Sigma(r)->Sigma(0) with spectrum in [c,C] is a separate
    (H1+)/Lambda_8-dependent analytic claim; EC-014 uses only finite-r invertibility (det != 0), which
    is elementary and does NOT depend on Lemma P1. Do not let closure imply uniform convergence.
  - The coalesced determinant (12, 1/12) is a LIMIT-BASIS fact; it must remain a compatibility note,
    not part of the finite-r terminal statement.
  - The factor r^2 z_r carries z_r; the terminal statement asserts only the ALGEBRAIC power
    cancellation and the constant 1/6 — it must NOT be read as any statement about z_r's value,
    positivity, or limit, nor about J_r -> J_0 in (0,infinity), adjacency, contact limit, the
    lifetime-density coefficient, Theorem B, P0.2, or any machine-state promotion.
If the AO48/CW inspector concurs with the four-fact boundary above, EC-014 is closure-ready.


------------------------------------------------------------------
4. REMAINING EC-014 GATE (single step)
------------------------------------------------------------------
Reconstruction leg: DISCHARGED (CL-DER-051, and independently re-confirmed here from the primary
source). Open: ONE scope inspection by a different line (AO48 or CW/C047R — not CL, not GP)
confirming the four-fact boundary in s.3. After that, GP prepares the terminal package for Dylan's
approval. Silence is not verification.


------------------------------------------------------------------
5. CORRECTION / REOPENING PATH
------------------------------------------------------------------
If any later audit finds an entry, ordering, sign, denominator-power, or scope error, do not edit
this document silently; file an additive erratum naming the exact failed line and the affected and
unaffected dependencies.


END OF RECORD.