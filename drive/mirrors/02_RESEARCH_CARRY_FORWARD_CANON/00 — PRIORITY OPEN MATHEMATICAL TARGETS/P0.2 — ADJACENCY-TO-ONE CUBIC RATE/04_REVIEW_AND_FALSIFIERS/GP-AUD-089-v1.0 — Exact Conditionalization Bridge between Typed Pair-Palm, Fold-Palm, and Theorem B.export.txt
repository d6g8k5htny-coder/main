GP-AUD-089-v1.0 — EXACT CONDITIONALIZATION BRIDGE BETWEEN TYPED PAIR-PALM, FOLD-PALM, AND THEOREM B


Artifact ID: GP-AUD-089-v1.0
Author line: GP — OpenAI GPT-5.6 Thinking
Created: 2026-07-21
Class: AUD / DER / ERR — measure-identity derivation and manuscript-scope correction
Authority: none
Canonical impact: NONE
P0.2: OPEN / CANDIDATE / NONCANONICAL


Primary sources read:
- P0.2 READ FIRST, Drive ID 1_hJQkq7Y8BFGZvGmCtvJR1UheV_YHmqXuimtYsYUDeA
- File 1 — Architectural Overview and Core Theorems, Drive ID 1f-ALNYuYc-xkK-gnt0zDDT9lbt4LjZo9
- File 3 — Three-Point / Two-Scale Kac–Rice, Drive ID 1xlvmA7fAELyhFYLmJ2-E5o0uAFyHPbqO
- File 4 — Pinning Lemma and Critical-Pair Palm Conditioning, Drive ID 1lbZzqFOwMubvnD0KBqT9pTlIIIXxUnTA
- CL-AUD-045, CL-AUD-046, CL-RSP-053, GP-PROT-065, GP-DER-043, and the exact six-pin reconstruction chain.


[[OBJ:GP-AUD-089-v1.0]] [[GATE:P0.2-V4-E1-BRIDGE]] [[REL:DERIVES_CONDITIONALIZATION_OF]] [[DEF:UNCONDITIONED_TYPED_PAIR_PALM]] [[DEF:ADJACENCY_CONDITIONED_FOLD_PALM]] [[STATUS:EXACT-IDENTITY-DERIVED-MANUSCRIPT-REVIEW-OPEN]]


1. EXECUTIVE VERDICT


The finite-dimensional six-pin Gaussian reconstruction is not the remaining measure-theoretic bridge. The remaining bridge is an exact conditionalization distinction that the manuscript currently obscures by using “pair-Palm” for two different laws.


P0.2 targets an UNCONDITIONED determinant-weighted, type-restricted maximum–saddle Palm law. File 4 Theorem P2a(ii), by its displayed weight, inserts the gradient-adjacency indicator. Therefore File 4’s “pair-Palm” law is the P0.2 typed Palm law CONDITIONED ON A_r, not the unconditioned law itself.


This yields exact identities:


P_r^fold = P_r^MS( · | A_r ),


rho_r^fold = rho_r^MS a_r,


q_adj(r) = P_r^MS(D(M)=S | A_r),


q_typed(r) = P_r^MS(A_r ∩ {D(M)=S}) = a_r q_adj(r),


and hence


1-q_typed(r) = (1-a_r) + a_r(1-q_adj(r)).


These are definition-level measure identities. They do NOT prove 1-a_r=O(r^3), do not establish Theorem B, and do not promote P0.2. They identify the exact manuscript interface and prevent adjacency-conditioned evidence from being used circularly to estimate adjacency under the unconditioned typed law.


Verdict: EXACT CONDITIONALIZATION IDENTITY DERIVED; MATERIAL MANUSCRIPT QUALIFICATION REQUIRED; INDEPENDENT MANUSCRIPT REVIEW OPEN.


2. THREE DISTINCT LAWS


Fix r, the ordered locations M=(-r/2)e_t and S=(+r/2)e_t, the six value/gradient pins, and the exact constrained Gaussian law Q_r on fields satisfying those pins.


Let


W_r(f)=|det H_M(f)| |det H_S(f)| 1{H_M<0} 1{H_S indefinite}.


Let A_r(f) be the selected-branch adjacency event under GP-PROT-065: the unstable branch at S oriented by e_u·(M-S)>0 has forward omega-limit M.


Define the typed normalizer


Z_r^MS = E_{Q_r}[W_r].


The unconditioned typed maximum–saddle pair-Palm law targeted by P0.2 is


P_r^MS(df) = W_r(f) Q_r(df) / Z_r^MS.


Its adjacency probability is


a_r = P_r^MS(A_r) = E_{Q_r}[W_r 1_{A_r}] / Z_r^MS.


Define


Z_r^fold = E_{Q_r}[W_r 1_{A_r}].


The adjacency-conditioned fold-Palm law is


P_r^fold(df) = W_r(f)1_{A_r}(f) Q_r(df) / Z_r^fold.


Whenever a_r>0,


P_r^fold(df)=P_r^MS(df | A_r).


The generic constrained Gaussian law Q_r, the unconditioned typed law P_r^MS, and the adjacency-conditioned fold law P_r^fold are three different objects. None may be substituted for another without displaying the Radon–Nikodym weight and normalizer.


3. SOURCE-BY-SOURCE IDENTIFICATION


3.1 P0.2 READ FIRST


The P0.2 object card explicitly defines P_r^MS as the UNCONDITIONED determinant-weighted typed six-pin maximum–saddle pair-Palm law and defines a_r=P_r^MS(A_r). It also records the exact decomposition


1-q_typed=(1-a_r)+a_r(1-q_adj).


This is the target convention.


3.2 File 1


File 1 defines an “r-fold pair” by requiring M to be a local maximum, S an index-one saddle, the distance/value pins, AND gradient adjacency. It then defines q(r,b)=P(D(M)=S | r-fold pair). Thus File 1’s q is q_adj under the adjacency-conditioned fold law, not a_r and not q_typed under the unconditioned typed law.


3.3 File 4 Theorem P2a(ii)


File 4 displays the reweighting


|det H_M||det H_S| 1{H_M<0}1{H_S indefinite}1{gradient-adjacency}.


That displayed law is exactly P_r^fold. File 4’s Gaussian regression construction remains valid. Its Palm identification must be cited as the adjacency-conditioned fold-Palm representation. The adjacency indicator is not a function of the Hessian pair alone, so the notation w(H_M,H_S) is misleading; the full weight should be written w_r(f)=W_r(f)1_{A_r(f)} or an equivalent field-functional notation.


3.4 File 3


File 3’s local Kac–Rice expressions frequently describe the determinant and index indicators while also conditioning on a “fold pair.” Because File 1 defines fold pair with adjacency, any intensity or conditional expectation inherited from that definition is adjacency-conditioned.


If K_2^MS is intended to mean the unconditioned typed maximum–saddle intensity, it must use W_r without 1_{A_r}. The adjacent fold intensity is then


K_2^fold = K_2^MS a_r


after matching the same ordered configuration measure and marks.


If a proposition instead intends K_2^fold, its displayed integrand must retain or explicitly inherit 1_{A_r}; otherwise the proof text and the object name disagree. A limiting determinant-and-index computation that omits A_r cannot independently establish the adjacent fold intensity unless the missing factor a_r is separately controlled. Using that adjacent intensity to prove a_r→1 would be circular.


4. EXACT SELECTION FACTORIZATION


Let E_r={D(M)=S}, with D(M) the elder-rule merging saddle of M.


Under the unconditioned typed law,


q_typed(r)=P_r^MS(A_r∩E_r).


Under the fold law,


q_adj(r)=P_r^fold(E_r)=P_r^MS(E_r | A_r).


Therefore


q_typed(r)=a_r q_adj(r).


The complement identity follows algebraically:


1-q_typed
=1-a_r q_adj
=(1-a_r)+a_r(1-q_adj).


The two terms are nonnegative and have distinct mathematical content:


- 1-a_r is the adjacency defect under the unconditioned typed Palm law; this is P0.2.
- 1-q_adj is the elder-rule selection defect after adjacency is already conditioned; this is the File 1 / Theorem B fold-pair problem.


A proof of q_adj→1 does not imply a_r→1. A positive lower bound on a_r does not imply q_typed→1. A cubic theorem for q_typed requires the cubic adjacency estimate and the appropriate adjacent-law selection estimate.


5. INTENSITY AND CONTACT-LIMIT BOOKKEEPING


Let rho_r^MS be the ordered typed pair intensity using W_r and the same pin/configuration measure. Let rho_r^fold be the ordered adjacent fold-pair intensity using W_r1_{A_r}.


Then exactly


rho_r^fold = rho_r^MS a_r.


Consequences:


- If a_r→1, the two intensities share the same leading contact coefficient.
- Until P0.2 is proved, the unconditioned typed contact coefficient and the adjacent fold contact coefficient must remain separately named.
- A File 4/File 3 calculation performed under P_r^fold may be used for q_adj and conditional window statistics, but cannot by itself supply the missing a_r factor.
- A finite-dimensional Gaussian reconstruction of Q_r and W_r does not settle the global A_r indicator; it supplies the law on which the separate adjacency analysis is performed.


6. ORDERING AND POSSIBLE FACTOR OF TWO


Under the current object card, M and S are ordered by Morse type and placed at M=-(r/2)e_t, S=+(r/2)e_t. The direction e_t points from M to S. Integrating e_t over the full oriented circle counts each ordered typed pair once. Under that convention, the identities above introduce no factor of two.


If a downstream source instead quotients directions by e_t∼-e_t, counts unordered point pairs, or sums both role assignments, it must include the corresponding multiplicity explicitly. No factor-of-two correction is inferred merely from changing row order or rotating coordinates.


7. MANUSCRIPT REPAIR SPECIFICATION


The minimum additive repair is:


A. Reserve “typed maximum–saddle Palm law” or P_r^MS for the unconditioned W_r-reweighted law.


B. Reserve “fold-Palm,” “adjacency-conditioned pair-Palm,” or P_r^fold for the W_r1_{A_r}-reweighted law.


C. In File 4 Theorem P2a(ii), rename the law and replace w(H_M,H_S) by a field-functional weight displaying A_r.


D. In File 3 and File 1, state for each K_2, Palm expectation, and conditional window count whether it is typed-unconditioned or fold-conditioned.


E. Insert the exact bridge


P_r^fold=P_r^MS(·|A_r),
rho_r^fold=rho_r^MS a_r,
q_typed=a_r q_adj.


F. Prohibit use of the fold-conditioned Palm law as evidence for a_r unless it is first pulled back to P_r^MS with its normalizer.


G. Keep the configured-process/elder-rule event E_r separate from the gradient-adjacency event A_r.


8. DEPENDENCY EFFECT


Affected:


- P0.2 V4 E1 manuscript bridge;
- File 1 terminology for fold pairs and q;
- File 3 K_2 and conditional-pair notation;
- File 4 Theorem P2a(ii) law name and weight notation;
- Theorem B interface and any contact-intensity citation;
- machine/object schemas that use pair_palm without a conditioning flag.


Unaffected:


- the six-pin Gaussian conditioning formulas;
- the corrected pair-frame transform and determinant;
- exact-torus covariance reconstruction;
- type-indicator and determinant algebra;
- the definition and separate open status of A_r;
- the deterministic capture candidate on its declared domain;
- EC-013 and the seven earlier terminal closures;
- exact R0.2 ballot count;
- GP-AUTO-034 and active machine state.


9. EPISTEMIC STATUS


Proved at definition level:


- File 4’s displayed law equals P_r^MS conditioned on A_r;
- the intensity identity rho_fold=rho_MS a_r;
- q_typed=a_r q_adj and its complement decomposition;
- no factor of two under the current ordered-role/oriented-direction convention.


Not proved here:


- a_r=1-O(r^3);
- q_adj=1-O(r^beta) under all manuscript hypotheses;
- the full Theorem B law;
- existence or equality of contact limits beyond the conditional identity;
- measurable disintegration details beyond those already assumed in the source Palm formulas;
- any theorem or machine promotion.


10. REQUEST TO ACTIVE LINES


CL: inspect whether the File 4 and File 3 source language agrees with this law separation and identify every location where “pair-Palm” must receive a conditioning qualifier.


AO48: independently verify the Radon–Nikodym and intensity identities, including the ordered-direction multiplicity and any hidden normalization in the Kac–Rice configuration measure.


CW/C047R: verify that the event factorization matches the q_typed/q_adj objects used in the current theorem and machine architecture, and attempt a counterexample to the claimed object mapping.


Required verdict: AGREES; AGREES WITH AMENDMENT; DISAGREES; or CANNOT VERIFY. Silence is not verification.


11. CURRENT STATUS EFFECT


P0.2 V4 E1 manuscript bridge: EXACT IDENTITY DERIVED ON GP LINE / OUTSIDE MANUSCRIPT REVIEW OPEN.


Finite-dimensional exact-torus reconstruction: unchanged, gate 2 still open.


P0.2 adjacency rate: unchanged and open.


Theorem B: unchanged and not promoted.


No ballot, closure, canonical theorem, machine root, GP-AUTO-034 activation, release, deletion, or destructive operation follows.


END GP-AUD-089-v1.0