GP-REQ-172-v1.1 — BLINDED NON-GP T2 TAKEOVER PACKET FOR EC-019 — CORRECTED CERTIFICATE REQUIREMENTS


Artifact ID: GP-REQ-172-v1.1
Prepared by: GP — OpenAI GPT-5.6 Thinking
Date: 2026-07-24
Class: REQ / BLINDED CLEAN-ROOM REVIEW ROUTING / CORRECTED ACCEPTANCE PACKET
Authority: research preparation and routing only
Canonical impact: NONE
Queue: EC-019 / HB-030 / T2 only
Predecessor: GP-REQ-172-v1.0
[[OBJ:GP-REQ-172-v1.1]] [[TASK:EC019-T2-BLIND]] [[STATUS:T1-PASS]] [[STATUS:T2-OPEN]] [[NO_PROMOTION]]


1. CURRENT GATE STATE


EC-019 Test T1 is PASS within its declared fixed-r symbolic scope. Only Test T2 remains open on the preferred fail-closed path.


T2 requires one source-independent, organizationally distinct, directed-rounding or otherwise rigorous implementation of the nineteen Route-B capture margins and five planted controls defined in GP-SPEC-166-v1.0.


This packet does not approve EC-019, restore terminal status, change a theorem, promote any machine, accept any root, authorize release, or perform any irreversible action.


2. BLIND-LANE FIREWALL


Before freezing both implementation source and primary result records, the reviewer must read only:
- this packet; and
- GP-SPEC-166-v1.0, Drive ID 1MzToKSsTJ-PtMMH6TVN6qETiInRz8Q4SpqUz5QTWeJY.


Before source and primary results are frozen, do not open or use:
- GP-DATA-168-v1.0 or GP-DATA-168-v1.1;
- GP-AUD-169-v1.0 or GP-AUD-176-v1.0;
- GP-DATA-106 source or stored arrays;
- GP-DER-106 Section 3 numerical outputs;
- CL-AUD-071 output values;
- Command Center passages containing EC-019 numerical margins;
- any copied implementation, stored interval array, margin table, discrepancy table, or same-line execution receipt.


Awareness that prior implementations exist does not invalidate blindness. Reading their code, numerical margins, interval outputs, control outcomes, or discrepancy analysis before freezing does.


3. REQUIRED PRE-REGISTRATION


Publish a short timestamped pre-registration before coding that states:
- provider, base-model family, model line, and session identity;
- software language, exact interval or arbitrary-precision library, and planned precision policy;
- all sources already read;
- whether any GP/OpenAI or other prior implementation or stored output was seen;
- commitment to derive the implementation only from GP-SPEC-166-v1.0;
- intended source normalization and result serialization rules;
- intended mechanism for outward enclosure;
- intended full-dimensionality representation and validation;
- planned negative-control execution;
- planned multi-precision or alternative containment checks.


4. IMPLEMENTATION REQUIREMENTS


Write a new implementation from the formulas and domains in GP-SPEC-166-v1.0 without copying existing source.


The implementation must satisfy all of the following before comparison:


A. RIGOROUS ENCLOSURE
- Every arithmetic operation contributing to a claimed one-sided margin must be enclosed outward.
- Square root must have a documented guarantee of lower and upper enclosure. Merely changing a Decimal context rounding mode around a nearest-rounded square-root function is not sufficient unless the library documentation proves directed behavior.
- Endpoint-derived divisions, products, additions, subtractions, Gershgorin bounds, chart margins, cone margins, handoff bounds, and auxiliary constants must remain under directed or verified interval arithmetic through final extraction.
- Point sampling and ordinary binary floating-point error bars do not qualify.


B. FULL-DIMENSIONALITY GATE
- The parameter object must preserve center/radius data or an equivalent representation from which strict positive widths are machine-checkable.
- A validator must reject any parameter box with a missing or nonpositive radius before any dynamical margin calculation.
- The primary box must pass the validator.
- Each of the nine parameter radii a, w, z, s, c40, c31, c22, c13, and c04 must be set to zero in a separate executed mutation test, and every mutation must return a machine-generated full-dimensionality failure.
- A hard-coded pass flag or prose-only assertion is nonqualifying.


C. FORMULA AND DOMAIN COVERAGE
- Implement all nineteen named primary margins exactly as specified.
- Preserve the polynomial continuation at xi=0; no numerical division by xi at the endpoint is permitted.
- Serialize all input intervals, all nineteen margins, W, rho, tau_upper, validator outcomes, and all five negative-control outcomes.


D. REGRESSION AND STABILITY
- Include a low-precision square-root containment test that checks lower^2 <= x <= upper^2 for a non-square positive input.
- Include interval containment tests for representative arithmetic operations.
- Include an even-power test on an interval crossing zero.
- Execute every planted control rather than assert its expected result.
- Re-run at at least three preregistered precisions or with an independent verified interval backend; all primary signs and control verdicts must be stable. The exact precision choices must be frozen before comparison.


5. FREEZE REQUIREMENTS


Before opening any comparison material:
- freeze the complete source bytes;
- record source byte count and SHA-256;
- record environment, runtime, operating system, and library versions;
- run the primary box and all five controls;
- serialize every interval and required outcome;
- freeze the primary result bytes;
- record result byte count and SHA-256;
- publish the pre-comparison verdict based solely on the independent run.


Post-comparison tuning, constant adjustment, hidden manual control overrides, or substitution of a prior implementation is nonqualifying.


6. POST-FREEZE COMPARISON LANE


Only after both source and primary-result hashes are fixed may the reviewer open prior implementations and results, including GP-DER-106, CL-AUD-071, GP-DATA-168-v1.0, GP-DATA-168-v1.1, GP-AUD-169-v1.0, and GP-AUD-176-v1.0.


Then publish a discrepancy table explaining every material difference by formula interpretation, interval representation, algebraic rearrangement, precision, library behavior, or convention. Constants and domains may not be tuned after comparison.


7. REQUIRED VERDICT


Return exactly one:


T2 — PASS: all nineteen margins are rigorously positive, all five controls are computationally executed and fire, the full-dimensionality gate rejects all nine zero-radius mutations, source independence and reproducibility are documented, and discrepancies are explained without post-comparison tuning.


T2 — FAIL: a declared margin, validator gate, or planted control fails under a correct independent implementation; identify the exact object, formula, interval, and counterevidence.


T2 — INCONCLUSIVE: rigorous enclosure, blindness, source independence, reproducibility, validator coverage, or convention resolution is insufficient.


A qualifying T2 PASS makes EC-019 eligible for separate operator/canonical adjudication. It does not terminally close EC-019 by itself.


8. SCOPE FIREWALL


A T2 pass closes only the independent verification gate for the fixed-r qualitative object P_{1/20}^{MS}(A_{1/20}) > 0 within the declared interface.


It does not establish:
- a numerical probability;
- any other r;
- r-uniformity;
- a_r -> 1;
- 1-a_r = O(r^3);
- P0.1;
- P0.2;
- Theorem B;
- elder-rule pairing;
- a lifetime law;
- GP-AUTO-034 qualification;
- machine replacement;
- package sealing;
- external release;
- deletion or any destructive action.


END GP-REQ-172-v1.1