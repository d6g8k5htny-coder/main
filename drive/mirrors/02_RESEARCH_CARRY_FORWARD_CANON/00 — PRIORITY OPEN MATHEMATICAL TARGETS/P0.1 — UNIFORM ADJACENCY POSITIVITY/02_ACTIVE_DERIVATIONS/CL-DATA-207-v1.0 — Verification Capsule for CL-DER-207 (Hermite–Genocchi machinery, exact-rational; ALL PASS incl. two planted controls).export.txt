CL-DATA-207-v1.0 — VERIFICATION CAPSULE FOR CL-DER-207 (MOVING-POINT CROSS-COVARIANCE LEMMA)


Artifact ID: CL-DATA-207-v1.0
Author line: CL — Anthropic Claude
Date: 2026-07-25
Class: DATA — verification receipts + verbatim output for CL-DER-207-v1.0
Governing protocol: OP-PROT-011
Canonical mathematical impact: NONE


[[OBJ:CL-DATA-207-v1.0]] [[VERIFIES:CL-DER-207-v1.0]] [[ALL-TESTS-PASS]] [[NO_PROMOTION]]


RECEIPTS (frozen 2026-07-25T21:32:32Z):
  cl_der_207_verify.py          12,265 B  sha256 fa00e617011875bc208c8e2a1e38b38f88fcf50bc36d6a54fbe912fbbc726226
  cl_der_207_verify_output.txt      918 B  sha256 335d1726254905f38f1e2f585db10787a28cd1252105c00851760506e8ba8fa8
Deterministic seed 20260726; byte-identical output across repeated runs (verified).
Backend: Python 3.11 standard library, fractions.Fraction — zero rounding anywhere.
Identity tests demand EXACT equality; bound tests use rigorous coefficient sup-bounds
sup_{|x|<=a,|y|<=b} |P| <= sum |c_ij| a^i b^j.


TRANSPORT NOTE: the authoritative source bytes are delivered to the operator as a raw file
(hash above). Per this project's own publication-incident history (GP-AUD-178 addendum;
CL-AUD-062 Section 2b), long payloads embedded in Doc wrappers are the transcription-failure
mode, so the listing is not embedded here. Reconstruct from the delivered file and verify the
sha256; or re-derive the script directly from CL-DER-207 Sections 3-5, which it tests one-for-one.


WHAT IS TESTED (all PASS):
  T1  Hermite-Genocchi: the simplex-integral representation, computed by TWO independent routes
      (complete-homogeneous-symmetric closed form; direct Dirichlet multi-index integration),
      equals the recursive confluent divided difference EXACTLY — 30 random polynomials with
      repeated (confluent) nodes.
  T2  Classical specialization x^n[t_0..t_k] == h_{n-k}(t_0..t_k), exact.
  T3  Confluent cancellation / no 1/r blow-up:
      |g[nodes in +-r/2] - g^(k)(0)/k!| <= sup|g^(k+1)| (r/2)/k!.
  T4  MOVING-POINT uniform bound: |c_r(rX) - (-1)^k Phi^(k)(0)/k!| <= Lambda r (R+1/2)/k! for all
      |X| <= R — 30 random fixed-order surrogates x 6 off-axis evaluation points each.
  T5a PLANTED CONTROL: holding the evaluation point FIXED (not shrinking with r) violates the
      origin-limit bound — the moving-point structure is load-bearing. FIRES.
  T5b PLANTED CONTROL: a node placed OUTSIDE the O(r) window breaks the confluent bound — the
      node-shrinking hypothesis is load-bearing. FIRES.
  T6  Composite six-pin-style family: x-divided-differences of orders 0..3 over {M,M,S,S} and
      partial_y composed with x-divided-differences of orders 0..1 over {M,S} — every member
      obeys ONE common O(r) bound uniformly over an off-axis X-grid.


DEFECT CAUGHT AND CORRECTED BEFORE FREEZE (recorded for reviewers): the first draft of T4 bounded
the deviation using only sup|partial_x Phi|. It FAILED at ratio 3.65 — correctly, because the
deviation displaces the evaluation point in BOTH coordinates (rX has a y-component). The
Lipschitz constant was corrected to the full first-order form sup|partial_x Phi| +
sup|partial_y Phi|, in both the test and CL-DER-207 Section 5, and T6 was strengthened to move X
off-axis so it exercises the same corrected bound. This is a correction to the CONSTANT, not to
the lemma: the O(r) conclusion is unchanged.


================ OUTPUT (verbatim, 918 B) ================
PASS  T1 Hermite-Genocchi: simplex integral (2 independent routes) == confluent divided difference, EXACT (30 random cases with repeated nodes)
PASS  T2 classical identity x^n[t_0..t_k] == h_{n-k}(t_0..t_k), EXACT
PASS  T3 confluent cancellation: |g[nodes in +-r/2] - g^(k)(0)/k!| <= sup|g^(k+1)|*(r/2)/k!  (no 1/r blow-up)
PASS  T4 moving-point uniform bound: |c_r(rX) - (-1)^k phi_x^(k)(0)/k!| <= Lip*r*(R+1/2)/k! for ALL |X|<=R (30 cases x 6 points)
PASS  T5a planted control: fixed (non-shrinking) evaluation point violates the origin-limit bound -> moving-point structure is load-bearing
PASS  T5b planted control: a node outside the O(r) window breaks the confluent bound -> node-shrinking hypothesis is load-bearing
PASS  T6 composite six-pin-style functional family (x-DD orders 0-3 on {M,M,S,S}; d/dy o x-DD orders 0-1): every member obeys the common O(r) bound uniformly on the X-grid


ALL TESTS PASS: True
================ END OUTPUT ================


END CL-DATA-207-v1.0