# LPW-R05-UPPER — same-law upper-bound investigation

## Exact target

For the exact normalized side24 two-dimensional Bargmann–Fock field, b=6/5, raw pins f(M)=b, f(S)=b-r^3/6, both gradients0 at M=(-r/2,0),S=(r/2,0), use the continuous Gaussian regression version and W=|detH_M detH_S|1_typed, Z=E_QW. Set q=P{D(M)=S}, the superlevel elder-rule event without adjacency conditioning. Seek C,r1>0 such that1-q<=Cr^3 for EVERY0<r<=r1.

No existing document in this work order is represented as a closed matching upper theorem. The local-path argument supplies only a lower bound and cannot be inverted to provide a cover of every failure. The separate3D AO48-OPR-045 result is not applicable.

## Work U1 — exhaustive deterministic failure coverage

Define measurable events for all possible mechanisms. Prove their union covers {D(M)!=S}, with clear handling of preemption before f(S), lack of relevant component connection, same-component/self-attachment or younger-component merges, and all genericity exceptions actually used. Do not infer from both gradient branches reaching selected maxima that the full merge-tree event has been covered without the required topological argument. Preserve eta_r in any identity p_r=a_r q_adj+eta_r. Prefer a direct full-pair law treatment when it avoids an unproved adjacency transfer.

## Work U2 — exact change of measure and height integration

Every channel bound must be under the SAME six-pin determinant-weighted law or carry a proved transfer. If using a thirdpoint Kac–Rice integral, write the joint gradient/value density and integrate over the complete height window. The candidate full form is

    Lambda_r(y)=Z_r^-1 int_{b-r^3/6}^b p_{grad f(y),f(y)|six pins}(0,v)
       E[W2 |detH_y|1_type |six pins,grad f(y)=0,f(y)=v]dv.

State the applicable Kac–Rice hypotheses. The historical representative-height evaluation can replace this only with an exact identity or a uniform error bound of appropriate polarity. An estimate of small sensitivity is not an exact equality.

## Work U3 — all-small-r and all-spatial regimes

For every covering event, obtain an O(r^3) probability bound or another summable contribution delivering the target exponent. Name near-pin, collision, angular, farfield and loop regimes; prove coverage and integrability. A fixednonzero residual cannot disappear intoCr^3 asr->0. Finite-rung fits and a singlearch law are exploratory evidence only. Explain which covariance normalizers, determinant powers and spatial Jacobians are used.

## Work U4 — attempt the cheapest decisive falsification

Before expensive quadrature, test whether the proposed channel cover omits a topological configuration, misidentifies its law, or leaves a residual of larger order. A failure of this upper route is not a failure of the established lower witness. Freeze a minimal counterexample or an exact failed step. The user authorized research, not protection of a preferred exponent.

## Work U5 — integrate only after the bounds match

State all constants, their dependence, the common radius, exact moment/volume/filtration scope, and any review/genericity assumptions. If successful, compose with the corrected, accepted lower bound on the minimum radius. Theta(r^3) is a two-sided order estimate; it does not establish existence of lim(1-q)/r^3 or identify that limit.

## Supplied provenance

Read offline_sources/README.md and UB0_LINEAGE_DECISION_RETRIEVED_TEXT.txt first. C020 and C021 are historical provenance at their stated grades and must be read together. Their numerical and trajectory-termination corrections cannot be dropped. The current operator record is in drive_before/01_OPERATOR_ADJUDICATION.md. No further owner vote is required for eligible evidence-gated actions.
