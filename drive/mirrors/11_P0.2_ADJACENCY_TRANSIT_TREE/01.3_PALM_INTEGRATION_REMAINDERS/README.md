# `01.3_PALM_INTEGRATION_REMAINDERS` — reading copies

Drive folder id `1R6GoA7c-qzseLoFKuW8IsYHup1jNIMb7`, 3 inventory items: this folder
(tree-only, indexed in the lane-root `_MANIFEST.jsonl`) and two native Google Docs,
stored here as text exports.

**Nothing here is byte-exact and nothing here can be made byte-exact.** Both objects are
native Google Docs; `drive/inventory.jsonl` declares no `sha256` for either and no
register tab declares a body digest for either, so the two digests in `_MANIFEST.jsonl`
were computed here from this repository's own exports and can be compared with nothing
the Drive publishes.

| file | Drive id | exactness | bytes stored | inventory bytes |
|---|---|---|---:|---:|
| `00_LEAF_CARD — 01.3 Palm Integration Remainders (CL-NAV-036.4).export.txt` | `1z3HP5142B_pQ_NqBJPUxFtKVBGZqnXA5Pyfpbe_QO4I` | `exact: false` — reading copy | 2,808 | 3,114 |
| `LCR-AUD-012-v1.0 — Adversarial Audit of P02-LM-003 Boundary-Layer Palm Integration.export.txt` | `1T3GxktbYBO2VaaNsvOnU1eeVqSWNj11EZz5iRFN2FFE` | `exact: false` — reading copy | 12,394 | 8,137 |

The stored and inventory byte figures differ in both directions because the inventory
cell is the Drive-reported size of a native Doc, which is neither a payload length nor a
digest.

The leaf card's opening note explains the folder's name: "Palm" here, it says, "here is
Palm calculus (pair-Palm conditioning of the determinant-weighted typed six-pin law),
not a model name".

## The status banners, verbatim

**The leaf card.** Its header line reads "ARTIFACT: CL-NAV-036.4-v1.0 · AUTHOR: Claude
(Anthropic), CL-* instance · CREATED: 2026-07-21 · CLASS: NAV — leaf charter +
live-state card · CANONICAL IMPACT: NONE · AUTHORITY: none", and it closes
"Non-authority header guards against stale-card citation." Under the heading it spells
"What must not be claimed" it says: "The O(r³) power count applies to the certified
bad-set decomposition, not yet to every true nonadjacent configuration; GT5 is
proved-proposed within the exact six-pin model. P0.2 remains OPEN; no decimal here is a
theorem constant."

Two of its live-state lines are fences. Of the normalizer: "Q_L is exponentially close
to, but not literally, the planar N(−b,2) law at fixed L = 24; the decimal 3.2309785… is
a planar-limit value, not an exact fixed-torus identity." And of what is still owed:
"Open audit obligation: the determinant-weight second-moment estimate DM2 (E[W_r²] ≤ C
r⁴) and the capture majorant still need independent audit (CM-043/CM-047)." It also
carries the other half of the collision the 01.1 card records: "Beware the identifier
collision with the endpoint-blocks GP-DER-044 (see 01.1 card; GP-CLS-001)."

**The audit.** `LCR-AUD-012-v1.0` opens with a successor notice, not with its own
verdict: "The proposed statement below is now historical critique scaffolding.", and
"External review must target the frozen successor hash, not the proposal below." Its own
header reads "Authority: same-organization technical audit only", "Canonical
mathematical impact: NONE" and "Independence credit: ZERO". Section 1 gives the current
status as "PROOF-CANDIDATE / SAME-LINE VERIFIED / ORGANIZATIONALLY DISTINCT REVIEW
PENDING." and adds "Do not close P02-LM-003 until a qualifying external reviewer audits
the density envelope, q=rs Jacobian, determinant factor, polynomial integral, and
normalizer direction." Section 9's verdict is "SURVIVES — PROPOSED P02-LM-003
DETERMINANT-WEIGHTED POLYNOMIAL BOUNDARY-LAYER INTEGRATION.", immediately fenced by
"This verdict supplies no independent-eyes credit. The lemma remains nonterminal pending
an organizationally distinct line-by-line audit of H1–H5 and the proof." Section 11 is
the narrowest statement in the lane: "P02-LM-003 would close only a conditional
measure-theoretic interface. It would not prove H1, H2, a deterministic inclusion for
the exact field, local-to-global adjacency, or P0.2."

The register agrees, in its own words rather than these bytes: the
`registers/json/artifact_index.json` row for `LCR-AUD-012-v1.0` records the status
"SURVIVES SAME-LINE / EXTERNAL REVIEW OPEN" and the note "Zero independence credit;
canonical impact NONE".

## What this does not establish

Mirroring is not review, replay, endorsement, promotion or acceptance. Every status word
above is the source's own and none was decided, weighed or aged here. A SURVIVES from a
same-organization reviewer earns no independence credit — `CLAUDE.md` rule 6: "A
same-provider reviewer earns zero independence credit." — and the independence-requiring
gate this audit names stays open.

The object the audit is about is not held here. `LCR-DER-014-v1.0`, the frozen successor
the notice points external review at, is a different Drive id
(`1dnGjJWQOJgRBrF6aq4kIE31i2PBWD2nHs7VgToANx_s`) whose body the audit declares as
"BODY_BYTES: 6874" and "BODY_SHA256:
5173d26d6bfaae5ef65a8ae2d0ca9b829a291be3f7dacaba82bb38d2526bb83e"; that pair matches
what `docs/OPEN_PROBLEMS.md` §D transcribes for `RV-LM003-MAIN` from the register. This
lane holds none of its bytes and no byte-exact copy of it exists here, but a reading copy
of that same id is held at `exact: false` under
`drive/mirrors/02_RESEARCH_CARRY_FORWARD_CANON/` — a text export, not the frozen body the
audit declares those 6,874 bytes for. `GP-DER-043-v1.0`, the primary source the audit
names, is likewise not held. Neither the H1–H5 statement nor any step of the
reconstruction was checked here; no integral, bound or exponent in these exports is
verified, reproduced or certified by anything in this repository. P0.2 is open in
`docs/OPEN_PROBLEMS.md` §D and in `claims/graph.json`, whose `RV-LM003-MAIN` record
carries the queue state "EXACT-HASH THEOREM VERDICT OPEN / MATHEMATICS-PASS / P0.2
OPEN", and is exactly as open after this port as before it.
