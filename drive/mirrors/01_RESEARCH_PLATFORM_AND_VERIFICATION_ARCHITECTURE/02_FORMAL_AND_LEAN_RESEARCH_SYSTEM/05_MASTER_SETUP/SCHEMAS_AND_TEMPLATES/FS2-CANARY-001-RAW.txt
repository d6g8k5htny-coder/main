FS2-CANARY-001 - BYTE-IDENTITY RETRIEVAL CANARY
Header. Not part of the canonical body.

DECOY A (space-padded; MUST NOT match under exact full-line equality):
  BEGIN_FS2_CANARY_BODY  
DECOY B (prose): the token BEGIN_FS2_CANARY_BODY inside a sentence.

BEGIN_FS2_CANARY_BODY
FS2-CANARY-001 CANONICAL BODY

trailing spaces follow this arrow ->   
tab	separated	fields
unicode: lambda λ kappa κ nabla ∇ integral ∫ emdash —
combining: nfc é and nfd é must stay distinct
equation-like: sup_{lambda in P_*} P^{MS}_{r,lambda}(E) <= C r^3
repeated repeated repeated words words words
prose containing END_FS2_CANARY_BODY must not terminate extraction


ascii apostrophe: Taylor's theorem
quotes: "double" and 'single'
LAST LINE OF CANONICAL BODY
END_FS2_CANARY_BODY

Trailer. Not part of the canonical body.
DECOY C (prose): the token END_FS2_CANARY_BODY inside a sentence.