from decimal import Decimal
w0=Decimal("0.96026")
vol=Decimal("1.024e-18")
assert w0>0 and vol>0
# Symbolic cancellation: r^2 numerator factor / r^2 denominator factor.
print("PASS — positive constants and exact r^2/r^2 cancellation interface")