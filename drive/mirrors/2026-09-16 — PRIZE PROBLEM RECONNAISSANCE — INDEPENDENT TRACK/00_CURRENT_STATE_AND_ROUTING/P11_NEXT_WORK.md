# P11 next exact work — no background tasks implied

1. Review P11-A/B/C at exact hashes, prioritizing transformed-cost versus probability separation, disjoint core/residual palettes, and singleton/end-point treatment. No promotion from test counts.
2. Reconstruct the two deterministic first-core-selected arguments and the asymmetric sprinkling reduction. Try to break the all-probability hazard bounds rather than merely retesting mu>=3/4.
3. Compare the exact potential statements with FKP pair-supported rounding and Kahn--Kalai before claiming novelty. Those prior results are not absent merely because the wording differs.
4. The new primitive interfaces are graph and rank<=3. Do not repeat those searches as though P10's old gate list remains unchanged. Broader arbitrary-rank primitive-gate potential or cheap global structural decomposition is the actual open target.
5. An all-rank induction that multipliesK with rank still does not settle a universalK problem. Seek a correlation-aware/structure-aware compression, preserving Phase09's random-color limitation.
6. Shared coordinates cannot be made independent by duplication. Use exact conditioning or an explicit exceptional generator cover. Common palettes are allowed for disjoint substitution; overlapping intersections require a separate proof and generally product palettes.
7. Strengthen independent review now rather than increasing self-generated theorem counts. Same-provider/author implementations do not confer organizational independence.
8. q0 remains separate: consume neither frozen D1 v2.2 nor v2.3 DRAFT as unconditionally closed. RN-UNIF and H5 uniformity require their own exact-source work; coordinate with the owner of that active line instead of duplicating it.
9. Reconcile the older LPW sheet against newer source-bound v4 proposals before any registry change. No change follows from the larger version number alone.
