# P15 current state — structural localization, not unrestricted closure

Grade: AUTHOR_SIDE_COMPLETE_ARGUMENTS_WITH_EXACT_FINITE_COMPANIONS. External independent reviews0; formal prover runs0; historical noveltyUNESTABLISHED; original prize closures0. Deployment is determined by the detached actual receipt, not this file's existence.

## Results

- **P15-A:** the optimized scalar-sandwich parameter is a strict-endpoint version of the ESTABLISHED critical threshold value alpha(D) of a simple game. For an empty/singleton-admissible proper downset, a sandwich of width kappa>=1 exists iff kappa>alpha. A checked feasible critical-weight vector with good-set maximum beta gives palette max(408,floor(408beta)+1) through the preceding P14 theorem. This is an attributed translation, not a new parameter.
- **P15-B:** partition original coordinates into disjoint blocks, using the ACTUAL local restrictions. Assign each block enough integer palette labels for its local obstruction-cover demand. If every crossing minimal witness meets palettes with empty total intersection, the union of local covers controls the global obstruction. Global hazard dominates the sum of local hazards even though crossing constraints exist. Palettes may overlap pairwise. This is a setwise, fixed-label certificate, not fake independent cloning.
- **P15-C:** an even-cycle family of block threshold constraints and half-block crossing witnesses has a fixed816-color/no-dilution hazard cover at every size and rank, while its GLOBAL critical threshold is exactly m(3s-2)/(2s) and grows unboundedly. Taking block size816(2s-1)+1 leaves a genuine816-obstruction. The displayed large example is mathematically defined, not enumerated.
- **P15-D:** if scalar modules of uniformly bounded local width kappa are coupled by a complete-transversal arbitrary graph, a fixed256ceil(408kappa)-piece/no-dilution compatible cover exists, independent of the macro graph's degree/chromatic number and original witness rank. At kappa1 this is104448. The local/macro conditions share coordinates and are NOT declared independent; their separate costs fit inside181/192 of the true global hazard. Complete cross-pair lifting is essential.

## What remains unresolved

There is no proof that every downset has a bounded palette-localization certificate, a suitable complete-transversal representation, or bounded scalar width. General incomplete crossings and shared-variable formulas remain outside these transfers. Fixed labels/palette assumptions are structural hypotheses, not automatic consequences of high good probability. No q0 uniformity obligation is discharged.

Before consumption, read exact archive SHA plus relative proof path; retain all source hypotheses and distinguish this turn's exposed same-author verification from external review.
