# P13-WEIGHTED-20260917T0452Z — Current author-side result

The preceding continuation audit and zero-Q endpoint addendum were uploaded and raw-readback verified BEFORE this new research. The attached and other stored Phase11 snapshots remain distinct. The live source frontier was Phase12, not the older Phase11 pointer.

## Main candidate
For a finite positive-resource family
\[
D=\{U:\sum_{i\in U}b_{ji}<1\text{ for every }1\le j\le d\},\quad b_{ji}\ge0,
\]
and arbitrary independent coordinate probabilities p, define Q=P_p(D^c) and phi(t)=min(1,-log(1-t)), phi(1)=1. At arbitrary nonnegative prices c_i<=phi(p_i),
\[
\boxed{\operatorname{covercost}_{\mathbf c}(D^{(408d)})\le\phi(Q).}
\]
No arity, coefficient-ratio, maximal witness-rank or incidence restriction is imposed at one gate. Capacity is strict. The palette depends on d.

The stronger nonsingleton statement allows prices z<=A p, A>=1 and Q<=2/3. With R=ceil(12A^2)+2 and K=8d(R+1), the explicit cover has cost<=2^(2-3d)Q<=Q/2.

## Composition consequence
Fix d_*. Arbitrarily deep finite hierarchies of these weighted gates, with d_v<=d_* at every node and disjoint original child supports, have one common palette408d_* and no dilution. Their root cover cost is at most min(1,-log mu_p(D)). Thus good probability>=3/4 gives cost<3/10; good probability>=2/3 gives cost<5/12.

This closes the author-side terminal-versus-composable interface explicitly left open in P12-NEXT and P12-E. It does not retroactively validate the old unfiltered certificate, whose counterexample remains correct. P12's192d terminal palette is smaller for a different, weaker cost conclusion; neither numerical constant is silently substituted.

## Grade and boundaries
Complete written author-side candidate with exact finite companions. External independent reviews0; formal-prover runs0; historical novelty UNESTABLISHED; unrestricted prize closures0. No theorem/protocol gate promoted. No q0 or Claude source changed.

The new package is identified by its unique task ID, manifest and detached delivery receipt. Its Drive status is determined only by a NEW actual deployment receipt. Old zero-write receipts are preserved.
