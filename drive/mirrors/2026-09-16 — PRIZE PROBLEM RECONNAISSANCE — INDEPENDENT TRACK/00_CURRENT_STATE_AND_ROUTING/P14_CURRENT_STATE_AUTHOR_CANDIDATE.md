# Phase14 — scalar sandwiches and fractional resource covers

Status: AUTHOR_SIDE_COMPLETE_CANDIDATES; external independent review0; noveltyUNESTABLISHED; originalprizeclosures0. Actual deployment is in a detached receipt.

P14-A: For a decreasing family with no singleton forbidders, a scalar sandwich a(U)<1 => good => a(U)<kappa, with0<=a_i<1 and kappa>=1, yields a K=ceil(408kappa) obstruction cover at prices c<=phi(p) of cost<=phi(Q), all product probabilities. Genuine singleton forbidders add their exact hazards. The generic inflated-price low-Q theorem is also proved.

P14-B: For weighted rows B, a_i=max_j b_ji after singleton/zero-column removal. Any exact lambda>=0 with sum_j lambda_j b_ji>=a_i certifies kappa=sum lambda. Thus raw row count d is replaced by a certified fractional cover width. Matching exact primal/dual vectors establish optimum by weak duality. An optional rational normalization repairs feasible directions, never trusts a floating solver tolerance.

P14-C: A family with binom(2s,s)-2 individually necessary rows has fractional width exactly2. It is not a single weighted-threshold family. At s20,857 coordinates and137846528818 rows admit a generic816-piece bound; a separately proved scalar width409/408 gives409pieces. Actual obstructions are nonempty; a high-probability product instance and exact full generator costs are computed without expanding the row family. This is not a claim that no other earlier method could cover this example.

P14-D/E: K_(m,m) has unbounded fractional and optimized scalar widths despite a proper2-coloring. A universal bounded-width inference is therefore false. Disjoint hierarchy composition works at a common palette determined by the maximum certified width, not by depth or row count.

The general Talagrand conjecture, arbitrary shared-variable composition, q0 analytic uniformity, and external novelty/correctness review remain open. No q0 or Claude source/gate/permissions changed.
