# Phase09 next work — no background jobs promised

1. Review P09-A through P09-D at their exact hashes, particularly the new arbitrary-proper-core induction. Do not treat the finite checker as external review.
2. Complete the applicability/priority comparison: fixed-rank existence is established prior mathematics. Determine whether our explicit bounded-dilution/product-measure tradeoff is already available or improvable from existing results.
3. Stop trying to obtain a universal palette merely by tightening the present uniform-random-color first-moment bound. P09-F supplies a full-premise family for which that expectation grows without bound although a structured two-coloring exists.
4. Develop a correlation-aware coloring or cover compression that can recognize disjoint-block transversal structure. P09-G supplies an exact transfer interface for complete occupancy lifts, not an assertion that all hypergraphs have that form.
5. Search for a decomposition theorem separating template-like structure from a genuinely low-cost residual. State the loss and test every failure mode; do not assume a decomposition from examples.
6. Alternatively derive a valid fractional certificate and compare it against the exact available smallness-rounding theorem. Do not cite pair-supported rounding before its hypotheses are supplied.
7. Keep K_r's rank dependence explicit. Reducing a constant for one rank is not closing the universal-quantifier gap.
8. When a write-capable action is available, apply drive_apply/PLAN.json additively and hash/read back every new object. The current run performed no remote writes and must not be retroactively relabeled as a successful deployment.
9. Preserve the Phase08 optimized-assert counterexample as a regression case in the architecture metrics. Introduction time, full blast radius, and propagation depth are unknown; do not infer them from file creation timestamps.
10. No interference with Claude's q0 work, no research-grade promotions, no prize claims, and no external submissions without their separate evidence/authorization requirements.
