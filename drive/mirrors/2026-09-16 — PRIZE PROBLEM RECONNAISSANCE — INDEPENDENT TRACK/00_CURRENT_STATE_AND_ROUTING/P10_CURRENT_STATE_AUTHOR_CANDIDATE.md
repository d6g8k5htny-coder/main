# Phase10 current mathematical state — author-side, review pending

## New complete written arguments

- P10-A: exact common-palette substitution on disjoint supports. U is k-coverable in the composite family iff its k-hard child index set is k-coverable in the outer family.
- P10-B: a capped-hazard scalar inequality for every independent Bernoulli vector, every finite threshold arity, and every K>=64.
- P10-C: fixed64-piece, no-dilution cover theorem for arbitrary-depth read-once threshold hierarchies with optional arbitrary monotone zero-preserving gates of arity<=64. Product-measure cover cost <=min(1,-log(mu(D))); mu(D)>=3/4 gives cost<3/10. Maximal witness rank and number of coordinates are unrestricted within this structural class.
- P10-D: same64-piece theorem survives an exceptional cause cover of cost<=1/5 at good probability>=3/4. A sufficient example is a repeated-coordinate core of total probability<=1/5 whose deletion leaves a valid disjoint tree.

## Important exclusions

The read-once/disjoint-support hypothesis is load-bearing. Arbitrary crossing hypergraphs need not have such a representation. The path on65 vertices gives a representation counterexample, not a counterexample to Talagrand or to earlier graph results. The threshold tree theorem is not an all-decreasing-family theorem and does not supersede the scope of the Phase09 all-rank result.

The constant64 is conservative, not optimal. There is no external review, formal proof, novelty certification, original prize solution, or change to q0 scientific status.

## Evidence

Written induction handles all depths, arities and ranks. Exact finite experiments cover substitution identities, direct versus recursive probabilities, actual partition witnesses, generator completeness, second moments, large-arity nonvacuous scalar tests and structural examples. Normal/optimized exact results agree; twelve mutation classes reject in both modes; two intentional survivor runs exit0 and are correctly NOT counted as rejections.

## Drive custody

Phase09's19 original objects were uploaded and re-fetched byte-identically during this execution. This is a new deployment receipt; the earlier historical receipt saying zero writes is not edited. Phase10's own deployment receipt is detached from its frozen ZIP and must be read separately for final remote custody.
