# Phase11 current mathematical state

## Strongest new written results

P11-A. For any finite downset with minimal forbidden rank<=2, independent probabilities p, and arbitrary cost weights0<=w_i<=phi(p_i), phi(x)=min(1,-log(1-x)), its64-fold-union obstruction has a generator cover of w-cost<=phi(1-mu_p(D)). No restriction on graph size, overlap or degree. This closes P10's graph-primitive potential interface at author-side proof level, not merely a fixed-probability smallness statement.

P11-B. The same all-probability transformed-cost conclusion holds for minimal forbidden rank<=3 with K=262144. The proof works for arbitrary independent p_i. With no singleton witnesses and0<H=-log mu_p(D)<1, its constructed cost is <(9437/12288)H. No dilution or codegree assumption.

P11-C. P10's read-once threshold hierarchy may now contain arbitrary-arity graph gates without increasingK=64. It may contain arbitrary-arity rank<=3 gates at fixedK=262144. Both statements are uniform in depth, original witness rank and ground-set size; original coordinates must still occur once. Cheap exceptional-cover stability remains valid.

P11-D. Exact counterexamples preserve disjoint-palette and product-intersection requirements, weighted rather than raw codegree, asymmetric sprinkling, and zero-cost-generator semantics.

## Grade

Complete author-side mathematical derivations with exact finite companions. External independent reviews0. Formal-prover runs0. Historical noveltyUNESTABLISHED. Original prize closures0. No q0/Claude or global research-grade transitions.

Correctness and novelty are different. Prior Kahn--Kalai already gives bounded-rank existence with a different dilution tradeoff. Prior pair-supported Talagrand rounding must be credited. These notes concern a stronger pointwise potential interface suitable for repeated composition.

## Unresolved universal gap

There is no proof here that every downset is a read-once hierarchy of the permitted primitives plus an explicitly cheap discrepancy. Arbitrary primitive rank/arity and uncontrolled shared coordinates remain outside the uniform-palette conclusion. Phase09's general all-rank result and these structural results retain separate hypotheses.

## Portfolio coverage

PORTFOLIO_CLOSURE_MAP.md records the bounded current-state survey. It is not a claim to have reread all underlying q0 proofs. The D1 v2.2/v2.3/RN-UNIF and LPW-v4 status layers remain unchanged.
