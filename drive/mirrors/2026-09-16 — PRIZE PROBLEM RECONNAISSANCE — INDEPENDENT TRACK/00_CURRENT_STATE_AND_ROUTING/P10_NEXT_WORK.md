# Next mathematical boundary — no background jobs promised

1. Review P10-A--D separately at exact hashes. In particular attack shared-coordinate leakage, zero-cost versus empty-family semantics, impossible colorings, and the scalar threshold tail inequality.
2. Complete the priority comparison for fixed-palette/no-dilution bounds on read-once threshold trees. Established read-once Boolean-function theory and threshold literature are not our invention. A bounded search is not a novelty certificate.
3. Seek a universal gate-level cover inequality compatible with the potential phi(q)=min(1,-log(1-q)). A theorem only at small total q is not automatically composable; the invariant must hold at every child probability or specify a safe reduction.
4. Extend beyond disjoint supports through an explicit exceptional-cover budget, not by pretending shared children are independent. The current cheap-core threshold is a sufficient condition, not a structural theorem guaranteeing such a core.
5. Investigate graph-like and bounded-rank gates as new primitive gates whose weighted cover cost can satisfy a stable potential inequality. A constant smallness result alone need not supply the stronger potential domination needed for arbitrary depth.
6. Try to find a decomposition of a general monotone family into a disjoint hierarchy plus a cheaply covered exceptional discrepancy. State the total exceptional budget. There is no claim this exists generally.
7. Do not spend the next campaign merely optimizing64. The large universal gap is representation/composable cover control, not the size of this conservative constant.
8. Preserve Phase09's random-color counterexample: the new hierarchy solves that example by correlated block coloring, but one success on a template does not establish a theorem for arbitrary high-rank overlap.
9. No q0 changes, external submissions, prize-success percentages, or automatic scientific promotion. Historical/source novelty and correctness remain distinct review questions.
