# Development, negative results and evidence boundaries

The current user required Drive updating first. The previously local continuation-audit ZIP, original receipt, manifest, exact audit/endpoint mirrors, new actual deployment receipt and navigation addendum were uploaded and byte-readback verified before this research. Old zero-write receipts were preserved, not rewritten.

Current Drive already had Phase12. We read its terminal weighted theorem and explicit failure of prefix-to-hazard reuse, rather than repeating Phase11 or changing another worker's sources. No private data were placed in public search queries.

The initial unfiltered prefix is deliberately retained as a failed candidate: it can spend O(p) on singleton generators when the actual failure probability is O(p^2). P13 changes the generator family itself, not only its claimed bound. All retained regular generators have scalar weight>=4d and hence an original bad seed in roughly their top quarter.

Zero-probability coordinates, strict capacity equality, original resource versus max-coefficient surrogate, and shared-coordinate cloning are tested. No numerical count of tested ranks or dimensions is used to prove an unbounded quantifier. The all-size theorem uses the written seed count, mean bound, geometric tail and common-palette induction.

The12 negative controls are exact deliberately false finite variants. They are not claimed to mutate every theorem line. A CLI/import/runtime error would exit2 or fail the runner and would not be counted as a rejection. Two intentional survivor probes exit0 with UNDETECTED and are excluded from the24 rejections. No external reviewer or agent swarm was run.

The preliminary finite suite passed; a later addition supplied a non-cardinality817-variable, nonempty-obstruction example. Only the final suite's receipt controls the final counts. Normal and optimized outputs match. No executor timeout occurred in the final suite of this pass; any limits in predecessor logs remain predecessor history.

The four large exact examples are not generic exhaustive hypergraph searches. Three have cardinality-like max weights, while one has408 weights2/5 and409 weights7/10, so two light elements are good but a light+heavy pair is bad. The complete817-element set is not408-coverable because each good set has at most two elements. Costs/probabilities are exact rational binomial calculations; displayed decimals are diagnostics and their full fractions are reproducible from the code.

The formal result permits arbitrary finite real coefficients; executable instances use Fractions. Generator enumeration may be exponential. No runtime or polynomial-size certificate theorem is claimed.
