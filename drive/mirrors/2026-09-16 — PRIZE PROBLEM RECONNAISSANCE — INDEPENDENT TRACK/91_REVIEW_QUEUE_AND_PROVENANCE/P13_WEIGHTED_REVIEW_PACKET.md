# P13-WEIGHTED exact-object hostile review

Review each manifest-bound source separately. Author exposure includes P12-A/B/C/E and the sources in SOURCE_REGISTER. No external, blind or organizational-independence credit is claimed.

R1. Exact-cell normalization: prove sum_{S bad} p_S = product(1+p_i) P_{p/(1+p)}(bad), with all outside-prefix coordinates fixed absent. Compare to the original increasing event by stochastic domination. Do not replace product(1+p) by product(1-p), or exact outcomes by overlapping containment events.
R2. Seed-extension count: a deterministic bad s-seed and its disjoint extension determine each k-generator. Extra descriptions only enlarge an upper bound. Preserve A^k, lambda^(k-s)/(k-s)!, all p=0/1 cases, and no division by Q.
R3. Filtered-prefix coverage: order by descending max-resource coefficient, cut off by cumulative ORIGINAL probabilities, retain only generators of scalar load>=4d. Verify the start index k0, integration on p-mass intervals, and the L+RM bound, including zero-length intervals.
R4. Original bad seed: k>=4d+1, s=ceil(k/4), m=floor(3k/4). The seed's max-coefficient sum>=d forces one of the ORIGINAL d rows to violate. Max-coefficient load>=1 alone would not suffice for multiple rows.
R5. Geometric comparison: check k<=2m, e<3, exp(2/R)<=1/(1-2/R), R=ceil(12A^2)+2, multiplicity<=2, and total2^(2-3d). Finite integer tables do not replace the written all-k proof.
R6. Mean/packing: derive m_j<4 at Q<=2/3; handle degenerate variance; prove strict-bin next-fit implication. Scalar-max packing is sufficient, not equivalent to multidimensional packing. Then K/2=4d(R+1)>4d+RM.
R7. Singleton and potential endpoints: factor good probability exactly after removing all singleton forbidders. Use c_i<=hazard(p_i), not just2p_i, on singletons. Treat Q=0, Q=1, p_i=0, p_i=1, empty generator cost1 versus zero-cost generators.
R8. Composition: verify both directions of the common-palette identity, exact Cartesian cover-cost lifting, independence only on DISJOINT original supports, and no palette multiplication with depth. Original failure probabilities are not child cover costs.
R9. Implementation: run code/reproduce.py normally; it itself executes ordinary/-O companions,12 false-claim variants both modes, and intentional survivors. Unexpected CLI/import failures do not count as mathematical rejections. Small-K identity checks are distinct from large-palette theorem tests. Check the non-cardinality817-coordinate example.
R10. Scope/priority: compare FKP Proposition2.1, P12 prefix and terminal statement, and Ouimet's separate projection/weighted-threshold preprint. Do not claim original singleton rounding, unrestricted Talagrand closure, or new bounded-rank existence. New-to-this-project is not historical originality.
R11. Custody: preserve original P11 snapshots, old failed-write receipts, continuation audit, and current P12 source. Current-user sequencing required Drive audit update before new work; inspect the new actual receipt.

Return PASS/AMEND/FAIL/CANNOT-VERIFY with exact source hashes and full reasoning. A mathematical correctness review, a novelty verdict and a deployment check are distinct.
