# Phase12 exact-object hostile review packet

Targets: the six proof files and hashes in MANIFEST.sha256. Author exposure includes
Phase11 and all stated primary sources. No blind or external independence credit.

R1. P12-A: descending coefficient order, cumulative heterogeneous prices, strict
weighted excess, zero prices, empty generators, prefix truncation, factorial cost,
and integral comparison on intervals that can include zero-length coordinate bins.
Compare directly to Talagrand's prior singleton proof; decide correctness and
historical novelty separately.

R2. P12-B/C: good capacity is STRICT <1. Verify next-fit's even/odd K argument and
Cantelli's direction. Separate individually forbidden items before mean control.
Use maximum resource weights, not minimum. Keep M_c<=2M_p and dimension d. The
nonpackability implication is one-sided, not an equivalence.

R3. P12-D: original child probabilities are independent only on disjoint supports;
child cover costs are different inputs. Prove common-palette substitution in both
directions. Verify duplicate generator descriptions only enlarge the upper cost.
The cheap-exception premise is cover cost, not event probability.

R4. P12-E: verify n193,a=3/5,p=10^-8,K192 directly. The full set is the ONLY K192
obstruction. Prefix singleton cost fails phi(Q) although the full-set cover is
excellent. This does not rule out a stronger existence theorem or structured cover.

R5. P12-F: exact outcomes X cap W=e are disjoint; containment events are not. Check
odds conversion, phi<=odds, minimum versus maximum witness rank, and K^(1-r).
Singleton hazards and irrelevant ports must be separated. q=0 and zero-cost
nonempty generators cannot be silently deleted. The condition forces q<1 only
on relevant nonsingleton ports; q=1 singleton cases are handled by the trivial cap.

R6. Component extension: no witness crosses connected components. Same K colors
can be reused; independent good probabilities multiply; costs add and are bounded
by summed hazards. Do not use independence for overlapping components.

R7. Nonvacuity: general rank-r family excludes every r-set except one on Kr+1
vertices. Verify maximal good cardinality r and nonempty K-obstruction. Large-r
families are mathematically specified, NOT exhaustively generated by the checker.
Disjoint-edge example refutes automatic-global-empty-mass inference only.

R8. Rebuild with code/run_all.py in a new output directory. Ordinary/-O outputs
must match; 14 faults must trigger the intended explicit guard, not arbitrary
crashes. Two intentional survivors must remain UNDETECTED, exit0. Zero assert
nodes. Mathematical proofs carry unbounded quantifiers, not the test counts.

R9. Custody: confirm UPDATE_FIRST_GATE predates research/recon; keep two distinct
Phase11 snapshots separate. Match readbacks to the exact declared phase digest.
No promotion from previous or current receipt status.

Return PASS/AMEND/FAIL/CANNOT-VERIFY per interface, exact hashes, objections and
actual independently written/executed checks. Separate correctness, scope,
priority, exposure and organizational independence.
