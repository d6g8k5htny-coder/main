# Phase09 exact-object review packet

Review the hashes in MANIFEST.sha256. The author was exposed to earlier proof routes and to the cited literature. This package has no blind-review or external-independence credit.

R1. Base lemma: inspect graph ordered-pair factors, hypergraph dependency weights, Cauchy--Schwarz, and the direction of the inferred expected-count bounds.

R2. Peeling: verify deterministic vertex selection, deletion of earlier core coordinates before defining a link, disjoint first-core-selected events, and product-to-singleton-cost conversion. Attack arbitrary coordinate probabilities, singleton forbidden sets, empty families, and strict/equality boundaries.

R3. Robust rank-three base: verify each of the 100/297, 49/50, 900/197, 100/121, 330, and 331/2 bounds and all parameter powers. Confirm the statement holds at good probability2/3, not merely3/4.

R4. Core sprinkling: a core is admitted by ACTUAL completion probability or a proved lower bound. Verify selection depends on the first sample only. Check antichain minimalization and prove all remaining proper links satisfy the stated cap. Expected link count alone is insufficient.

R5. Moment recursion: partition by exact intersections in a uniform hypergraph. Check the ordered-partition formula, factorial bound, and the use of each larger-core link. No assumption on all-rank concentration or independent overlapping links is allowed.

R6. Rank four: rederive the1015/3 residual moment,4^(-3) monochromatic factor,4^(-4) parameter factor,6400 product palette, and exact final budget. Verify the construction covers ALL bad induced sets.

R7. All-rank induction: check epsilon_r/a_r=epsilon_(r-1), L_r=L_(r-1)/a_r, q+t-qt=p, and q>=a_r p coordinatewise. Audit the potentially LARGE change-of-measure factor (r^2/L_r)^r. Check the residual budget for every r, not just a test table. Preserve the quantifier order on K_r.

R8. Counterexample family: show why a random-color expectation can be huge even though the optimal two-coloring has cost zero. Distinguish the weaker one-parameter example from the stronger family satisfying the original probability premise. No universal impossibility claim for structured colorings is justified.

R9. Block compression: verify exact Cartesian completeness, disjoint blocks, generator sum versus occupancy probability, two heavy-block pieces, and the additional TEMPLATE-cover hypothesis.

R10. Computation: reconstruct exact outputs; verify -O does not disable guards; run all16 mutation classes. Do not interpret the finite tests as an exhaustive theorem proof or count same-author code paths as independent external reviews.

R11. Prior work: compare the explicit bounded-dilution/rank-dependent-palette tradeoff with Park--Pham, nonuniform extensions, hypergraph containers, and smallness results. Do not claim new bounded-rank existence, which is already available. An exact phrase search is not novelty verification.

R12. Custody: confirm Phase08 hashes, distinguish old development snapshots from the final checker, and preserve the record that this execution made zero remote writes.

Return separate PASS / AMEND / FAIL / CANNOT-VERIFY for each applicable interface, exact reviewed file hashes, full objections/proofs, independently authored code if any, and exposure/provenance. A correctness verdict and a novelty verdict are different objects.
