# Independent review packet — Phase02

Review the five proof files and their frozen hashes in MANIFEST.sha256. Request separate mathematical and numerical dispositions. The current grade is author-side complete, not externally endorsed.

R1. Mixed-radix semantics: integer membership, zero padding, shared-prefix argument, composite periods, and infinite-descent AP exclusion. Attempt an AP crossing varying-radix children. Do not replace nonzero modular differences with coprime differences or four distinct residues.

R2. Infinite objective: exact removal of the zero pole, regularity at a=0, recursion, finite-menu uniform convergence, and identification of the Bellman fixed point with the supremum over arbitrarily history-dependent trees. Prove attainment, not just a finite-depth maximum.

R3. Small-radix exhaustive certificate: inspect both enumerators and the precise local weight inequality. Verify all2..30 cases and grid maxima, not only the displayed winning alphabets. Check coordinatewise domination at11/22 and the 2-Lipschitz full-cell correction. Audit every rational rounding and integer overflow range.

R4. Single-switch theorem: scrutinize the a.e. derivative argument for U, which need not be smooth. Check q_min/q_max envelopes, the b^-2 factor, every between-grid parent interval, strict monotonicity, and the exact root signs. A sampled derivative plot is insufficient.

R5. Explicit policy: independently implement integer membership and show its actual infinite sum exceeds the DP lower endpoint. Distinguish its stored rounded state from the unknown exact-threshold policy. Confirm the64 layers and441 runs are fully specified.

R6. Expanded Walker menu: verify the exact larger alphabets, all42 affine images, compact shift domain, and uniform local inequalities. Do not generalize from these specified alphabets to all alphabets at those bases. Verify how B55-B0 bounds transfer the entire small-radix certificate.

R7. Infinite-sum certificates and sandwich: independently derive the centered-moment recurrence/remainder, the explicit branch mixture, and H-rho <=(1-rho)S<=H. Confirm every published decimal is outward-rounded from an exact fraction.

R8. Literature and novelty: compare Walker2022/2025, the older digit-AP and summation literature, and the recent computo ergo sum first-party report. A correct local derivation is not automatically a new theorem. Evaluate the adaptive optimum and strict-threshold statement separately from the general Bellman architecture and the already-known digital reformulation.

R9. Scope: no solution of Erdos #3, #142, or #169; no improved unrestricted harmonic record; no polynomial bound on r_4(N) outside the specified tree family; no claim of a constant uniform over unbounded bases. Try to identify any prose allowing those misreadings.

Required return: exact target hashes, separate PASS/AMEND/FAIL/CANNOT-VERIFY per interface, independently written scripts if any, all numerical enclosures and transcripts, exposure/provenance description, and no pooled confidence score pretending to measure truth.
