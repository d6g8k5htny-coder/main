# P15 exact-object hostile review packet

The author was exposed to P14/P11 and the public sources named in the source register. This is not a blind derivation. Return correctness, scope, novelty and organizational independence separately.

R1 Critical threshold: prove clipping at1 preserves bad constraints; verify compact attainment; distinguish strict kappa>alpha from kappa>=alpha. Check the constructive rational mixture and all-singletons-good assumption. Verify each dual support set, marginal inequality, normalization and objective.

R2 Localization: every local family must be the actual restriction. Check the full intersection of the palettes touched by EVERY cross minimal witness. Pairwise intersections need not be empty. Prove union-cover containment setwise even with singleton obstructions, empty local parts and zero prices. Check global-good probability <= product local-good probabilities, not the reverse or equality.

R3 Necessity claim: it concerns safety of EVERY permissible locally proper coloring, not existence of some global coloring. Use a crossing minimal witness at one common color. Do not claim failure of this certificate refutes a desired cover.

R4 Even-cycle family: exact minimality/rank, local restrictions, connectedness, independent-set bound on high blocks, matching primal/dual alpha, canonical fractional-row width,817 required parts inside one block, failure of local-bit representation, all-s probability and empty-mass estimates, transfer-matrix trace. The large implicit family must not be labeled enumerated.

R5 Complete-transversal transfer: verify original occupancy probabilities, capped singleton-cover costs <=phi(q), and Q_G<=Q ONLY under complete cross lifting. Review every use of the attached P11 generic-inflation source. Twofold refinement uses actual edge generators, not singletons. Cartesian cover costs multiply only on disjoint coordinate blocks. Local-good and macro-good events are generally dependent; the181/192 budget must not use their product as the original good probability.

R6 Singleton/zero cases: remove true singleton forbidden coordinates, factor their absent probability exactly, and retain zero-price generators. If no coordinates remain, the remaining obstruction is empty. Empty generator costs1; empty FAMILY costs0.

R7 Computational evidence: reconstruct ordinary and optimized baselines, all14 false-variant families, intentional survivor and CLI inputs. A syntax/import/argument failure is not a mathematical rejection. Batch receipts are source/hash bound; incomplete wrappers are not completed runs. Finite local demands are separate from huge theorem palette constants.

R8 Scope: the construction does not prove that an arbitrary hypergraph admits the needed partition/palettes or complete-transversal representation. It does not close Talagrand or any q0 gate. Stronger overlapping formulas need separately proved transfer rules.

Return PASS/AMEND/FAIL/CANNOT-VERIFY for each interface with actual executed scripts and exact input/output hashes. No pooled confidence percentage or external-review credit from same-author replay.
