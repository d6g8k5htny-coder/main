# Phase10 exact-object hostile review packet

Grade: author-side complete. Exposure: author had access to Phase09 and all stated primary sources. No blind reconstruction or external review credit is claimed.

R1. Verify P10-A's equivalence in BOTH directions for arbitrary outer downsets and arbitrary child downsets on disjoint coordinates. Check impossible k-coverability, singleton forbidden ports, empty parts and zero-padding conventions. A common palette must not silently use product colors.

R2. Verify the exact generator-cover composition. Duplicate descriptions may increase the summed upper cost, not decrease required coverage. Cost0 need not mean empty cover. An empty generator has cost1, unlike an empty family with cost0.

R3. Verify P10-B for all q_i in[0,1] and all s,n. Check Q=0,Q=1,s=1,m>n; the mean bound; exact-intersection second moment; factorial moment versus tail inversion; the s-to-m factor s!/m!; and both parameter2^m and exponent32s. No sampled-q interpolation proves this theorem.

R4. Verify the potential induction. q_i is the ORIGINAL child failure probability, while covers target child K-piece obstructions. Do not exchange those. Check arbitrary zero-preserving gates up to64, all probabilities including0 and1, and the absence of depth-dependent losses.

R5. Verify P10-D's exceptional-cover containment and repeated-variable-core reduction. The discrepancy need not equal the exception upset but must be contained in it. The claim must not be inferred from small discrepancy PROBABILITY alone.

R6. Verify expression-class strictness using the path P65, including reduction of irrelevant/unary ports and false-twin reasoning. It is a representation limitation, not a counterexample to the conjecture.

R7. Rebuild exact results from frozen source in ordinary and optimized modes. Check absence of removable asserts, integer/Fraction arithmetic, input validators, failure exits, actual partitions and separately computed finite probabilities. The two intentional survivor probes MUST be recorded as UNDETECTED/exit0, not rejected faults.

R8. Scope/novelty: compare current Talagrand definitions, Kahn--Kalai, classical read-once theory and threshold circuits. No claim of new general bounded-rank existence, no all-hypergraph theorem and no prize closure. Gate arity implementation cap16 is distinct from the written64 theorem.

R9. Custody: compare Phase09's original ZIP and19 deployed objects, retain original zero-write historical receipts, and examine separately the current deployment receipts. Hash match is not proof approval.

Return per-interface PASS/AMEND/FAIL/CANNOT-VERIFY, exact target hashes, full objections, scripts and real execution receipts. Do not pool grades into a probability of truth.
