# P11 exact-object review packet

Bind all proof/code inputs by MANIFEST.sha256. This is an exposed same-author continuation, not a blind review. Return correctness, scope, provenance, novelty, and computational integrity separately.

R1 Graph potential. Prove the core product floor>=1-(7/6)Q>=(1-Q)^2 for0<H<1, and the bound sum_C w<=2H. Verify ordered-edge factors in EY^2 and mu<=5(e^H-1).
R2 Core coloring. The core is COLORED, not paid as singleton generators. Verify cost quadratic inH, eight/56 disjoint palettes, no uncovered crossing edge, and27/28H.
R3 High pairs. At rank3 use weighted DISTINCT extension probabilities>10, q=p/2,t=p/(2-p). Verify first-sample measurable edge selection, independent extension hit>99/100, QP<=100Q/99, HP<=100H/97.
R4 Inflated costs. In high-pair graph w<=4q, producing pair factor16; in low triples w<=2p, producing factor8. Verify no illicit substitution of w for event probabilities.
R5 Low triple peeling. Link graph weighted degree<=10; mass>84 gives hit>4/5. Previous core vertices are deleted. Verify residual dependency282=252+30, including exact intersection decomposition.
R6 Palette/cost arithmetic. Graph1024 from256+768; low256 from128+128; product262144. Costs ADD to9437/12288H, not multiply. Check all strict inequalities and boundaries.
R7 Singletons/empty/full/zero/unit probabilities. Minimal singleton witnesses are disjoint from other minimal witnesses. Hazard factors accordingly. Empty generator costs1; zero-weight generators must not be removed just because cost0.
R8 Composition. Reuse frozen P10-A/B at exact hashes; verify common-palette identity for disjoint children, and all-probability w_i=c_i<=phi(q_i) premises. The graph gate family is arbitrary arity, not just the old64-input class.
R9 Constructive code. Inspect deletion/contraction against direct probabilities, conditional-expectation derandomization, symbolic generator products, and exact exponential upper certificate. Large K means some small obstruction tests are vacuous; no hidden claim of exhaustive theorem verification.
R10 Negative controls. All12 real faults must reject in normal/-O modes, while the intentional survivor must be UNDETECTED/exit0. Production code must contain no removable asserts. Test normal/-O is not external proof review.
R11 Scope counterexamples. Triangle intersection, shared original coordinate, wrong second sprinkle, raw count versus weighted mass. Attempt new counterexamples at cost weights strictly larger than original probabilities.
R12 Prior work and status. No new generic bounded-rank existence claim. P11 conclusions are author-side and noveltyunestablished. The whole-Drive inventory is a routing snapshot, not a validation of every proof. No q0 status change is claimed.
