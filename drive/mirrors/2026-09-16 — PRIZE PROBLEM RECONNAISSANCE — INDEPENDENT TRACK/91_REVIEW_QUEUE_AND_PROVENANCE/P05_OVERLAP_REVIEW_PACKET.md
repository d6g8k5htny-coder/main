# Independent review request — Phase05

Review the frozen hashes in MANIFEST.sha256. This is a single-author implementation/proof package; ordinary/optimized replay is not independent review. Return PASS, AMEND, FAIL, or CANNOT-VERIFY separately per interface. Distinguish a counterexample from missing provenance or uncertain novelty.

R1. PR-TAL-003: exact zero-scope extraction, residual probability factorization, redundancy pruning, log capacity budgets, the four small-rank rational estimates, all-r>=5 factorial bound, geometric summation, and the five-copy laminar conclusion. Do not charge nested scopes as independent or drop rank-zero cost.

R2. PR-TAL-004: deterministic greedy coverage with K=d(t-1)+1. Attack off-by-one, positive-capacity requirement, all nonchosen colors, and the distinction between inclusion and equality. Test the crossing triangle. The constant d is measured after a valid structural normalization, not assumed away.

R3. PR-TAL-004/006: independently reconstruct fractional Holder/Finner for finite product measures; verify u^d<=product(1-q_B), log directions, and weighted incidence Lambda. Joint independence from nonincident coordinates is not pairwise independence of scope events. Lambda=0 must be handled without division.

R4. PR-TAL-005: clone splitting, regular bipartite completion, Hall matching and collapse. Check parallel edges and zero capacities. Audit the exact k-fold decomposition criterion separately from the probability budget. The analogous criterion is false on an odd cycle.

R5. PR-TAL-007: heavy/light restriction, existence of a two-piece partition for heavy coordinates, lifted generator cost, and additive versus multiplicative number of pieces. The whole heavy part need not be feasible. No general efficient algorithm is claimed.

R6. PR-TAL-008 decomposition: check the binomial coloring tail, x_B=(3/4)^r, full dependency-neighbor sum, product lower bound, exact rank100 inequality, and the asymmetric Local Lemma. Confirm the entire argument holds for arbitrary finite ground size/number of scopes and all ranks>=100. Finite resampling examples are not the proof.

R7. PR-TAL-008 smallness: verify the SAME normalized representation satisfies the weighted load for both coloring and cost. Check the no-dilution20-piece theorem,22-piece nonuniform extension, exact zero contribution, and explicit nonvacuous unbounded-incidence family. Condition(H) is a substantive restriction, not guaranteed for all downsets.

R8. Code/custody: independently implement small subset partitions, product probabilities, generators, and edge coloring. Inspect exact Fraction arithmetic, sentinel None for nondecomposability, overlarge K, graph validation, and mutation semantics. Every reported negative test must exit1 with a mathematical counterexample, not an argument-parser or missing-import failure.

R9. Scope/priority: compare primary Talagrand/read-k/Local-Lemma/matroid literature. No original prize, arbitrary-downset conjecture, general finite-field theorem, or q0 status is closed. Classical ingredients are credited. Check whether known results already imply any exact subclass claims or constants.

Required delivery: exact source identities, per-interface judgments, explicit objections or corrections, actual scripts/transcripts, and exposure statement. No number of passing checks is to be translated into a confidence percentage.
