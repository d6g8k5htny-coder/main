# Development record and scope limitations

- P14's frozen archive and104-payload manifest were checked before use. Its replay is separate from the new test totals.
- Reconnaissance identified the scalar optimization with the known critical threshold value. The output credits that fact rather than creating a new name for a known invariant.
- No mathematical counterexample to the four written P15 statements was located in this exposed same-author pass. That is not a correctness or novelty guarantee.
- Palette separation uses the ACTUAL local restrictions and ALL original crossing witnesses. The exact CLI accepts explicit inputs and has a16-vertex local scalar-enumeration cap. This is an implementation bound, not the theorem's dimension limit.
- The two new cross-block methods have different hypotheses. P15-B permits arbitrary crossing hyperedges satisfying palette separation. P15-D avoids a bounded macro chromatic number but requires complete cross-pair lifting from a graph. Neither supersedes every earlier class.
- Large examples are specified and analyzed by formulas. The721977344-coordinate family is NOT expanded; only bounded examples and symbolic parameter inequalities are executed.
- Tests reject false equality of dependent local/macro probabilities; false substitution of occupancy probability for occupancy cover price; false use of a macro probability bound with incomplete lifting; and strict critical-width endpoint errors.
- Initial whole-suite wrapper attempts exceeded tool execution limits. An attempted streaming terminal was unsupported. None of these interrupted wrappers is counted as a completed run. The final runner supports source-bound6-job batches; the collector requires every expected job and compares source and output hashes before issuing PASS. Actual successful batch receipts are supplied.
- Baseline count rose by one after reproduce.py was added because the AST scan also checks that fourth source file for removable assertions. Only final source-bound counts are reported as completed evidence.
- Negative controls are deliberately false certificate/claim variants, not a claim to have mutated every mathematical line. Intentional survivors must emit UNDETECTED/exit0. Unexpected syntax/import/CLI errors do not count as mathematical rejections.
- All computation is same-author. There are no independent external reviewers, no formal proof runs and no q0 status changes. Exact hashes establish byte identity, not proof approval.
