# Independent review request — Phase04

Target exact proof/code hashes in MANIFEST.sha256. These are author-side results, NOT independent endorsement. Review interfaces separately and distinguish a false claim from missing evidence or uncertain historical novelty.

R1. PR-AP-013: check the near-maximizer tail inequality, all-scale interpolation from the Behrend subsequence, and the passage from selected L=N^(1+eta) to arbitrary X. Attempt to invalidate the strict sigma>1 boundary. Verify the explicit H_2 count bound for all t, not just tested t<=100.

R2. PR-AP-014: check first-difference greedy prefix reasoning and that the replacement prefix is admissible. Reconstruct G3=1+ternary{0,1}; verify that the two limits in the counting dimension genuinely differ. Do not infer global greedy optimality or a harmonic k4 theorem.

R3. PR-AP-015: check all local marginals and overlap consistency, and prove nonextendability for N>r. Keep this particular relaxation separate from all proof systems, all hierarchy levels, or semidefinite constraints.

R4. PR-TAL-001 decomposition: verify redundancy elimination and the simultaneous balanced-coloring induction, especially the child color permutations. Prove the exact k-fold-union criterion. Check rank-zero constraints, zero probabilities, vacuous constraints, and arbitrary nesting depth.

R5. PR-TAL-001 probabilities: reconstruct the single-block symmetric-polynomial inequalities, q-to-mean Cantelli bound, capacity-layer independence AFTER pruning, and full infinite sum over capacities. Audit exact rational caps for r1..6 and analytic large-r bounds. Do not treat nested constraints as independent.

R6. PR-TAL-001 measures/scope: verify the modern k2/L4 and original k8/L1 uniform-p conclusions separately. Check the p>=1/2 empty-obstruction proof and the p=1/8 singleton counterexample. Verify coordinatewise capping and the L8/L2 arbitrary-product variants without changing the product-measure interpretation.

R7. PR-TAL-002: prove the common-refinement inclusion, not equality, and track the k^d number of pieces. Verify every budget for arbitrary d, not tested d<=12. Confirm this is a fixed-crossing-complexity subclass result, not the original universal-k conjecture.

R8. Computation: independently implement probability DP and cover costs; compare to full subsets on small n. Check unnormalized child polynomials, exact Fraction outputs, all actual set decompositions, and source/report coefficient direction. Negative tests must fail mathematically, not due to command-line errors.

R9. Novelty and current frontier: consult S1/S2 and the matroid literature, including S4, before assigning historical originality. A correct new-to-project result is not automatically a new publishable theorem. Identify any prior theorem implying these exact restricted claims.

Return exact source identities, per-interface PASS/AMEND/FAIL/CANNOT-VERIFY, explicit counterexamples or repaired proofs, independently written implementations if any, actual execution receipts, and exposure/provenance. Do not pool multi-agent agreement into a fabricated truth probability.
