ARTIFACT ID:       AO48-AUD-021-v1.0

AUTHOR:            Claude Opus 4.8 (Anthropic) — ANTHROPIC-OPUS48

CREATED:           2026-07-21

CLASS:             AUD — independent second-line review of GP-CLS-PROP-006/007/008/009,
                   per OP-CNS-001-R0.2 §E2's independent-review requirement

STATUS:            All four packages ENDORSED FOR HUMAN TERMINAL APPROVAL, each on
                   the basis of fresh recomputation performed for this review and
                   displayed below — not on trust in the cited receipts

AUTHORITY:         none — terminal approval remains Dylan M. Roy's per OP-PROT-004

CANONICAL IMPACT:  NONE

READ BASIS:        the four CLS-PROP packages in full, exactly as filed. The
                   C047R battery source, GP-AUD-011, and CW-RSP-012 cited in
                   008/009 are NOT independently read by me (CW/C047R receipts
                   partly MIME-invisible to my tooling); nothing below leans on
                   them — my verification routes are disjoint from theirs, which
                   is what makes this review additive rather than circular.

---

# §1. GP-CLS-PROP-006 (A₂ fold-gap ℓ = r³/6) — ENDORSED

**Recomputed:** critical points ±√λ; values ±(2/3)λ^{3/2}; gap (4/3)λ^{3/2};
with λ = r²/4, ℓ = (4/3)(r³/8) = r³/6. Four lines, exact. ✓

**Prior independent routes in my own record, disclosed:** I have used this
identity twice from the *other* direction — DER-005 §1's pin check
(\(\tilde P(\mp\tfrac12,0)=0,-\tfrac16\)) and DER-007 §2's divided-difference
rederivation of \(f_{xxx}=2\) *from* the gap \(r^3/6\). Both would have failed
if the identity were wrong. Three routes total.

**Scope boundary endorsed as load-bearing:** confining closure to the canonical
normal form and retaining the κ = 6ℓ/r³ mark for physical fields is exactly
right — it closes the arithmetic while keeping the physical-field coefficient
question open, which is where it belongs.

# §2. GP-CLS-PROP-007 (planar z₀ formula) — ENDORSED

**Recomputed the general identity from scratch:** with X = μ+σZ,
E[X²𝟙{Z≤α}] = (μ²+σ²)Φ(α) − σφ(α)(2μ+σα), and σα = a−μ gives
2μ+σα = μ+a — matching the package's −σ(μ+a)φ(α) exactly. At a=0, μ=−b,
σ=√2: z₀(b) = (b²+2)Φ(b/√2) + √2 b φ(b/√2). ✓ My prior independent
evaluation at b = 6/5 (3.230978…, REG-018 K1 chain) agrees with the package's
50-digit value to my computed precision.

**The scope limit is the most valuable sentence in the package:** permanently
blocking the planar decimal from being cited as a literal side-24 torus
identity — with z₀,L named as a distinct positive object per GP-DER-044 —
is precisely the object-card discipline R0.2 §C6 exists to enforce. Endorsed
with emphasis.

# §3. GP-CLS-PROP-008 (six-pin conditional jet law) — ENDORSED

**Recomputed every displayed matrix from the double-factorial rule**
\((-1)^{|\beta|}(-1)^{m+n}(2m-1)!!(2n-1)!!\): all entries of Σ_PP, Σ_JJ, Σ_JP
confirmed, including the three load-bearing off-diagonals (Cov(f,f_xx)=−1,
Cov(f_x,f_xxx)=−3, Cov(f_xxy,f_yyy)=3). **The regression matrix verifies by a
structure worth recording:** each J-row of Σ_JP is exactly (−1) or (−3) times a
single row of Σ_PP, so Σ_JPΣ_PP⁻¹ is the displayed sparse matrix by
inspection — e.g. row 3: (0,−1,0,0,0,3) = −1·(0,1,0,0,0,−3). Conditional mean
(−b,0,0,0) ✓ (the f_xxx = 2 pin contributes nothing — column 6 of the
regression is zero, the same parity structure I verified in DER-007 §2).
**Schur complement:** diagonal corrections (1,1,1,9) give (2,2,2,6) ✓, and the
raw Cov(f_xxy,f_yyy)=3 is *exactly* cancelled by the f_y conditioning — the
off-diagonal zero is earned, not assumed. diag(2,2,2,6) fully confirmed.

**Consistency note adding value:** this law is the cubic sub-block of the
nine-jet law my DER-007 §2 verified entry-by-entry against GP-DER-019 — the two
computations share the (q,a,w,z) block and agree. **Suggested next queue item:**
the full nine-jet extension (quartic block: means (−3b,0,0,0,3b), variances
(24,6,6,6,96), covariances (−2,−12,+12)) is equally closure-ready on the same
evidence standard — GP-DER-019 + my Schur redo + GP-AUD-039's re-execution —
and closing it alongside 008 retires the whole conditional-law family at once.

# §4. GP-CLS-PROP-009 (collision-Gram determinant) — ENDORSED

**Recomputed all six matrix entries from the source variances (2,2,2,6):**
M11 = s²(3c⁴+3c²s²+s⁴)/24 ✓, M22 = s²(4c²+s²)/2 ✓,
M12 = −cs²(2c²+s²)/4 ✓, M33 = 2s² ✓, M13 = M23 = 0 by source disjointness ✓.
**Determinant factorization expanded independently:**
(3c⁴+3c²s²+s⁴)(4c²+s²) − 3c²(2c²+s²)² = 3c⁴s²+4c²s⁴+s⁶ = s²(c²+s²)(3c²+s²),
giving det M = s⁸(c²+s²)(3c²+s²)/24 ✓. **Negative control verified
analytically:** Var(a_yyy) 6→5 shifts M11 by −s⁶/144 exactly (6−5 draws on the
(s³/12)² coefficient) — the planted perturbation tests a load-bearing entry,
not a pass-string. The s⁸ power and strictly positive cofactors for s≠0 are
the reusable content; endorsed with the declared conventions as binding.

# §5. Census reconciliation and the remaining leaks

Mapping to AO48-REG-018: 007 ⇔ **K1** (evidence chain now five routes);
008 ⇔ the cubic sub-block of **K5**; 006 ⇔ the fold arithmetic underlying K2's
cancellation; 009 is new inventory. **Still leaking, per the operator's own
directive, and cheap:** S4 (the convex-combination identity — single-source
since I filed it, 30 seconds for any second line) and S5 (the \(w\cdot\nabla
f\equiv0\) section-normalization step). I cannot second-check my own work;
these two remain the cheapest unclaimed closures on the board. And the full
nine-jet closure per §3's suggestion.

# §6. E2 checklist, per package

Exact object ✓ · evidence ✓ · **independent review = this file, routes
disjoint from the cited receipts** ✓ · dependencies affected/unaffected
endorsed as stated (in particular: none of the four touches adjacency, P0.2,
Theorem B, or machine state) ✓ · terminal labels endorsed as proposed ✓ ·
human approval REQUIRED and correctly reserved ✓ · additive correction paths ✓.

**Recommendation to the operator:** all four are ratifiable as filed, batch-wise
per your stated preference; each proposed approval statement is accurate to
what was verified.

---

*Cross-references: GP-CLS-PROP-006/007/008/009-v1.0 (full texts) ·
OP-CNS-001-R0.2 §§E2–E5, C6 · AO48-REG-018 §§2–3 · AO48-DER-005 §1 ·
AO48-DER-007 §2 · GP-DER-019 §2 · GP-DER-044 (exact-torus, by Drive ID per B9) ·
OP-PROT-004 · `_RULES_OF_ENGAGEMENT` Rule 1*
