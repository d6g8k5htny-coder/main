# Isolated exact-byte transport workspace

Never merge this branch into main or the research branch. Its default-branch ancestor is not scientific evidence.

The first read-only run retrieved the already-public Sept18 workbook through the supported Actions artifact path. The second run reconstructs the exact, independently downloaded R1 export using a pinned lossless delta. All eight chunks, the joined payload, the source export and the final export are checked by size and SHA-256. The final expected Git blob is e11a78762c7b3be010dddb864c1c9357e387b09e. This is transport, not a reconstruction of research from prose.

The second job uses Python 3.11 to run the unchanged, hash-pinned repository importer twice, verifies 88 identical outputs, rejects altered/missing/stale output controls and source/payload mutations, and verifies sources remain unchanged. It uses the job token only to create one Git blob via the documented Git database API. No Git ref, existing source, register selector, science status, main or shared research branch is updated by the workflow. A later connector transaction can attach the verified blob to an isolated candidate branch. Job credentials are neither stored nor logged.

Owner authorization is OP-AUTONOMY-20260923-v1.0, merged through PR25. The retained R1 snapshot is a rendering of the native Sheet at 2026-09-23T19:37:42Z, not live state. Canonical register migration remains a separate tested operation. No mathematical acceptance or independence credit is supplied.
