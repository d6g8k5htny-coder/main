# Intermediate physical scale

Additive envelope between the reviewed fixed scaled annulus and the reviewed fixed remote window, for one witness at physical distance `s` with `r ≪ s ≪ 1`.

The note is [PROOF.md](PROOF.md), object `D5-INTERMEDIATE-SCALE-20260926-v1`. It does not edit either parent proof. Scientific effect is none: the algebraic guards below are jet identities, and this landing is not an acceptance of the intensity bound or a change to RN, elder, lifetime, or 24-jet status.

The intended Math- location is `frontiers/intermediate_scale_20260926/`. Push of that tree was refused: `cursor[bot]` received HTTP 403, `Permission to d6g8k5htny-coder/Math-.git denied`. This workspace copy is the delivered source. SHA256 of `PROOF.md` is `b15b4395cbaf84bdf5674f44c01f0ffe641ff74f074a2ee7305d3e71ede1f90b` (14340 bytes).

Run the algebraic guards with

```sh
python3 -m unittest notes/intermediate_scale_20260926/test_intermediate_scale.py
```

Those guards check jet minors and the cubic curvature identity. They are not a Gaussian-field test and not an acceptance of the intensity bound.
